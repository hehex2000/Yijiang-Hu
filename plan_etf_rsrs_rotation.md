# 计划：RSRS 宽基 ETF 轮动（量化复现 BV1MS3d6qEZx「14 倍」）+ 反过拟合验证

> 来源视频：BV1MS3d6qEZx《宽基 ETF 轮动策略，9.5 年 14 倍》
> UP：站内私信发代码（非卖课，🟢 推销程度低）｜ 你的评级：**B-（有干货但需自己验证）**
> 目标：**测 RSRS+双均线+ATR 这套方法有没有可转移边际，不是复现 14 倍**（UP 自己承认是从"网传 52 倍→自测 8 倍→优化到 14 倍"调参调出来的）

---

## 0. 策略还原（来自转写，逐字对齐）

| 模块 | 设定（faithful 口径） |
|---|---|
| 候选池 | **8 只宽基 ETF**：沪深300(大盘) / 中证1000(中盘) / 国证2000(小盘) / 创业板50(创业板) / 科创100(科创板) / 恒生(港股) / 纳指(美股) / 黄金 |
| 选股 | **月度选排名第 1 的 1 只**持有（原始版就是"20 日涨跌幅选最强 1 只"） |
| 模块① 双均线过滤 | **MA5 > MA20** 才允许买入（多头排列过滤，下跌趋势不进） |
| 模块② RSRS 复合排序 | 20 日涨跌幅(动量) 与 RSRS(阻力支撑相对强度) **按 6:4 加权**（z-score 标准化后加总排序）；RSRS 权重 0→100% 扫过，**40% 最优** |
| 模块③ ATR 自适应止损 | 每支 ETF 独立阈值（波动大放宽）：黄金/纳指/沪深300/恒生=5%；国证2000=5.4%；中证1000=5.9%；创业板50=7.5%；科创100=8.7% |
| 区间 | 2017-01 ~ 2026-07（近 10 年）｜ 宣称累计 1388%(≈14×) / 年化 32.6% / 最大回撤 30.7% / 夏普 1.25 / 卡玛 1.06 / 胜率 60.3% / 止损触发 17 次 / 约 204 笔交易 |
| 保守替代 | 无过滤 + 0%RS + 8% 固定止损：年化 27% / 回撤 25% |

UP 给的"验证全景"：4 种均线过滤 × 4 种 RS 权重 × 2 种止损模式 = **32 组参数网格**，选最右上（年化最高）那组。

**RSRS 是什么**：光大证券 2017 年公开因子（阻力支撑相对强度），对价格高低点做 OLS 斜率 β，β>1 表示支撑强于阻力、趋势易延续。平台 `run_etf_rotation_v6_phase3.py` 已实装（`V6.RSRS_W` / `W_RSRS` / `W_MOM`），**可直接复用，不必重写**。

---

## 1. 数据可行性（已探库 D:/tu-shareData/astock_daily.db ✅）

8 只候选 ETF 实际上市日（etf_daily MIN(trade_date)）：

| ETF | 代码 | 首交易日 | 备注 |
|---|---|---|---|
| 沪深300 | 510300.SH | 2012-05 | 全区间覆盖 ✅ |
| 中证1000 | 512100.SH | 2016-11 | 2017+ ✅ |
| 创业板50 | 159949.SZ | 2016-07 | 2017+ ✅ |
| 恒生 | 159920.SZ | 2012-10 | ✅ |
| 纳指 | 513100.SH | 2013-05 | ✅ |
| 黄金 | 518880.SH | 2013-07 | ✅ |
| 国证2000 | 159628.SZ | **2022-07** | ⚠️ 2017-2022 不可用 |
| 科创100 | 588030.SH | **2023-09** | ⚠️ 2017-2023 不可用 |

**结论**：
- 8 只全部在本地 DB 有数据，**可 100% 离线实现**（本机 tushare 被墙无影响）。
- 成本：复用平台 `calc_etf_fee`（佣 0.025% + 最低 5 元 + 滑点 0.1%，ETF 免印花税），与 V6 同基准。
- **关键诚实约束（见 §2.3）**：faithful 复现必须用"**上市日闸门**"——ETF 在其首交易日之后才纳入候选池。否则 2017 年就凭空持有了 2023 才上市的科创100，那是真·前视。

---

## 2. 诚实前提（决定整个验证口径，必读）

1. **14 倍不可复现 / 不追求复现**：UP 明说 14 倍 = 从 8 倍(自测)调参优化来的；32 组网格选最优点是典型的 in-sample 过拟合。我们 faithful 实现是**规则选**，拿到 14 倍的概率极低，这是预期内。
2. **选池须事前固定**：不得为美化回挑标的（头号纪律，同刘不牛）。
3. **🔴 结构性 regime 锁定（你那条"前视偏差风险"的物理答复）**：这 8 只宽基 ETF 绝大多数 2012–2016 才上市，科创100/国证2000 更是 2022–2023 才上市。**用这 8 只 ETF，我们物理上无法回测 2017 之前，更不可能测你担心的"2008–2014 动量失效期"**。回测被锁死在 2017–2026 这个牛市偏多的 regime 里——**该策略永远无法自己排除"只是踩中 regime"**。这是它最大的诚实局限，必须在报告里写明。
4. **缓解 §2.3 的备用手段（长样本扩展）**：对"有长历史的宽基腿"改用 `index_daily` 指数代理延长样本——沪深300 指数(000300)自 2005、中证500(000905)自 2005、创业板指(399006)自 2010、恒生自 2004。用指数代理替换对应 ETF 腿，可把 A 股宽基部分拉回到 2008–2016 股灾/震荡 regime，至少部分回应"动量失效期"担忧（但纳指/黄金/科创/国证2000 仍无长历史，且指数≠ETF 有跟踪误差）。列为阶段 2 的**扩展子测试**，非核心。

---

## 3. 实现方案（新建 run_etf_rotation_rsrs.py，复用现有引擎 + RSRS）

**M0 骨架**：以 `run_etf_rotation.py` / `run_etf_rotation_v6_merged.py` 为基座，读 `etf_daily`+`etf_adj_factor`（QDII/商品缺 adj_factor → 沿用刘不牛已验证的 `pct_chg` 累乘复权，避开份额折算断崖），**月频第 5 交易日开盘调仓**，复用 `calc_etf_fee`。

**M1 信号层（faithful）**
- 动量 = `close_adj[t] / close_adj[t-20] - 1`（20 日涨跌幅）
- RSRS = 复用 `run_etf_rotation_v6_phase3` 的 RSRS 计算（对近 `RSRS_W` 日高低点 OLS 斜率 β，z-score 标准化）
- 双均线过滤：`MA5 > MA20` 才允许该标的进入排名（否则视为过滤掉）
- 复合分数 = `0.6·z(动量) + 0.4·z(RSRS)`，横截面排序选第 1
- 无达标标的（全被 MA 过滤）→ 持现金/货币（UP 未给空仓率，实测后定）

**M2 风控层**
- **ATR 自适应止损**：每支 ETF 独立阈值表（见 §0），持仓回撤 ≥ 阈值 → 次日开盘离场 → 现金
- 阈值表做成 dict 入参，便于消融（关止损 / 统一 5% / 统一 8%）

**开关设计**（便于反过拟合消融）：`--no-ma-filter` / `--rsrs-weight 0..1` / `--no-stop` / `--stop-mode atr|fixed` / `--fixed-stop` / `--cost` / `--start` / `--end` / `--use-index-proxy`（长样本扩展）。

---

## 4. 反过拟合验证电池（对齐你评级里的红/黄旗 + 刘不牛同款方法学）

| 编号 | 检验 | 对应你的质疑 |
|---|---|---|
| A | **faithful 复现**：关成本跑，看能否接近 14 倍；再加成本（单边 0.1%×2≈0.2%/笔 ×204 笔≈年化 ~0.4%）看剩多少 | 🔴 交易成本缺失 |
| B | **walk-forward**：选参窗 2017-2021 / 验证窗 2022-2026 隔离；用选参窗的"最优 RS 权重+最优止损"去验证窗跑，看是否退化 | 🔴 过拟合嫌疑 / 你"正确做法②" |
| C | **参数敏感性**：动量窗(10/20/25/40)、RS 权重(0/0.2/0.4/0.6/1.0)、MA 组合(无/MA5>MA20/MA10>MA60/双)、止损(关/ATR/fixed 5%/8%) 单变量隔离 | 🔴 32 组网格 in-sample |
| D | **嵌套 WF 选池**（同刘不牛阶段3）：从"全部 2017 前上市宽基+跨资产 ETF"里 ex-ante 按 2021 前数据挑 top8，测 OOS；对照原 8 只 OOS 排第几百分位 | 🔴 选池是否后视镜（你那句"池子本身有无过拟合"） |
| E | **模块归因消融**：删双均线 / 删 RSRS(纯动量) / 删 ATR 止损 / 删动量(纯 RSRS) → 各自贡献多少 | 验证 RSRS 是否真在加边际（还是装饰） |
| F | **长样本扩展（缓解 §2.3）**：A 股宽基腿换 `index_daily` 代理，拉回 2008–2016，看动量失效期是否塌 | 🔴 你"未含动量失效期" |
| G | **空仓率 + 触发统计**：MA 过滤触发率、止损触发次数（UP 称 17 次）、月度换仓率 | 对齐 UP"每个模块是否真在运行" |

**核心判据**（沿用刘不牛阶段3逻辑）：
- 若 faithful ≈ 14 倍 且 OOS(walk-forward) 不塌 → 方法有效（但仍受 §2.3 regime 锁限制）
- 若 faithful 远不及 14 倍（预期）但 OOS 仍显著正 → 方法有一定边际，14 倍是调参泡沫
- 若 OOS 塌 / 选池 D 显示原 8 只处 90%+ 分位 → 证实过拟合（同 V6 被标红）

---

## 5. 交付物
- `run_etf_rotation_rsrs.py`（策略实现 + 全套开关）
- `etf_rsrs_rotation_report.md`（含 A–G 电池结果 + 与 V6 / 刘不牛 / 沪深300 买入持有对比 + 诚实结论）
- 可选：`run_backtest.bat` 加 `[8] RSRS 宽基轮动` 菜单入口（待你决定，不替换现有）
- 可选：把本视频评级(B-)记入 bilibili 骗子指纹库/已验证知识库（用 bilibili-critical-summary 技能）

---

## 6. 诚实声明 / 风险
- 科创100 / 国证2000 上市日闸门必须做，否则 faithful 即前视（§1 红字）
- RSRS 窗口、动量窗口、双均线组合等 UP 未完全给的参数 = 自由参数，**必须做 C 敏感性，不得为美化挑参**
- **§2.3 regime 锁定是物理限制，任何测试都改不了**——报告须明确"14 倍无法排除踩中 2017–2026 regime"
- 长样本扩展(F)用指数代理 ≠ ETF，结论仅供佐证
- 与 V6 / 刘不牛 关系：三者都是"跨资产/宽基月频轮动 + 动量 + 趋势过滤"兄弟策略；本报告直接三向对比，看 RSRS 是否比 R²/纯动量多一块可转移边际

---

## 7. 里程碑（供评审，未开工）

- **阶段0（~半天）**：M0+M1 faithful 信号（双均线+RSRS60/40），跑通，出裸收益 vs 基准（沪深300 / 中证全指买入持有 / 等权 8 只被动）
- **阶段1（~半天）**：M2 ATR 止损接入 + 成本建模（A 检验的一半）
- **阶段2（~1天）**：反过拟合电池 B–G 全跑（含 walk-forward / 嵌套 WF 选池 / 长样本扩展），写报告
- **阶段3（可选）**：bat 菜单接入 / 三策略(V6·刘不牛·RSRS)同口径对比表

> 待你确认：① 候选池 8 只代码草案（见 §1，含上市日闸门）② 是否现在开工 ③ 长样本扩展(F)做不做（需多动 index_daily 代理，耗时但能回应你最硬的"前视"质疑）
