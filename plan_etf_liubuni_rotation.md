> ❌ **INVALIDATED · 无效策略归档**：本文件对应量化刘不牛「173倍」ETF 轮动 (BV1BqMX6TEx2)，
> 经严谨回测 + 月度调仓 bug 修复后已彻底证伪（关止损 -64.1%、池子 0.0%ile condemned）。
> 保留作证据归档，不作为任何实盘/复现参考。详见 `etf_rebalance_bugfix_reverify.md`。

# 计划：ETF 轮动策略（量化刘不牛「173倍」）复现 + 反过拟合验证

> 来源视频：BV1BqMX6TEx2《从堆砌到精简：我如何把 ETF 轮动策略做到 173 倍收益》
> UP：量化刘不牛（9位老号）｜ 评级：🟢 高（5个视频里质量最高，self-aware 拆自己台）
> 目标：**测方法有没有真实边际，不是复现 173 倍**（UP 自己都标了"特定区间特定参数假设"）

---

## 0. 策略还原（来自总结文档）

| 模块 | 设定 |
|---|---|
| 候选池 | A股宽基 + 行业 + 黄金/原油/纳指/日经/港股 ETF（全球多资产） |
| 信号 | 25 日动量；**R² 趋势线拟合度过滤**（趋势质量门槛） |
| 持仓 | **1 只 ETF**（测过 2-3 只，换仓增加收益下降） |
| 保留 | **90% 阈值**：当前持仓分数 ≥ 第1名的 90% 就不换（降换手） |
| 止损 | **5% 固定** |
| 避险切换 | A股走弱判定 = 4 指数（沪深300 / 小盘 / 创业板 / 中证A500）中 **≥3 个跌破 10 日均线** → 切海外/商品池 |
| 区间 | 5.5 年（具体年份未说）｜ 宣称累计 173 倍（不可信） |

UP 给的验证方法论（四步）：①触发统计（每个模块跑过几次、删掉它结果怎么变）②样本外 ③压力测试（调成本/窗口/阈值）④模拟盘（每个模块是否真在运行）。

---

## 1. 数据可行性（已探库 D:/tu-shareData/astock_daily.db ✅）

- `etf_daily`（2011 只）+ `etf_adj_factor`（有）→ 可用复权价。
- **全球多资产全部可用**：纳指 513100 / 标普500 513500 / 纳指100 159941 / 日经 513520 / 恒生 159920 / 德国 513030 / 法国 513080 / 印度 164824 / 原油 501018 / 黄金 518880 / 豆粕 159985 / 有色 159980 / 国债 511010 / 十年国债 511260 / 货币 511880。
- **A股宽基/行业充足**：红利 510880 / 创业50 159949 等。
- **指数（index_daily）**：沪深300 ✓、创业板指 ✓、中证1000（代"小盘"）✓；**中证A500 缺失 → 用 中证500(000905) 或 中证全指(000985) 替代**（计划内标注）。
- `etf_nav`（54362 行）有 → 若加溢价护栏可选（非核心，本策略不依赖）。
- 成本：复用平台 `calc_etf_fee`（佣 0.025% + 最低5元 + 滑点0.1%，ETF 免印花税），与 V6 同基准。

**结论：策略 100% 可本地实现与回测，无需补外部数据（本机 tushare 被墙也无影响）。**

---

## 2. 诚实前提（必读，决定整个验证口径）

1. **173 倍不可复现**：技能库 §5.17 已把同类 ETF 动量轮动（V6）的 1886% 钉死为过拟合（后视镜选池 + 样本内调参 + 口径未披露）。本策略同属一类，**目标不是复现 173 倍，是测方法本身有没有可转移边际**。
2. **选池须事前固定**：不得为美化结果回挑标的——这是 §5.17 / §6.7 的头号纪律。
3. **动量在 A 股无稳定超额**（§5.9 / §5.8），但本策略是**全球多资产轮动**（类 GTAA / Antonacci Dual Momentum），学术有支撑、真实边际有可能存在——**这才是值得测的点**，不是纯 A 股动量。
4. U P 视频里"1 只 ETF / 173 倍"几乎必然含后视镜挑当期最牛那只的成分；我们 faithful 实现是**规则选**，不会拿到 173 倍，这是预期内。

---

## 3. 实现方案（新建 run_etf_rotation_liubuni.py，复用现有引擎基座）

**M0 骨架**：以 `run_etf_rotation.py` / `run_etf_rotation_v6_merged.py` 为基座，读 `etf_daily`+`etf_adj_factor`，月频（或对齐 UP"周五决策下周一开盘"）调仓，复用 `calc_etf_fee`。

**M1 信号层（faithful）**
- 25 日动量 = `adj_close[t] / adj_close[t-25] - 1`
- R² 趋势质量：对近 N 日（默认 60，待敏感性）`adj_close` 对时间做线性回归，取 R² 作趋势干净度门槛（低 R²=震荡→不买）
- 分数 = 动量原始值（或横截面排名）；R² 过阈才进入排名
- 选 1：分数最高且 R² 过阈

**M2 风控层**
- **90% 保留**：当前持仓分数 ≥ 第1名 ×90% → 留仓不换（降换手，呼应 §5.19"高换手被摩擦吃光"）
- **5% 固定止损**：持仓回撤 ≥5% 离场 → 现金/货基
- **避险切换**：沪深300/中证1000/创业板指/中证500 中 ≥3 跌破 MA10 → 候选池限海外+商品（纳指/日经/恒生/德/法/原油/黄金/国债/货币）
- 无达标标的 → 持 511880（货币）或空仓（UP 未给空仓率，实测后定）

---

## 4. 反过拟合验证电池（对齐 §5.17，本计划重点）

| 编号 | 检验 | 目的 |
|---|---|---|
| A | **扩展池对照**：把手挑池换成"全部 2018 前上市全球 ETF"轮动 | 边缘是否塌（同 §5.17①） |
| B | **walk-forward**：2018-21 训练 / 2022-23 验证 / 2024-26 测试，三窗隔离 | 样本外退化签名 |
| C | **参数敏感性**：25d动量(10/20/25/40/60)、R²窗(30/60/90/120)、R²阈、90%保留(80/90/95/100)、5%止损(3/5/8/10)、MA10(5/10/20) 单变量隔离 | 防为美化挑参 |
| D | **蒙特卡洛选标偏差**：全局 ETF 无放回随机抽 N 只跑同架构 N 次，看手挑池排第几百分位 | 选标运气量化（同 §5.17④） |
| E | **模块归因消融**（UP"触发统计"法）：删 R² 过滤 / 删 90%保留 / 删 5%止损 / 删避险切换 / 删动量 → 各自贡献 | 哪个模块真在贡献、哪个是装饰 |
| F | **空仓率 + 触发统计**：每模块执行次数 | 对齐 UP"模拟盘验证每个模块是否真在运行" |

---

## 5. 交付物
- `run_etf_rotation_liubuni.py`（策略实现，含 `--pool` / `--no-retention` / `--no-stop` / `--no-switch` 等开关便于消融）
- `etf_liubuni_rotation_report.md`（含 A–F 电池结果 + 与 V6 对比 + 诚实结论）
- 可选：`run_backtest.bat` 加 `[6] (V6 刘不牛版)` 菜单入口（待用户决定，不替换现有）

---

## 6. 诚实声明 / 风险
- 中证A500 → 中证500 替代（已在库标注）
- R² 窗口、动量窗口等 UP 未给的参数 = 自由参数，**必须做 C 敏感性，不得为美化挑参**
- 本机网络受限 → 但所需数据全在本地 DB，**可 100% 离线完成**
- 与 V6 关系：V6 = 2只组合+RSRS+MA闸+冷却；本策略 = 1只+25d动量+R²+90%保留+5%止损+全球避险切换。两者是**兄弟策略**，报告里直接对比

---

## 7. 里程碑（供评审，未开工）

- **阶段0（~半天）**：M0+M1 faithful 信号，跑通，出裸收益 + 基准（沪深300 / 中证全指买入持有 / 等权多资产被动）
- **阶段1（~半天）**：M2 风控层（保留 / 止损 / 避险切换）接入
- **阶段2（~1天）**：反过拟合电池 A–F 全跑，写报告
- **阶段3（可选）**：bat 菜单接入 / 与 V6 merged 同口径对比

> 待用户确认：① 候选池草案（见下）② 是否现在开工
