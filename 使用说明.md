# 多因子选股与回测平台使用说明

## 1. 项目概述

本平台是一个完整的 A股量化投资系统，包含两大核心模块：

1. **多因子选股平台**：基于价值、成长、质量、动量、技术、低波动等因子，对沪深300成分股进行评分选股
2. **回测平台**：对选出的股票运行多种量化策略（双均线、海龟、MACD/KDJ、RSI趋势等），评估策略表现

---

## 2. 目录结构

```
multi_factor_selection/
├── config.py                  # 主配置文件（修改后生效）
├── run_selection.py           # 选股主脚本（读取 config.py）
├── run_backtest.py            # 回测主脚本（支持多种策略）
├── run_multi_year_backtest.py # 多年度动态资金回测
├── ml_stock_selector_final.py # ML选股器（可选）
│
├── src/                      # 选股核心模块
│   ├── data_fetcher.py       # 数据获取器（支持本地DB/Tushare/Akshare）
│   ├── factor_calculator.py # 因子计算器（5大类因子）
│   ├── factor_processor.py   # 因子处理器（清洗、标准化、打分）
│   └── stock_selector.py    # 选股器（最终筛选和导出）
│
├── backtest/                 # 回测模块
│   ├── data_loader.py        # 回测数据加载器
│   ├── metrics.py           # 策略评估指标
│   ├── quantstats_reporter.py # QuantStats 报告生成器
│   ├── dual_ma_strategy.py  # 双均线策略
│   ├── turtle_strategy.py   # 海龟策略
│   ├── dca_strategy.py      # 定投策略
│   ├── weekly_dca_strategy.py # 周度定投策略
│   ├── rsi_trend_strategy.py # RSI趋势策略
│   ├── macd_kdj_strategy_fixed.py # MACD/KDJ策略（修复版）
│   ├── bollinger_strategy.py # 布林带策略
│   ├── rsi_strategy.py      # RSI超买超卖策略
│   ├── smart_switch_strategy.py # 智能切换策略
│   └── macd_rsi_strategy.py # MACD/RSI组合策略
│
├── scripts/                  # 工具脚本
│   ├── calc_industry_momentum.py # 行业动量因子计算
│   └── download_industry_index.py # 行业指数下载
│
└── data/                    # 数据目录
    └── results/             # 选股结果输出目录
```

---

## 3. 配置文件说明 (config.py)

`config.py` 是主配置文件，修改后直接运行 `run_selection.py` 即可生效。

### 3.1 数据源配置

```python
DATA_FETCHER = {
    "primary_source": "local_db",  # 主数据源: "local_db", "akshare", "tushare"
    "tushare_token": None,         # Tushare API token（可选）
    "use_akshare_backup": True,   # 是否使用 Akshare 作为备用
    "use_tushare_backup": False,  # 是否使用 Tushare 作为备用
    "local_db_path": "D:/tu-shareData/astock_daily.db"  # 本地数据库路径
}
```

**说明**：
- 推荐使用 `local_db`（本地 SQLite 数据库），速度最快
- 如果本地数据库没有数据，会自动切换到 Akshare 或 Tushare

### 3.2 因子计算器配置

```python
FACTOR_CALCULATOR = {
    "enable_quality": True,       # 启用质量因子（ROE、毛利率等）
    "enable_momentum": True,      # 启用动量因子（收益率、Alpha等）
    "enable_technical": True,     # 启用技术因子（MA、RSI、MACD等，需TA-Lib）
    "enable_volatility": True,     # 启用低波动因子（换手率、波动率等）
    "enable_money_flow": False,   # 启用资金流因子（历史数据可能不准确）
}
```

**可用因子列表**：

| 因子类型 | 因子名称 | 说明 |
|---------|---------|------|
| 价值因子 | VF1_PE, VF2_PB, VF3_PS, VF4_PEG | 市盈率、市净率、市销率、PEG |
| 成长因子 | GF1_revenue_growth, GF2_profit_growth | 营收增长率、利润增长率 |
| 质量因子 | QF1_ROE, QF2_gross_margin, QF3_ROA | 净资产收益率、毛利率、总资产收益率 |
| 动量因子 | MF1_12M_return, MF2_6M_alpha | 12个月收益率、6个月Alpha |
| 技术因子 | TF1_MA20, TF2_MA60, TF3_RSI, TF4_MACD | 均线、RSI、MACD（需TA-Lib） |
| 低波动因子 | VF1_turnover, VF2_volatility | 换手率、波动率 |

### 3.3 因子处理器配置

```python
FACTOR_PROCESSOR = {
    "winsorize_limits": (0.01, 0.99),  # 去极值范围（去除1%和99%极端值）
    "standardization_method": "zscore"  # 标准化方法: "zscore" 或 "rank"
}
```

### 3.4 选股器配置

```python
STOCK_SELECTOR = {
    "top_n": 20,          # 选股数量（TOP N）
    "min_score": 0.0,      # 最低得分阈值（0.0 = 无限制）
    "scoring_method": "equal_weight"  # 打分方法: "equal_weight" 或 "custom_weight"
}
```

### 3.5 选股日期配置

```python
SELECTION_DATE = "20230103"  # 选股日期（格式：YYYYMMDD）
```

**说明**：用于计算因子和选股的基准日期。通常使用某年的第一个交易日。

### 3.6 行业动量因子配置

```python
ENABLE_INDUSTRY_MOMENTUM = True   # 启用行业动量因子
INDUSTRY_MOMENTUM_LOOKBACK = 6    # 回看月数（默认6个月）
```

**说明**：行业动量因子基于行业指数的历史表现，需要先计算并保存到数据库。

### 3.7 回测配置

```python
BACKTEST = {
    "start_date": "2023-01-01",  # 回测开始日期
    "end_date": "2023-12-31",    # 回测结束日期
    "total_capital": 200000,      # 初始资金（元）
    "benchmark": "hs300"           # 基准指数
}
```

### 3.8 回测策略选择配置

```python
STRATEGIES_TO_TEST = ["RSI", "布林带", "MACD/KDJ", "MACD/RSI", "RSI趋势"]
```

**可选策略**：
- `"买入持有"` - 买入并持有（基准策略）
- `"双均线"` - 双均线策略（MA15/MA60）
- `"海龟"` - 海龟交易策略（突破20日高点）
- `"MACD/RSI"` - MACD + RSI 组合策略
- `"MACD/KDJ"` - MACD + KDJ 组合策略
- `"月定投"` - 月度定投策略
- `"周定投"` - 周度定投策略
- `"智能切换"` - 智能切换策略（根据市场状态切换）
- `"RSI"` - RSI 超买超卖策略
- `"布林带"` - 布林带策略
- `""` (空列表) - 测试所有策略

### 3.9 输出配置

```python
OUTPUT = {
    "output_dir": "data/results",  # 输出目录
    "export_csv": True,            # 是否导出 CSV
    "print_top_n": 20,           # 控制台打印前 N 只
    "save_log": True,             # 是否保存日志
    "log_file": "stock_selection.log"  # 日志文件名
}
```

---

## 4. 选股平台使用指南

### 4.1 快速开始

1. **修改配置文件** `config.py`：
   - 设置 `SELECTION_DATE`（选股日期）
   - 设置 `STOCK_SELECTOR["top_n"]`（选股数量）
   - 设置 `FACTOR_CALCULATOR`（启用哪些因子）

2. **运行选股脚本**：
   ```bash
   cd C:\Users\99395\WorkBuddy\multi_factor_selection
   python run_selection.py
   ```

3. **查看结果**：
   - 选股结果保存到 `data/results/selection_results.csv`
   - 控制台打印 TOP N 股票

### 4.2 选股流程详解

选股流程包含以下步骤（对应 `run_selection.py`）：

1. **计算行业动量因子**（可选，如果 `ENABLE_INDUSTRY_MOMENTUM=True`）
2. **初始化模块**（数据获取器、因子计算器、因子处理器、选股器）
3. **获取沪深300成分股**
4. **计算因子**（对所有沪深300成分股计算配置的因子）
5. **读取行业动量因子**（可选）
6. **处理因子**（清洗、去极值、标准化、打分）
7. **执行选股**（选择 TOP N 股票）
8. **打印和导出结果**

### 4.3 输出结果说明

选股结果 CSV 文件包含以下列：

| 列名 | 说明 |
|------|------|
| rank | 排名 |
| code | 股票代码 |
| name | 股票名称 |
| total_score | 总得分（越高越好） |
| VF1_PE, VF2_PB, ... | 各因子得分 |
| market_cap | 市值 |
| current_price | 当前股价 |

---

## 5. 回测平台使用指南

### 5.1 回测平台架构

回测平台支持多种策略，对选股结果中的股票进行回测，生成策略表现报告。

**核心模块**：
- `backtest/data_loader.py` - 从本地数据库加载股票数据
- `backtest/metrics.py` - 计算策略评估指标（收益率、夏普比率、最大回撤等）
- `backtest/quantstats_reporter.py` - 生成 QuantStats 可视化报告

### 5.2 策略说明

#### 5.2.1 双均线策略 (Dual MA)

**原理**：短期均线突破长期均线时买入，跌破时卖出。

**参数**：
- `ma_short`: 短期均线周期（默认 15 日）
- `ma_long`: 长期均线周期（默认 60 日）
- `take_profit`: 止盈线（默认 25%）
- `stop_loss`: 止损线（默认 10%）

**未来函数**：✅ 无（使用前一日的信号，次日开盘价成交）

#### 5.2.2 海龟策略 (Turtle)

**原理**：突破 N 日高点时买入，跌破 N 日低点时卖出。支持加仓（ pyramiding）。

**参数**：
- `channel_period`: 通道周期（默认 30 日）
- `position_mode`: 仓位模式（`'full'` = 全仓，`'half'` = 半仓）
- `max_positions`: 最大加仓次数（默认 3 次）
- `take_profit`: 止盈线（默认 20%）
- `stop_loss`: 止损线（默认 10%）

**未来函数**：✅ 无（使用前一日的突破信号，次日开盘价成交）

#### 5.2.3 RSI 趋势策略 (RSI Trend)

**原理**：RSI 从下方突破 50 时买入（趋势确认），从上方跌破 50 时卖出。

**参数**：
- `rsi_period`: RSI 计算周期（默认 14）
- `rsi_center`: RSI 中轴线（默认 50）
- `take_profit`: 止盈线（默认 50%）
- `stop_loss`: 止损线（默认 15%）

**未来函数**：✅ 无（使用前一日的 RSI 值，当日开盘价成交）

#### 5.2.4 MACD/KDJ 策略 (MACD/KDJ)

**原理**：MACD 金叉且 KDJ 金叉时买入，MACD 死叉或 KDJ 死叉时卖出。

**参数**：
- `macd_fast`: MACD 快线周期（默认 12）
- `macd_slow`: MACD 慢线周期（默认 26）
- `macd_signal`: MACD 信号线周期（默认 9）
- `kdj_n`: KDJ 计算周期（默认 9）
- `kdj_m1`: KDJ 平滑参数 1（默认 3）
- `kdj_m2`: KDJ 平滑参数 2（默认 3）
- `take_profit`: 止盈线（默认 25%）
- `stop_loss`: 止损线（默认 10%）

**未来函数**：✅ 无（使用前一日的金叉/死叉信号，次日开盘价成交）

#### 5.2.5 布林带策略 (Bollinger)

**原理**：价格触及下轨时买入（均值回归），触及上轨时卖出。

**参数**：
- `boll_period`: 布林带周期（默认 20）
- `boll_std`: 标准差倍数（默认 2.0）
- `take_profit`: 止盈线（默认 15%）
- `stop_loss`: 止损线（默认 10%）

**未来函数**：✅ 无（已修复，使用前一日的布林带信号）

#### 5.2.6 定投策略 (DCA)

**原理**：固定日期定投，忽略价格波动。

**参数**：
- `dca_day`: 定投日（默认每月 1 日）
- `take_profit`: 止盈线（默认 30%）
- `stop_loss`: 止损线（默认 10%）

**未来函数**：✅ 无（定投与价格无关）

### 5.3 运行回测

#### 方法 1：使用 `run_backtest.py`（推荐）

`run_backtest.py` 是回测主脚本，支持多种策略和选股结果。

**使用步骤**：

1. 修改 `config.py` 中的 `STRATEGIES_TO_TEST`（要测试的策略列表）
2. 确保 `data/results/selection_results.csv` 存在（选股结果）
3. 运行回测：
   ```bash
   python run_backtest.py
   ```

**输出**：
- Excel 报告（多个 Sheet）：`backtest/result/backtest_report_YYYYMMDD.xlsx`
- 可视化图表：`backtest/result/plots/`

#### 方法 2：使用 `run_multi_year_backtest.py`（多年度动态资金）

对多年份进行回测，每年底清仓，下年初用剩余资金重新分配。

**使用方法**：

1. 修改脚本中的 `strategies` 列表（要测试的策略）
2. 确保 `data/results/selection_results_YYYYMMDD.csv` 存在
3. 运行回测：
   ```bash
   python run_multi_year_backtest.py
   ```

**输出**：
- 每年度的回测报告：`backtest/result/multi_strategy_report_{strategy}_2022_2023.txt`

### 5.4 回测报告说明

回测报告包含以下指标：

| 指标 | 说明 |
|------|------|
| 总收益率 | (期末资金 - 期初资金) / 期初资金 |
| 年化收益率 | 考虑时间加权 |
| 夏普比率 | 风险调整后收益 |
| 最大回撤 | 最高点到最低点的跌幅 |
| 胜率 | 盈利交易占比 |
| 平均盈利 | 盈利交易的平均收益率 |
| 平均亏损 | 亏损交易的平均收益率 |

---

## 6. 策略参数调优指南

### 6.1 双均线策略参数调优

**目标**：找到最优的短期/长期均线组合。

**方法**：
1. 修改 `backtest/dual_ma_strategy.py` 中的 `ma_short` 和 `ma_long`
2. 运行回测，对比不同参数组合的表现
3. 选择夏普比率最高且最大回撤最小的组合

**推荐参数范围**：
- `ma_short`: 10 ~ 20
- `ma_long`: 50 ~ 120

### 6.2 RSI 趋势策略参数调优

**目标**：找到最优的 RSI 中轴线和止盈/止损线。

**方法**：
1. 修改 `backtest/rsi_trend_strategy.py` 中的 `rsi_center`、`take_profit`、`stop_loss`
2. 运行回测，对比表现

**推荐参数范围**：
- `rsi_center`: 50 ~ 60（越高越稳健）
- `take_profit`: 30% ~ 50%（越高越激进）
- `stop_loss`: 10% ~ 20%（越高越保守）

### 6.3 MACD/KDJ 策略参数调优

**目标**：找到最优的 MACD 和 KDJ 参数组合。

**方法**：
1. 修改 `backtest/macd_kdj_strategy_fixed.py` 中的 `macd_fast`、`macd_slow`、`macd_signal`、`kdj_n`
2. 运行回测，对比表现

**推荐参数范围**：
- `macd_fast`: 10 ~ 12
- `macd_slow`: 24 ~ 26
- `macd_signal`: 9 ~ 12
- `kdj_n`: 9 ~ 14

---

## 7. 常见问题 (FAQ)

### 7.1 选股结果为空或只有几只股票？

**可能原因**：
1. 本地数据库中没有对应日期的数据 → 检查数据库文件是否存在，数据是否完整
2. 因子计算失败（网络问题或数据缺失）→ 检查日志文件 `stock_selection.log`
3. `min_score` 设置过高 → 降低 `min_score` 或设为 0.0

**解决方法**：
- 确保 `DATA_FETCHER["local_db_path"]` 指向正确的数据库文件
- 确保 `SELECTION_DATE` 是交易日（否则没有行情数据）
- 查看日志文件，定位具体错误

### 7.2 回测结果异常（收益率过高或过低）？

**可能原因**：
1. **未来函数问题** → 确保使用修复后的策略（已修复所有策略的未来函数）
2. 数据不准确（复权价格错误） → 检查 `data_loader.py` 中的前复权计算逻辑
3. 交易成本未考虑 → 当前版本未考虑交易成本，实际收益会更低

**解决方法**：
- 使用 `macd_kdj_strategy_fixed.py`（修复版）而不是 `macd_kdj_strategy.py`（原始版）
- 检查回测报告中是否有异常交易记录（如：同一天买入又卖出）

### 7.3 如何添加新因子？

**步骤**：
1. 在 `src/factor_calculator.py` 中添加因子计算函数
2. 在 `src/factor_processor.py` 中添加因子方向定义（正向/反向）
3. 修改 `config.py`，启用新因子
4. 运行 `run_selection.py` 测试

### 7.4 如何添加新策略？

**步骤**：
1. 在 `backtest/` 目录下创建新策略文件（参考 `dual_ma_strategy.py`）
2. 实现 `run(df)` 方法，返回 `{'daily_values': [...], 'trades': [...]}`
3. 在 `run_backtest.py` 中导入新策略
4. 修改 `config.py`，将新策略添加到 `STRATEGIES_TO_TEST`

**注意事项**：
- ⚠️ **必须避免未来函数**！信号必须基于前一日数据，成交用当日开盘价
- 使用 `shift(1)` 获取前一日数据
- 在循环中用 `row['adj_open']` 获取当日开盘价

### 7.5 为什么有些策略表现很差？

**可能原因**：
1. 策略不适合 A股市场 → 尝试其他策略
2. 参数不合适 → 调优参数
3. 选股结果不好（选出的股票本身表现差） → 改进因子模型，或使用 ML 选股器

**建议**：
- 先测试 `"买入持有"` 策略（基准）
- 如果主动策略跑不赢基准，说明策略无效
- 尝试 ML 选股器（`ml_stock_selector_final.py`）

---

## 8. 高级用法

### 8.1 使用 ML 选股器

ML 选股器使用随机森林或 XGBoost 模型，基于多因子预测股票未来收益率，生成 TOP N 股票。

**使用方法**：

1. 准备训练数据（需要历史因子数据和未来收益率标签）
2. 运行 ML 选股器：
   ```bash
   python ml_stock_selector_final.py
   ```
3. 将 ML 选股结果保存到 `data/results/ml_selection_results.csv`
4. 修改 `run_backtest.py`，读取 ML 选股结果
5. 运行回测

### 8.2 使用 QuantStats 生成可视化报告

QuantStats 是一个专业的量化策略绩效分析库，可以生成交互式 HTML 报告。

**使用方法**：

1. 确保已安装 QuantStats：
   ```bash
   pip install quantstats
   ```
2. 在 `run_backtest.py` 中调用 `backtest/quantstats_reporter.py`
3. 生成报告：
   ```python
   from backtest.quantstats_reporter import generate_quantstats_report
   generate_quantstats_report(returns, benchmark_returns, output_path)
   ```

**输出**：
- 交互式 HTML 报告（包含收益曲线、回撤曲线、月度收益热力图等）

### 8.3 批量回测多组参数

如果需要测试多组参数组合，可以编写批量回测脚本：

```python
# batch_backtest.py
import itertools

# 参数网格
param_grid = {
    'rsi_period': [14, 21, 28],
    'take_profit': [0.3, 0.4, 0.5],
    'stop_loss': [0.1, 0.15, 0.2]
}

# 遍历所有组合
for params in itertools.product(*param_grid.values()):
    # 修改策略参数
    # 运行回测
    # 记录结果
    pass
```

---

## 9. 附录

### 9.1 依赖库安装

```bash
# 核心依赖
pip install pandas numpy akshare tushare protobuf

# 技术分析（TA-Lib）
# Windows: 先下载 TA_Lib-0.4.24-cp39-cp39-win_amd64.whl
pip install TA_Lib-0.4.24-cp39-cp39-win_amd64.whl

# 日志
pip install loguru

# 回测分析
pip install quantstats

# 机器学习（可选）
pip install scikit-learn xgboost
```

### 9.2 数据库表结构

本地 SQLite 数据库 (`astock_daily.db`) 包含以下表：

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| daily | 日线行情 | ts_code, trade_date, open, high, low, close, vol, amount |
| daily_basic | 每日指标 | ts_code, trade_date, pe, pb, ps, total_mv |
| fina_indicator | 财务指标 | ts_code, end_date, roe, grossprofit_margin, netprofit_yoy |
| index_daily | 指数日线 | ts_code, trade_date, close |
| industry_momentum | 行业动量因子 | trade_date, industry, momentum |

### 9.3 联系方式

如有问题或建议，请联系：huyijiang

---

**文档版本**：v1.0  
**最后更新**：2026-05-22  
**适用版本**：multi_factor_selection v2.0+
