# ETF轮动 V6（BV1uPu86sENG）未来函数 / 幸存者偏差 / 可复现性 专项审计

> 审计时间：2026-08-11
> 审计对象：`run_etf_rotation_v6.py`（基线引擎）+ `run_etf_rotation_v6_phase3.py` / `sens_clean.py`（三件套）
> 触发：用户要求"再查一遍"——确认没有过拟合、结果可复现、有无未来函数/幸存者偏差/用未来池子选现在的产品。

## 结论先行（一句话）

**代码层无未来函数；所用 24 只 ETF 全部在 2018-01-01 前上市（无"未来产品"）；结果确定性可复现；但"4 只赢家池"本身是后视镜幸存者偏差（从 354 只里挑的），这是视频数字虚高的根，已用"含 4 赢家 + 16 同期 ETF"的对照坐实。我的 +407% 忠实复现了这份偏差，但它仍远不及视频 1886%——过拟合结论不变。**

---

## 一、未来函数（look-ahead）代码审计

逐行核对 `run_etf_rotation_v6.py`，信号时点 vs 执行时点：

| 环节 | 代码位置 | 信号用到的最晚数据 | 执行时点 | 判定 |
|---|---|---|---|---|
| RSRS | L93 `close_adj.rolling(25)` | 决策日(周五)收盘 | 下周一开盘 | **无前视** |
| 动量 | L94 `close_adj.rolling(21)` → `x[-1]/x[0]-1` | 周五收盘 | 下周一开盘 | **无前视** |
| 横截z | L105-107 当日 4 只均值/标准差 | 周五 | 下周一开盘 | **无前视** |
| 前二闸门 | L139 `rc==1 and rr<=2 and rm<=2` | 周五收盘/排名 | 下周一开盘 | **无前视** |
| 周五决策 | L205-213 `compute_target(F,d)`→`pending[next_monday_open]` | 周五收盘 | 次周一开盘 | **无前视** |
| 止损触发 | L197-204 `low<=pre*(1+STOP)` | 当日实测最低价 | **次日开盘**卖出 | **无前视** |
| NAV记账 | L191-195 持仓用当日 `close_df` | 当日收盘 | 当日收盘标记 | **无前视** |
| 冷却恢复 | L214-227 `close>MA` 清 cooldown | 当日收盘 | 仅更新状态供将来决策 | **无前视** |

关键设计：决策与执行之间隔一个周末（无交易），周五收盘是公开信息后才在周一开盘动作；止损用当日最低价判定、卖在次日开盘，是标准保守执行，未用到未来价。

**复权口径**：`adj_factor` 用全序列最大值归一（后复权标准做法，不改相对收益），且 `START=2018` 在 adj_factor 起始(2015-01-05)之后，前视边界不影响本窗口。纳指/黄金无 adj_factor 用 raw，与视频一致且保守。

**一处微小建模瑕疵（非未来函数）**：`cal` 取 4 只并集日历后 `ffill`，纳指 ETF 在 A 股节假日虽底层美股交易但 ETF 停牌，会偶发 1 日价格滞留——属真实市场日历差异，不影响结论方向。

**判定：引擎无未来函数。**

---

## 二、"用未来池子选现在的产品"审计（选池时点）

实查 `etf_daily` 上市日期（非记忆）：

| 池 | 代码数 | 首交易日≤2018-01-01 | 含未来产品 |
|---|---|---|---|
| 4 只核心 | 4 | 全部（510880=2010/159949=**2016-07-22**/513100=2013/518880=2013） | **无** |
| 20 只扩展 | 20 | 全部（最晚 512000=2016-09-14） | **无** |

数据库全景：首交易日≤2018-01-01 的 ETF = **354 只**（= 视频"从 354 只里选 4 只"的源头）；首交易日>2018-01-01 = **1657 只**（确凿未来产品）。我的复现 24 只(4核心+20扩展)全部取自 354 只，**1657 只未来产品一个没碰**。

**判定：无"未来产品"泄漏。** 但注意——"选哪 4 只"这件事本身是后视镜（见第三节），这是幸存者/前视**选池**偏差，不是代码级未来函数。

---

## 三、幸存者偏差审计

**机制**：视频从 354 只(2018前上市)中挑了 4 只已知赢家。我的 4-ETF 复现忠实地继承此偏差。

**对照设计（关键）**：20 只扩展池 **包含那 4 只赢家**（510880/159949/513100/518880 全在）+ 16 只同期合理 ETF，全部为存活至 2026 的幸存者。

| 池 | 年化(0.04%) | 空仓率 | 说明 |
|---|---|---|---|
| 4 只赢家池 | ~21–23% | 41% | 忠实视频偏差 |
| 20 只(含4赢家+16同期) | **7.7–7.8%** | 60% | 赢家魔法被 16 只稀释 |

→ 即便**保留 4 赢家**，只要加入 16 只同期 ETF，收益就从 21% 暴跌到 7.7%。证明 4 只**集中度**才是收益来源，选池是后视镜。

**残余偏差（诚实边界）**：20 只扩展池取的是"存活到 2026"的 ETF，排除了期间退市/合并者 → 7.7% 是**幸存者偏差下界的上界**（若纳入已退市 dud，结果只会更低）。策略的"前二闸门"(须 combo 排第1 且两单项≤2) 是为 4 名池校准的，直接套 354 只会极少触发→长期空仓，故未跑全 354；但方向结论(樱桃选池驱动差距)已由"含赢家仍崩"充分证明。

**判定：选池幸存者/前视偏差确凿，已被对照量化；非代码 bug，是视频方法论缺陷。**

---

## 四、可复现性（复跑对照）

代码无随机源（无 RNG/无 seed 依赖/字典与日历均确定性排序），理论可复现。实操复跑两遍，数字逐位一致：

| 口径 | 累计 | 年化 | 最大回撤 | 夏普 | 空仓率 |
|---|---|---|---|---|---|
| 基线 0.04%（复跑1） | +407.1% | +21.1% | -24.1% | 1.18 | 41.2% |
| 基线 0.15%（复跑1） | +195.97% | +13.63% | -30.5% | 0.82 | 41.2% |
| RSRS窗20（干净单变量） | +580.1% | +25.3% | -20.8% | 1.35 | 37.9% |
| 权重0.5/0.5 | +520.0% | +24.0% | -22.3% | 1.18 | 41.3% |
| 堆叠 W20+0.5/0.5+金MA20 | +663.1% | +27.0% | -20.8% | 1.27 | 31.0% |

两次复跑 +195.97% / +407.1% / +663.1% 完全一致。**判定：结果可复现。**

> 注：平台层"回测非确定性 bug"（memory 记载）存在于其它策略的并发/随机路径；V6 引擎为纯确定性 pandas 流水线，不受其影响。

---

## 五、最终判定

1. **未来函数**：无（代码审计逐环节通过）。
2. **未来产品**：无（24 只全≤2018-01-01，1657 只未来产品未用）。
3. **幸存者偏差**：有，且在选池层——视频从 354 只挑 4 赢家，我的复现忠实继承；"含 4 赢家+16 同期"对照把年化从 21% 打到 7.7%，坐实集中度来源。
4. **可复现性**：成立（复跑逐位一致，确定性引擎）。
5. **过拟合结论**：**不变且更稳**。视频 1886%/每年正/空仓16.6% 在诚实口径下不可复现；即便堆叠最优参数(年化27%)仍差约 3×。框架本身（年化13–27%、夏普~1、空仓~40%=熊市门控）是真实可交易的 ETF 轮动骨架，建议保留作平台轮动/风控参考，数字按 Template C 丢弃。

### 诚实边界清单
- [x] 引擎无未来函数
- [x] 无未来产品（选池时点合规）
- [x] 结果可复现（确定性 + 复跑验证）
- [x] 选池幸存者偏差已量化（4→20 对照，7.7% 为偏差下界上界）
- [ ] 未跑全 354 只（闸门为 4 名池校准，直接套会极少触发）；方向已由"含赢家仍崩"证明，非致命缺口
- [ ] 扩展池取存活者，未含退市 ETF（7.7% 为保守上界）
