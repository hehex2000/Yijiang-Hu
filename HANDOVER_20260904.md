# 交接文档 · 2026-09-04（周五）→ 周一续

> 作者：WorkBuddy　日期：2026-09-04　状态：P0 已提交，P1 待开
> 仓库：`multi_factor_selection`（master，本地领先 origin 4 commits，未 push）
> 关联计划：`docs/position_sizing_experiment_plan.md`

---

## 0. 一句话结论

仓位管理实验 **P0 已跑通并提交**（`6be0691`）。结论：**视频处方（66.7% 重仓）被证伪，平台 20% 默认稳健**；真实风险轴是尾部回撤而非破产。下一步开 **P1**（组合级 sizing 引擎 + 6 档 A/B + Sharpe/暴露度归一化）。

---

## 1. 本轮已完成（已提交 `6be0691`，未 push）

| 文件 | 产物 |
|---|---|
| `run_sizing_bootstrap.py` | P0 block-bootstrap 蒙特卡洛（5000 路径 × 8 档） |
| `docs/position_sizing_experiment_plan.md` | 计划 + B站视频批判 + §3.1.1 固化 P0 结果 |

> 注：`.workbuddy/skills/bilibili-critical-summary/SKILL.md` 的「🌗 观察名单·数学严谨/事实注水」新 tier 是**用户级** skill（在 `~/.workbuddy/`，不在本仓库），无需随本仓提交。

---

## 2. P0 核心数字（周一直接看，不用重跑）

| label | f | cap | 实现笔数 | 终值中位(x) | 破产50% | MDD_P95 | 最差权益P5 | CAGR_P50 |
|---|--:|--:|--:|--:|--:|--:|--:|--:|
| FULL | 1.000 | 1 | 229 | 7404.2 | 0.00% | −29.8% | 71.5% | 42.8% |
| **RISK_2PCT_NOM**（视频处方） | 0.667 | 1 | 229 | 532.6 | 0.00% | −19.9% | 81.2% | 28.5% |
| KELLY_FULL | 0.393 | 2 | 454 | 1765.5 | 0.00% | −13.7% | 89.2% | 34.9% |
| RISK_2PCT_MEAN | 0.300 | 3 | 673 | 4582.4 | 0.00% | −12.1% | 91.2% | 40.1% |
| **FIXED20**（平台默认） | 0.200 | 5 | 1077 | 5997.0 | 0.00% | −10.3% | 92.9% | 41.6% |
| RISK_2PCT_REAL | 0.107 | 9 | 1608 | 1065.8 | 0.00% | −6.4% | 96.4% | 32.2% |
| RISK_2PCT_P99 | 0.078 | 12 | 1756 | 271.6 | 0.00% | −4.7% | 97.4% | 25.1% |
| QUARTER_KELLY | 0.050 | 20 | 1775 | 38.9 | 0.00% | −3.1% | 98.3% | 15.8% |

**4 条已定结论：**
1. **破产概率 0%（全 8 档）**——视频「满仓连亏两次归零、概率 16%」是稻草人（建立在「每次 all-in 全亏光」的荒谬假设上；本策略最差单笔 −29.84%，永不归零）。
2. **真实风险轴 = 尾部回撤，随 f 单调**：MDD_P95 从 −3.1%（f=0.05）到 −29.8%（f=1.0）；最差权益 P5 从 98.3% 到 71.5%。风险预算/低仓位的唯一真实价值是**压尾部回撤**，不是防破产。
3. **视频处方 RISK_2PCT_NOM（66.7%）被平台 FIXED20（20%）全面压制**：终值中位 532x vs 5997x（差 11 倍）、MDD_P95 −19.9% vs −10.3%、最差权益 P5 81.2% vs 92.9%——增长与风控双维度都劣于现状。
4. **iid 与 block 尾部几乎无差**（MDD 差 1~2pp，最差权益差 3~7pp，且 iid 反而略差）→ P1 可放心用 iid 提速，block 仅更稳。

---

## 3. 方法学坑（周一必读，避免重踩）

- **⚠️ 终值不能跨 f 直接排序**：上表终值非单调（5997@f0.20 > 4582@f0.30 > 1765@f0.393 < 532@f0.667 < 7404@f1.0）。根因是**并发上限 `floor(1/f)` 把「成交笔数」与 f 耦合**：f 越小→并发越多→实际成交笔数越多（1775→1077→454→229），笔数本身驱动终值。所以「哪个 f 终值最高」被容量机制主导，不是凯利分数主导。
  → **P1 必须用真实日历引擎算 Sharpe / 年化收益，并强制报「成交笔数」+「平均在市资金占比（暴露度）」**。沿用布林带教训：收益高但暴露度也高，须看风险调整后。
- **回撤 bug 已修**：首版用「全局最低÷全局最高」误算 −99%；已改为逐时点 running-peak 相对回撤最小值（标准最大回撤）。P1 直接复用 `simulate_correct` 的回撤算法。
- **载体必须正期望**：在负期望策略上比仓位，答案永远是「越少越好」，零区分力。载体锁 `mean_reversion` 基线（§11 已验证单笔净期望 +4.189%、胜率 62.66%、盈亏因子 2.69、窗口 skill +3.67pp）。

---

## 4. 周一 P1 执行清单（按序）

1. **新建 `run_position_sizing_ablation.py`（组合级共享资金池）**——复用现有 `load_stock_prices` + as-of 快照管线（沪深300 as-of 20190101 前 40 只，2019-01-01~2026-07-31，初始 100 万共享池）。**不能沿用「每只独立 10 万」**（那套口径下比仓位是假数字，见计划 §2 D1）。
2. **`base_strategy.py` 加 opt-in `risk_budget` 分支（默认 `False`，零行为变更）**。
3. **6 档 A/B（只改仓位算法，信号/出场/成本/池子全共用）**：
   FULL(100%) / FIXED20(20%) / KELLY_FULL(~39%) / KELLY_HALF(~20%) / RISK_2PCT_NOM(66.7%) / RISK_2PCT_REAL(~11~30%)。
4. **凯利 p/b 滚动估计**：只用 t 之前已平仓交易；样本 <20 笔回退保守先验 p=0.5/b=1.5（全样本 p/b 是前视，禁用）。
5. **强制报告**：Sharpe + 年化 + 平均在市资金占比（暴露度）+ 成交笔数 + 部署率自查（≈100%）。
6. **敏感性子扫描**：风险预算 ∈ {0.5%, 1%, 2%, 3%} × 距离口径 ∈ {名义 3%, 滚动均亏, 滚动 P95}。
7. **读 `sizing_bootstrap.csv` 验证 P1 终值与 P0 同口径一致**（脚本已落盘 `data/results/position_sizing/`，gitignored）。

---

## 5. 验收红线（复用，防事后找补）

- 任一档「收益更高但暴露度也更高」→ 必须同时报**暴露度归一化后**的数字，否则不算胜出。
- 不追 argmax；敏感性出现孤峰（只有一组好、邻域塌）→ 判为挑参，用默认值不优化。
- **不动 `config.py` 的 `mean_reversion` 默认值**，除非 walk-forward 5/5 且 Sharpe 同步改善（参照「凡收益降/回撤小一律先假定是减震器，须 Sharpe 在 ≥2 个不重叠窗口证伪」）。
- 每档必过部署率自查（≈100%），未过的结果不进结论表。

---

## 6. 其他未提交改动（**不属于本轮**，周一另批，勿混进 sizing 提交）

> unified cleanup：本轮只提交了 sizing 的 2 个文件。以下 13 个文件是 divlow_b9 / 负成本 / 年度落地 等其他流，保持 uncommitted，周一单独处理。

**modified（4）：**
- `HANDOVER_20260902.md`
- `divlow_b5_rebal_demystify.md`
- `divlow_b7_demystify.md`
- `divlow_b9_pool_lab.py`

**untracked（9）：**
- `_pre_annual_landing_20260904/`
- `decompose_trail_ab.py`
- `divlow_b9_demystify.md`
- `divlow_b9_nav_cmp.py`
- `divlow_b9_turnover_audit.py`
- `divlow_turnover_matrix.py`
- `docs/negative_cost_trail_ab_divlow_report.md`
- `docs/negative_cost_trail_ab_report.md`
- `iso_annual_landing.sh`

---

## 7. 关键文件索引

| 用途 | 路径 |
|---|---|
| 实验计划 + 视频批判 + P0 结果 | `docs/position_sizing_experiment_plan.md` |
| P0 脚本 | `run_sizing_bootstrap.py` |
| P0 结果 csv（gitignored） | `data/results/position_sizing/sizing_bootstrap.csv` |
| 基线单笔期望（载体依据） | `docs/bollinger_enhancement_report.md` §11 |
| 视频批判 skill 档（用户级） | `~/.workbuddy/skills/bilibili-critical-summary/SKILL.md`（🌗 观察名单 tier） |
| P1 待建脚本 | `run_position_sizing_ablation.py`（新建） |
| P1 待改 | `backtest/base_strategy.py`（`risk_budget` opt-in 分支） |

---

## 8. 周一第一句话建议

> 「开 P1：先建组合级 shared-pool sizing 引擎（复用 as-of 快照），跑 6 档 A/B，强制报 Sharpe + 暴露度，对照 P0 的 `sizing_bootstrap.csv` 校验同口径终值。」
