> ❌ **INVALIDATED · 无效策略归档**：本文件对应量化刘不牛「173倍」ETF 轮动 (BV1BqMX6TEx2)，
> 经严谨回测 + 月度调仓 bug 修复后已彻底证伪（关止损 -64.1%、池子 0.0%ile condemned）。
> 保留作证据归档，不作为任何实盘/复现参考。详见 `etf_rebalance_bugfix_reverify.md`。

> ⚠️ **本报告数字已失效**：基于修复前的错误月度调仓日程（每年仅 2 个决策日）。修正后：原10只由 **77.7% 分位（优于78%随机）** 反转为 **0.0% 分位（差于100%随机）**。详见 `etf_rebalance_bugfix_reverify.md` 第3节。
>
# 刘不牛 ETF 轮动 · 阶段2：扩展池蒙特卡洛（选标偏差检验）

> 问题来源：阶段0 证明「关 5% 止损 + R² 关」配置在原 10 只上累计 **+120.7%**（年化 12.0%，夏普 0.58）。
> 这 +120.7% 究竟是**策略规则本身有效**，还是**恰好选了这 10 只**的运气？
> 本阶段用蒙特卡洛回答（呼应 UP 视频「Bug 级纪律：选池必须事前固定，不可后视挑标的」）。

---

## 1. 方法

- **候选全集**：内置跨 A股 / 海外股票 / 商品 / 债券 / 货币 / 黄金 的代码清单（带 A/G 分类，见脚本 CANDIDATE）。
- **可用性校验**：运行时查库，要求 `MIN(trade_date) <= 2019-01-01`（与原池同一起点，可比）。通过校验得 **28 只**（A股 13 / 全球 15）。
- **蒙特卡洛**：每次从 28 只里**随机抽 10 只**，跑与基准完全相同的配置（关止损 + R² 关 + A股走弱切池开 + 90% 保留 + 动量 25 日），重复 **K=300** 次。
- **关键分位**：看原 10 只的 +120.7% 落在随机池收益分布的哪个分位。
  - 落在 90%+ 分位 → 是选标运气 / 后视镜挑池；
  - 接近中位 → 收益来自规则本身，普适。
- **性能处理**（踩坑记录）：
  - 原脚本逐轮重连 14 次 DB + 逐日用 `.loc` 标签查价，300 轮 ≈ 30 分钟且被沙箱限时杀进程（exit 127）。
  - 改为**跨轮数据缓存**（每只 ETF/指数只从 DB 读一次）+ **numpy 化 `fast_backtest`**（逻辑与源 `backtest` 一致，逐日 `.loc` 换成数组下标，提速 ~100×）。
  - 校验：`fast_backtest` 对原 10 只复现 **120.7%**、对含陌生 ETF 子集复现 **23.14%**，与源函数逐笔一致 → 语义未变。
  - 另修正 `equal_weight` 区间 bug：原实现用全历史（2010 起）算等权，与策略（2019-07 起）不同区间；已改为同区间 `[START:END]`。

## 2. 基准复现

| 配置 | 累计 | 年化 | 夏普 |
|---|---|---|---|
| 原 10 只 · 策略（关止损 R² 关） | **+120.7%** | 12.0% | 0.58 |
| 原 10 只 · 等权月度 | +112.9% | — | — |

> 注：原 10 只上「策略 ≈ 等权」（120.7% vs 112.9%，差约 +8 点），说明**对该池而言轮动规则增益有限**。

## 3. 蒙特卡洛分布（K=300，随机抽 10 只，关止损 R² 关）

| 指标 | 随机池·策略 | 随机池·等权 |
|---|---|---|
| 均值 | **72.2%** | 79.3% |
| 中位 | 53.7% | 77.9% |
| P10 | 3.0% | — |
| P90 | 170.4% | — |
| 最小 / 最大 | -31.2% / **377.8%**（≈3.8 倍） | — |
| 正收益占比 | 92.0% | — |
| 超自身等权占比 | **36.0%** | — |

## 4. 关键结论：原 10 只 +120.7% 的分位

- 原 10 只 **+120.7%** 落在随机池策略分布的 **77.7% 分位**
  → 跑赢约 **78%** 的随机 10 池；约 **22%** 的随机池比它更好。
- 原 10 只 vs 随机池均值：**+48.5%**；vs 随机池中位：**+66.9%**。
- **不在**前 10% 尾部（P90=170.4% > 120.7%）；最佳随机池达 377.8%（3.8 倍）。

**直接回答用户问题**：+120.7% 是「部分运气，但非极端巧合」。
- 它明显好于随机中位（53.7%）和均值（72.2%），处于 78 分位——比多数随机选池好；
- 但不是凤毛麟角的尾部事件（最好随机池 3.8 倍、22% 随机池更优），所以**不能简单归为「恰好选中这 10 只」的纯运气**，原池（UP 事前固定挑选）确实优于随机。

## 5. 对照：收益到底来自哪

| 分组 | n | 策略均值 |
|---|---|---|
| 含全球资产池（必有海外/商品） | 300 | 72.2% |
| 含避险（货基/国债）池 | 279 | **68.1%** |
| 不含避险池 | 21 | **127.0%** |

- **轮动规则本身增益有限**：随机池策略均值 72.2% < 等权均值 79.3%；仅 **36%** 的池轮动能跑赢自身等权。即「单只动量轮动」平均跑输「一篮子等权持有」。
- **避险资产是拖累**：2019–2026 货基/国债收益低，含它们的池均值 68.1%，不含的池均值 127.0%。原 10 只含国债+货币却仍达 120.7%，靠的是其余 8 只高风险资产（纳指/标普/黄金/原油等）踩中 2019–2026 全球/商品/债券顺风。
- **「173 倍」彻底证伪**：连最佳随机池也只有 3.8 倍，原池 2.2 倍；所谓 173 倍是后视镜选池 + 样本内过拟合的产物（与阶段0 faithful 复现 -19.3% 互相印证）。

## 6. 与阶段 0/1 的一致性

- 阶段0：faithful 复现 -19.3%；关止损 R² 关 +120.7%（接近等权）。
- 阶段1：关止损 R² 关在 2024 后仍 +53.3% 稳健；**叠加 R² 过滤**才在 2024 后塌陷到 +3%（regime 失效）。
- 阶段2：+120.7% 非极端选标运气（78 分位），但轮动规则本身不创造超额（平均跑输等权、仅 36% 胜出）。
- **总判断**：该策略的真实 alpha 来自「事前固定地持有一篮子跨类多资产 + 覆盖 2019–2026 顺风资产」，而非月度动量轮动规则。这与 UP 视频「选池事前固定」纪律自洽——原池是事前挑的、且确实优于随机，但「173 倍神话」不可复现、轮动技巧近乎中性。

---

## 附：可复现文件

- `analyze_liubuni_stage2_mc.py` —— 蒙特卡洛脚本（含跨轮缓存 + numpy 化 backtest + 同区间等权基准）
- `etf_liubuni_stage2_mc.csv` —— 300 次随机池逐笔记录（trial / total / ann / sharpe / mdd / n_A / n_G / has_cashbond / eq_total）
- 运行：`venv_ml\Scripts\python.exe analyze_liubuni_stage2_mc.py`
