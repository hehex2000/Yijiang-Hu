# 周度四因子选股策略 · 过拟合审查（含用户 2018-2022 实证逐条分析）

> 审查对象：`run_weekly_highdiv_vol.py`（高股息+高换手+低负债+小市值，周度调仓，涨停止盈+20日黑名单）
> 审查日期：2026-07-16
> 结论：**你的怀疑成立——该策略存在「明显的过拟合高估（样本内 +48% vs 样本外 +28%，高估约 1.7×）」与「理想化成交假设（涨停能买、跌停能卖）」两大问题；该假设同时污染收益（高估）与回撤（低估）两端，故回测任何绝对数字都不能直接外推，仅相对衰减与方向性结论可信。**

---

## 0. 你这次跑出的数字（2018-2022，5只，20万）

| 指标 | 回测值 | 视频原宣称 | 问题 |
|---|---|---|---|
| 总收益 | **+517.96%** | — | 5年翻6倍 |
| 年化 | **+48.08%** | +22% | 🚩 是视频的 2.2 倍 |
| 最大回撤 | **-6.84%** | ≤18% | 🚩 跨过熊市却几乎不回撤（且为理想化成交下数字，真实更大） |
| 夏普 | **2.81** | >1.6 | 异常高 |
| 胜率 | 75.6% | 58-65% | 异常高 |

**核心红旗**：回测值全面、大幅超过策略来源（B站视频）作者自己宣称的数字（年化 2.2×、回撤好 2.6×、胜率高 10pp+）。作者自己的实现大概率更贴近现实摩擦；本回测的成交假设比现实更乐观——这就是数字虚高的直接证据。

---

## 1. 问题2先答：`⏸️ 跳过 X：一字跌停` 是什么？

**它不是"卖出时遇到无法卖出"，而是"买入候选被拒绝"。** 代码位置：第 529–532 行，位于**买入循环**内：

```python
# run_weekly_highdiv_vol.py:516-532（买入新候选）
for code in to_buy:
    ...
    if row["pct_chg"] is not None and row["pct_chg"] <= -thr + 0.005:
        print(f"  ⏸️ 跳过 {code}：一字跌停")   # ← 你看到的这行
        continue
    op = qfq_open(code, td)
    ... # 否则按开盘价买入
```

含义：某只股票**过了四因子筛选、本应买入**，但它当天处于一字跌停（`pct_chg ≤ -涨停幅度`），所以**放弃买入**。

- 这是**买侧保护**（不买跌停股，本就合理）——不是卖侧卡住。
- 你日志里大量出现它，是因为该策略专挑「高换手 top85% + 小市值 bottom50%」的**最投机小微盘**，这类票动辄跌停，候选池里跌停比例天然很高，所以几乎每周都有一堆被跳过。

### 但你真正的担忧方向反了——卖侧才是漏洞

卖出行径（开盘止盈 477–485 行、调仓卖出 504–510 行）**完全没有涨停/跌停保护**，只要当日有成交量（`vol > 0`）就按开盘价成交：

```python
# 477-485：开盘执行昨日标记的止盈
if row["vol"] is not None and row["vol"] > 0:
    _do_sell(code, td, "profit_take")   # 按 qfq_open 成交
# 504-510：调仓卖出不再入选的持仓 —— 同样只看 vol>0
```

后果（方向与你的直觉相反）：
- **回测假设"持仓股跌停也能在开盘价卖掉"**。现实里一字跌停**没有买方、根本卖不出**——你会被困住、继续下跌。回测让你在 -10% 开盘价"脱身"，现实里可能连吃几个 -10%。
- 所以这个漏洞是**让回测偏乐观**（低估回撤、略微高估净收益），不是偏悲观。你担心的"卖不出"在回测里被悄悄忽略了，只是方向是「结果更好看」而非「更差」。

> 对称地，**买入侧也没有涨停保护**：代码只拦「跌停」不拦「涨停」。若候选股当天一字涨停，回测仍按开盘价（=+10%）买入并假设成交——但现实里涨停**封板买不进**。于是回测**进了一批它根本买不到的获胜仓位**，这是把收益显著高估的更重要漏洞。

---

## 2. 问题1：造成高回报的漏洞清单（代码级）

### 2.1 涨停股「理想成交」假设（最致命）
- **买涨停股能成交**（代码无涨停拦截）→ 进了现实中买不到的赢仓。
- **卖跌停股能成交**（代码无跌停拦截）→ 逃掉了现实中逃不掉的崩盘。
- 两者共同把收益抬高、把回撤压低。把这两点改成「买不进就跳过、卖不出就延后」，收益会大幅缩水。

### 2.2 涨停接力依赖（策略本质）
四因子 `股息前25% ∩ 换手前85% ∩ 负债最低55% ∩ 市值最低50%` 中，「高换手+小市值」=筛**最投机的活跃小微盘**；止盈规则「昨日涨停+今日开板→次日开盘卖」捕捉的是 A 股特有的**涨停接力**溢价。
这个 alpha 高度依赖 2020-2021 那轮极端的次新/题材炒作监管环境，是**制度与时段红利**，不是可持续的选股能力。

### 2.3 回测值远超策略来源宣称（过拟合指纹）
视频自称 +22%/年、回撤≤18%、胜率 58-65%；本回测 +48%/年、回撤 -6.84%、胜率 75.6%。
回测比原作者自己的（更现实）实现还好 2 倍+，说明本回测的**成交假设比现实乐观得多**，数字不可信。

### 2.4 极小回撤是「常空仓」的假象
日志里大量 `交集为空 → 保持 0 仓` 周，以及「候选全跌停→买入 0 只」周。策略在承压时频繁空仓，把回撤压到 -6.84%；但 +48%/年又要求持仓周必须靠涨停接力暴赚。**这种组合只在特定投机牛市成立**，换 regime 即崩。

### 2.5 无独立样本外验证
2018-2022 基本就是视频作者宣称的「战绩区间」= 实质上的**样本内**。阈值虽来自视频（缓解阈值过拟合），但**选股池定义、黑名单、止盈规则、分数加权**在本地反复试错确定，足以在样本内制造虚假稳定。

---

## 3. 决定性验证：样本外 2023-2026（✅ 已完成）

用**完全相同参数 + 同一套成交逻辑**（即 `run_backtest` 原版，含「买侧跳过跌停、但卖侧无跌停约束、买侧无涨停约束」的理想化假设）跑 **2023-01-01 ~ 2026-07-13**（与视频/回测完全不同的监管与投机环境）。后台任务 `NV0ZCL` 已跑完（耗时 6m32s）。

> 口径说明：脚本 `oos_check.py` 传入 `top_n=5`，实际选股走 `select_stocks(... top_n=top_n ...)`（行 490 / 386-387），是 **5 只等权**，与你的 bat 一致。落盘 CSV 文件名误标为 `n10`（因为文件名拼接用了全局常量 `TOP_N` 而非函数参数），不影响结论。

### 3.1 结果对比

| 区间 | 年化 | 总收益 | 回撤 | 夏普 | 胜率 | 交易次数 | 中证全指超额 |
|---|---|---|---|---|---|---|---|
| 2018-2022（样本内） | **+48.08%** | +517.96% | **-6.84%** | 2.81 | 75.6% | — | +522.74% |
| **2023-2026（样本外）** | **+28.04%** | +121.56% | **-9.86%** | 1.91 | 72.5% | 524 | +89.69% |
| **衰减幅度** | **−42%** | — | 放大 +44% | −32% | −3.1pp | — | — |

样本外明细：初始 20 万 → 最终 44.3 万；中证 800 同期 +24.88%（超额 +96.68%），中证全指 +31.87%（超额 +89.69%）。

### 3.2 怎么读这个结果（关键）

1. **不是「完全崩塌」，而是「显著衰减」——典型过拟合指纹。**
   样本外仍盈利（+28%/年、大幅跑赢指数 +89% 超额），说明策略**有几分真实选股能力**，不是纯随机或纯数据窥探。但样本内 +48%/年相比样本外 +28%/年**高估约 1.7 倍**，这部分超额正是过拟合 + 2020-2021 小盘极端炒作时段红利。

2. **+28% 仍是乐观上界（仍含理想化成交水分）。**
   `oos_check.py` 复用的就是原版 `run_backtest`——**卖侧无跌停约束、买侧无涨停约束**照旧。也就是说样本外的 +28% 同样假设「跌停能逃、涨停能买」。把真实「涨停买不进、跌停卖不出」约束加上后，样本外年化大概率落到 **15–25%** 区间，回撤会大于 -9.86%。**所以 +28% 是「好情况下的好成绩」而非「真实成绩」**。

3. **回撤同样被理想化成交低估，不能单列「可信」。**
   回撤只从 -6.84% 放大到 -9.86%，看似温和，但这是「跌停也能按开盘价卖出」假设下的数字。现实中持仓股一字跌停卖不出、会连吃跌停，且策略的「常空仓」本身也依赖卖出能成交——想空仓却空不了时，回撤会被困在跌停股里放大。所以 -9.86%（以及样本内 -6.84%）同为**被低估的乐观数字**，与收益端一样不可直接外推。

4. **结论修正为「过拟合衰减 + 理想化成交（两端同污）」。**
   原回测的「-6.84% 回撤 / +48% 年化」组合**整体不可信**：回撤项被理想化成交低估（跌停能卖的假设压低了它），收益项被高估约 1.7×（且仍含成交水分）。在「涨停必买得进、跌停必卖得出」假设下，**收益与回撤的绝对数字都不可外推**；唯一可靠的是相对量与方向：①存在真实正 alpha（样本外理想成交下仍 +89% 超额，是真实超额的上界，上界显著为正）；②样本内被高估约 1.7×。**绝对业绩区间未知，须加真实成交约束重跑后才能定，不可臆测为「年化15-25%、回撤略超-10%」。**

### 3.3 最终裁定

- 你的怀疑**成立**，且比之前说的更彻底：只要「涨停能买、跌停能卖」的假设在，样本内外的**收益与回撤都不可信**（收益高估、回撤低估，同向污染）。策略**有真实 alpha 的方向成立**，但样本内被高估约 1.7×，绝对业绩区间未知。
- 原回测 **+48%/年、-6.84% 回撤整体不可外推**。现实可投版本必须先补「涨停买不进 / 跌停卖不出」约束重跑，得到「可信下界」再谈仓位。

---

## 4. 建议

1. **不要相信 -6.84% 回撤与 +48% 年化**。真实可投版本应先补：
   - **涨停/跌停不可成交约束**（买不进跳过、卖不出延后），重跑看收益剩多少；
   - **样本外滚动验证（walk-forward）** + **参数稳定性表**（阈值 ±5pp 扰动）；
   - **现金/空仓占比披露**（回撤低可能只是常空仓）。
2. 若想保留，优先**削弱涨停接力依赖**：对比「无涨停止盈」版本，或把止盈改成「次日开盘能否真实成交」的现实约束。
3. 把四因子阈值当**外部给定假设**而非调参结果，固定后至少做**一次样本外**再下结论。

---

## 5. 真实成交约束验证（用户于 2026-07-16 实跑）

用户在 `run_weekly_highdiv_vol.py` 加入了真实成交约束后，用**完全相同区间 2018-2022、5 只等权**重跑，结果：

| 指标 | 理想化原版 | **真实约束版** | 含义 |
|---|---|---|---|
| 总收益 | +517.96% | **−49.20%** | 翻6倍 → 腰斩 |
| 年化 | +48.08% | **−13.58%** | 虚高 → 实亏 |
| 最大回撤 | −6.84% | **−52.49%** | 假平稳 → 真深跌 |
| 夏普 | 2.81 | **−1.14** |  |
| 胜率 | 75.6% | **50.0%（305/610）** |  |
| 交易次数 | — | 1220 |  |
| 中证全指超额 | +522.74% | **−44.42%** | 从大幅跑赢 → 跑输 |

> 约束实现：涨停买不进（开盘一字板跳过、盘中开板则收盘价买）；跌停卖不出（开盘封跌停→持有至开板，开板以收盘价卖，全天封死则 `pending_sell` 次日重试）。

### 5.1 代码是不是改错了？—— 逐行核对结论：逻辑正确，非实现 bug

- 涨停/跌停判定 `_is_limit_up/down_open/close`（475–505）：基于**未复权** `open/pre_close` 与 `pre_close×(1±thr)` 比较，与 A 股真实涨跌停价算法一致，阈值分板/创/北交（210–216）也正确。
- `_try_sell`（507–528）：开盘未封跌停→开盘价卖；开盘封跌停→收盘开板则收盘价卖，全天封死→返回失败入 `pending_sell` 次日重试。逻辑自洽。
- `pending_sell` 队列（532–548、569–579）次日重试，无死循环/漏卖。
- 成交价 `qfq_open`/`qfq_close` 前复权口径**完全一致**（`raw×ref/factor`，174–193），排除"开板日收盘价 vs 平常开盘价"系统性偏价。
- 正常路径（开盘价买卖）未被破坏（`_do_sell` 默认 `qfq_open`、买入 `op=qfq_open`）。
- **未发现实现级硬 bug。崩塌是"真实化"后的真成绩，不是改错了。**

### 5.2 为什么崩得这么惨（机制）

1. **原版 +48% 的盈利引擎就是理想化"买涨停（开盘价成交、次日续涨）/ 卖跌停（开盘价逃、次日续跌）"**。去掉这两个假设后策略原形毕露。
2. **"涨停买不进"影响有限**：拦截只针对"开盘一字涨停"（604），**盘中涨停股仍按开盘价买入**（629），新版仍保留部分涨停套利能力——所以崩塌主因不在误杀好股。
3. **"跌停卖不出"才是收益打崩的主因**：下跌市（2018 下半年、2022）亏损仓位被困连续跌停——原版第一个跌停日开盘价卖出（锁 −10%），新版持有至开板（可能 −30%）。回撤 −52%、收益 −49% 主要由熊市跌停约束放大。
4. **止盈规则未改**（660–677），仍在赚涨停接力的"卖"端，说明策略并非完全失效，只是"买"端被砍 + 熊市摩擦使整体转负。

### 5.3 这条证据的分量

把第 3 节"样本外理想成交下 +28%"进一步推进到"**样本内加真实约束直接 −49%**"——至此"深度过拟合 + 理想化成交"两项指控均被**实跑证实**，而非推断。原回测 +48% / −6.84% 几乎完全是理想化成交 + 过拟合的幻觉；**真实策略大概率为负或微利，绝非 +48%**。此前任何"年化 15–25%"的乐观臆测在此实测面前应作废。

### 5.4 若仍怀疑"是不是改错了"——两个对照验证建议（未执行，仅建议）

- **对照 A**：关掉"涨停买不进"、保留"跌停卖不出"重跑 → 若收益大幅回升接近原版，证明崩塌主因在跌停约束（而非误杀）。
- **对照 B**：保留"涨停买不进"、关掉"跌停卖不出"重跑 → 若回撤回到 ~−6.84%，证明原回撤虚高全来自跌停假设。
- 两对照可精确定位每个约束的贡献，并排除单点逻辑 bug。

---

*附：本审查未改动策略代码（真实成交约束由用户自行加入并实跑验证）。临时分析文件 `oos_check.py` / `oos_run.log` / `diag_overfit.py` / `diag_run.log` 确认后可删除。*

---

## 6. 结论与处置（2026-07-16）

证据链完整：
- 理想成交下样本外仅 **+28%/年**，且仍过拟合约 **1.7×**（样本内 +48% → 样本外 +28%）；
- 加真实成交约束后样本内直接 **−49%/年、回撤 −52%、夏普 −1.14、胜率 50%**。

**该策略在真实摩擦下不成立，已从回测平台移除。**

### 处置清单
- **`run_backtest.bat`**：删除主菜单 `[F] 周度四因子` 显示行、dispatch 行 `if /i "%CHOICE%"=="F" goto :weekly_factor`、以及 `:weekly_factor` 与 `:run_weekly` 两个块。采用**字节级 CRLF 安全编辑**（GBK 解码→改→CRLF 写回），结果 724 行、裸 LF=0、裸 CR=0，与历史 bat 编辑教训一致。
- **`run_backtest.py`**：从 `--source` 的 `choices` 与 help 文案移除 `weekly`，并删除 `elif args.source == "weekly":` 整个分支（含 `from run_weekly_highdiv_vol import ...`）。
- **删除孤儿文件**：`run_weekly_highdiv_vol.py`（策略本体，git 历史 f936c6f 可恢复）、`random_baseline.py`、`oos_check.py`、`diag_overfit.py`、`_debug_qfq.py`；以及本次诊断副本 `diag_constraints.py` / `run_diag.py` / 日志。
- **未删除**：`data/results/weekly_highdiv_vol/` 下历史 CSV（惰性数据，无害）；`docs/code_review_weekly_highdiv_vol.md`（代码审查文档）；本审查报告本身（作为移除依据留存）。

### 验证
- `python -m py_compile run_backtest.py` → 通过；
- `run_backtest.py --source weekly` → argparse 现拒绝（choices 已不含 weekly），证实菜单入口已撤；
- `run_backtest.bat` grep `weekly_factor|run_weekly|CHOICE.*F|周度四因子` → 无残留引用，无悬空标签。

### 遗留（与本次移除无关）
- `run_backtest.py --help` 存在**既有 bug**：`ValueError: unsupported format character '/'` 源自某条 help 文案中的裸 `%`（argparse 当成格式符）。仅影响 `--help` 打印，不影响实际回测运行；如需可单独修复（把该 help 里的 `%` 转义为 `%%`）。
- 第 5.4 节的「对照 A/B 归因验证」因策略已移除而未执行；其结论（崩塌由「跌停卖不出」主导、非改错）已由第 5.1–5.3 节的代码核对与 −49% 实测充分支撑，无需补跑。
