# B9 判读：取消"每行业最多 2 只"反而**提高**换手 +30.2pp（实测推翻实验室预测）

> 状态：2026-09-04 实测完成 + 口径自证通过（md5 逐位一致）。
> 🔴 **本轮推翻了 B9 实验室的全部结论**（含昨日"−34.3pp 最大杠杆"）。
> ✅ **决策已落定（§八）**：采纳"年度调仓、低换手"方向，**放弃 45% 目标**。
> 🚨 **但具体数字暂不可发布**：§8.5.1 实测发现年度档**确定性失败**，
> 52.6% 无法被当前代码复现，已降级为临时值。方向成立，数字待重测。

---

## 零、一句话结论

**"每行业最多 2 只"不是换手来源，而是换手稳定器。**
实测取消它：换手 **103.8% → 134.0%（+30.2pp）**、组合 **100% 押银行**、年化 5.45%→4.33%（配对 t=−0.49，噪声）。
→ **B9 判死，不落地。**

**降换手只有一条路走得通：降频到年度（142.4% → 52.6%，−63%），且必须配 fwd_yield 键**
（🔴 换 vol 键在季频省 38.6pp、在年频反而亏 15.0pp → 两杠杆负交互，见 §7.6）。

---

## 一、🔴 B9 实验室结论作废：它复现不出引擎的选股

`divlow_b9_pool_lab.py` 用落盘候选池离线估算 9 种口径的换手，昨日据此得出
「取消行业上限 −34.3pp、是单一最大杠杆」。**这个结论建立在错误复现上。**

逐期对拍（27 期，同池同法，只换排序键）：

| 复现用的排序键 | 与引擎实际持仓的平均交集 | 27 期中完全一致 |
|---|---:|---:|
| `fwd_yield` 降序（实验室 case A「现状」） | **4.41 / 12** | **0** |
| `volatility` 升序（实验室 case B「B8 换键」） | 9.52 / 12 | 3 |

→ 实验室标为「现状」的 case A **根本不是现状**。以它为分母算出的所有"降幅"（含
"A 149.5% → C 115.2%，−34.3pp"）**全部作废**，§三横向定位表同理。

⚠️ 顺带：**实验室的绝对换手数字也不可信**（vol 键复现率也只有 9.52/12）。
候选池落盘的是"行业 cap 前的 48 只"，与引擎 `_cap_industry` 的真实输入存在偏差，
未定位前不得引用实验室任何数字，只能引用引擎实测。

---

## 二、引擎真实口径是「volatility 键」，`--final-key` 的默认值骗了人

追查"引擎到底按什么键选股"的过程（三个独立证据）：

**证据 1｜持仓序列**：20200108 选出的 12 只，`volatility` **严格升序**
（0.1418→0.1742），`fwd_yield` 完全无序。12 只严格升序的概率 ≈ 1/4.8e8，不是巧合。

**证据 2｜行业 cap 的执行键**（一锤定音）：池子里 11 只银行中——

| 取法 | 得到的 2 只 | 引擎实际选的 |
|---|---|---|
| `volatility` 最低 2 只 | 601988 中国银行、601288 农业银行 | ✅ **完全一致** |
| `fwd_yield` 最高 2 只 | 601328 交通银行、601988 中国银行 | ❌ 601328 未被选 |

→ 连"每行业留哪 2 只"都是按**波动率**定的。

**证据 3｜代码注释**：`run_dividend_low_vol_quality_bt.py:1164`
`sel_r = sel.reset_index(drop=True)   # rank 序 = select_stocks 输出序（score 降序）`
而 `score = −volatility`。

### 为什么 `--final-key` 显示默认 `fwd_yield` 却跑出 vol 序

基线 NAV/sel 产物是 **9-03 13:12** 跑的，当时 B8 正在开发中：
`--final-key` 的 default 一度是 `volatility`，且**文件名加 `_kv` 标签的逻辑还没写**
（该逻辑随 commit `1df8ff3` 才入库）。

→ **基线产物是 vol 键口径，但文件名没有 `_kv` 标记**。
这是"串档坑"的第 11 次变体：**不是文件名撞车，而是口径变了却没打标签，
导致今天默认参数跑出来的东西与历史产物不可比。**

> 🔑 教训：改 opt-in 参数的**默认值**时，必须同时给产物打上口径标签；
> 否则"默认参数"会静默变成另一种实验，且从文件名完全看不出来。

---

## 三、实测 A/B（引擎全管线，同窗口 20200101~20260723，27 期）

### 3.1 换手与集中度（`divlow_b9_turnover_audit.py`，从选股明细回算）

| 档位 | 每期单边 | **年化单边** | HHI↑ | 最大个股权重 | **银行权重占比** |
|---|---:|---:|---:|---:|---:|
| 基线（vol键 + 每行业≤2） | 25.9% | **103.8%** | 0.142 | 25.4% | 31.7% |
| B9（vol键 + 不限行业） | 33.5% | **134.0%（+30.2pp）** | 0.125 | 23.0% | **100.0%** |

### 3.2 收益与风险（`divlow_b9_nav_cmp.py`，1588 交易日）

| 档位 | 总收益 | 年化 | 最大回撤 | 年化波动 | 夏普 | 卡玛 |
|---|---:|---:|---:|---:|---:|---:|
| 基线 ind_cap=2 | 41.62% | 5.45% | −18.22% | 13.80% | 0.25 | 0.30 |
| B9 ic0 | 32.04% | 4.33% | −10.63% | 9.28% | **0.25** | 0.41 |
| 中证800（基准） | 39.46% | 5.21% | −38.75% | 19.29% | 0.17 | 0.13 |
| 中证红利低波 922 | 69.65% | 8.40% | −18.39% | 17.07% | 0.37 | 0.46 |

**配对检验（日收益，n=1587）**：年化差 −1.12pp，**t = −0.49 → 噪声**。

### 3.3 判读

- **换手**：+30.2pp，**方向与研究目标完全相反** → B9 判死。
- **回撤** −18.22% → −10.63%（改善 7.60pp）**看起来很美，但不能当卖点**：
  同期年化波动 13.80% → 9.28%（降 33%），回撤降幅（42%）**基本由波动下降解释**，
  夏普 **0.25 → 0.25 纹丝不动** → 没有 alpha，只是整体换了批低波动股票。
  按既有红线：「凡收益降/回撤小，先假定是减震器」。
- **集中度**：银行权重 31.7% → **100%**，个别期次 12 只全银行。
  官方 930955 的"行业权重≤20%"软约束在此情形救不了——全是同一行业时，
  归一化后该行业权重恒为 100%，软约束无法跨行业分散。

### 3.4 口径自证：✅ 已通过（md5 逐位一致）

第一版 `--ind-cap 0` 未做重排，长出的是 `select_stocks` 原始序（score 降序 = volatility 升序）、
无行业 cap。这与「`--final-key volatility` + 无 cap」应当等价 —— 但**不能靠推导，必须实测**：

| 2023 短窗口（4 期）产物 | md5 |
|---|---|
| 第一版（score 序，无 cap）`..._ic0_...` | `5e784e840de58803900eb6dc418c6d40` |
| 修复版（显式 vol 键，无 cap）`..._ic0_kv_...` | `5e784e840de58803900eb6dc418c6d40` |
| 对应 NAV（第一版） | `f3abdadf50f16af4d843d943bb1a7d3c` |
| 对应 NAV（修复版） | `f3abdadf50f16af4d843d943bb1a7d3c` |

**sel 与 NAV 双双逐位一致** → 全窗口那次 57min 回测**无需重跑**，结果可直接引用
（产物已按真实口径补 `_kv` 标签重命名）。

> 📌 顺带结论：`score = −volatility`，所以在 `--final-key volatility` 下，
> `select_stocks` 的原始序**已经就是**目标序，重排是幂等的。但如果哪天 `score` 的定义变了，
> 这个等价性就会失效 —— 所以修复代码（显式 `sort_values(final_key)`）**保留**，
> 语义是"`--ind-cap 0` 尊重 `--final-key`"，不再依赖 score 的巧合。

---

## 四、机制：行业 cap 为什么是"稳定器"而不是"换手源"

直觉认为"硬约束会强制换手"，实测相反，机制是：

1. **cap=2 时**：48 只候选被强制摊到 6+ 个行业，每个行业只留**该行业内排名最前**的 1~2 只。
   "各行业头部"是**慢变量**（银行的低波动股下期还是它），跨期稳定 → 换手低。
2. **取消 cap 后**：top12 被波动率最低的一批（银行）整体填满。
   银行**内部 12 只的波动率排名会随行情频繁互换** → 每期换的是"哪几只银行"，换手反而更高。

→ 换手的主因是**同一批高度相似标的之间的排名漂移**，
**行业 cap 通过强制跨行业分散，意外地躲开了这种漂移。**

这与 B7' 的发现同构：_constraint 表面上"限制"，实际可能"稳定"；
在改约束之前必须先跑实测，不能靠"换手应该会降低"的直觉。

---

## 五、本轮踩坑清单（4 条，均已修或已记录）

1. **🔴 让参数"关掉"一个函数时，先确认它还顺带干了什么**
   `_cap_industry(cap=0)` 看起来是"不做行业限制"，实际同时关掉了**按 final_key 重排**
   （`cap<=0` 时第一行就 return）。已修：显式 `--ind-cap` 时补 `sort_values(final_key)`，
   且只挂在 `IND_CAP_OVERRIDE is not None` 上，不动 `official` 模式（原生 ind_cap=0）的历史行为。
   ⚠️ **曾因此误判**：第一轮我看到"取消上限后 12 只全银行"就判它是 bug，
   实际上那正是"取消行业上限"的**正确**结果（只是当时还不知道基线也是 vol 键）。
   真正的收获是这条代码隐患，不是那次回测错了 —— 别把"结果反直觉"直接等同于"代码有 bug"，
   **先钉死对照组的口径**。
2. **🔴 改默认参数值必须同时改产物标签**（§二）。基线是 vol 键却无 `_kv` 标签，
   导致"今天的默认"与"昨天的产物"静默不可比。
3. **🔴 离线实验室必须先自证复现率再信它的数字**。本轮复现率只有 4.41/12，
   实验室却照常输出了 9 行"换手对比表"和一句"−34.3pp 最大杠杆"。
   → 实验室类脚本应内置"复现率自检"，低于阈值直接拒绝出结论。
4. **改选股逻辑后必须删同名 partial**，否则断点续跑复用旧结果，日志照打、退出码 0。

---

## 六、结论与下一步

- ❌ **B9（取消每行业≤2）判死**：换手 +30.2pp、银行 100%、收益无改善（t=−0.49 噪声）。
- ❌ **B8（换排序键）判死**（延续昨日结论，但理由要改：实验室 case B 才是现状，
  vol 键本来就是基线口径，不是"换"来的）。
- ✅ **引擎改动保留**：`--ind-cap`（opt-in，`_ic{n}` 标签）、`--start`/`--end`、
  以及 `--ind-cap 0` 时的 final_key 重排修复 —— 都是干净的 opt-in，不碰默认。
- ✅ **引擎改动保留**：`--ind-cap`（opt-in，`_ic{n}` 标签）、`--start`/`--end`、
  以及 `--ind-cap 0` 时的 final_key 重排修复 —— 都是干净的 opt-in，不碰默认。
- ✅ **已修 `divlow_b9_pool_lab.py`**：加 `--verify-sel <引擎sel.csv>` 复现率自检
  （默认阈值 11/12）。实测它现在会自己报出
  「fwd_yield 4.41/12、volatility 9.52/12 → 引擎实际键 = volatility」并**拒绝输出结论**，
  正是本次事故的防护。

---

## 七、🔴 45% 目标的结构性上限：三条路**全部证伪**

§六 曾把真杠杆指向"持仓 ≈ 合格池"（这是从历史记忆继承的判断，未经实测）。
本节用**极低成本**（SQL + 离线池子，共几分钟，不需要 57min 回测）验证——**它也被证伪了**。

### 7.1 路①：合格池改绝对门槛 → 证伪（严门槛反而更不稳）

24 个有效选股日（剔除 `daily_basic` 缺失的 20210407 / 20260108 / 20260407），
纯股息率门槛下池子自身的稳定性：

| 股息率门槛 | 池子均值 | 逐期留存 | 换名率 | 「持仓=池子全部」时年化单边 |
|---|---:|---:|---:|---:|
| >1.0% | 1704 | **86.8%** | 13.2% | **52.7%** |
| >2.0% | 875 | 82.3% | 17.7% | 70.7% |
| >3.0% | 493 | 78.9% | 21.1% | 84.4% |
| >5.0% | 163 | 69.1% | 30.9% | 123.6% |
| **>6.0%** | **95** | **62.4%** | **37.6%** | **150.3%** |
| 现役 top48（排名门槛） | 48 | 70.3% | 29.7% | **118.8%** |

🔴 **反直觉**：**门槛越严，池子越不稳**。>6% 的留存(62.4%)比 top48(70.3%)还低——
卡在门槛边缘的股票，股息率随股价小幅波动就频繁跨越门槛，"绝对"门槛其实是**最脆**的边界。
留存够高的宽门槛（>1%：86.8%）需要**持仓 1704 只**，等于买下大半个市场，不是 930955 复制。

### 7.2 路②：扩持仓到 ≈ 池子 → 证伪（降幅有限，仍远超 45%）

同一批池子、同一排序键（vol 升序、不限行业、dy 加权），只变持仓数：

| 持仓数 | 每期单边 | **年化单边** | 每期双边 |
|---:|---:|---:|---:|
| 8 | 35.6% | 142.5% | 71.2% |
| **12** | 34.1% | **136.5%** | 68.3% |
| 20 | 31.4% | 125.5% | 62.8% |
| 32 | 30.8% | 123.0% | 61.5% |
| **48（=池子全部）** | 28.8% | **115.3%** | 57.6% |

扩到 48 只确实降换手（−21.2pp），但**仍高达 115.3%**，距 45% 还差 70pp。

✅ **顺带完成池子自证**：持仓 12 + 不限行业，池子估算 **136.5%** vs 引擎实测 **134.0%**
（差 2.5pp）→ **池子在"无行业 cap"口径下是准的**。
→ §一的偏差根因定位：池子 ⊃ 引擎候选集（多出少量股票）；纯排序不受影响，
  一旦叠加行业 cap，多出来的股票可能正好是某行业的 rank1/rank2 → 复现率掉到 9.52/12。

### 7.3 路③：降频 → **有效，但先得看清它在哪个口径上**

B5' 报告：季度 142.3% → 年度 **51.0%**（−64%）。这是本主线唯一被证明有效的杠杆，
代价是收益端配对 t 全不显著 + 回撤单调恶化约 5pp。

### 7.5 🔴 口径地雷：142.3% / 51.0% 与基线 103.8% **根本不可比**

拍板引用数字前发现的硬伤——**B5' 那组数（142.3%→51.0%）是股息率键口径，
而现役基线（103.8%）是波动率键口径**，两者混讲会凭空多算 38pp 的"降幅"。

判定依据（`_cap_industry` 末尾无条件按 `sort_key` 重排 → **文件行序直接暴露排序键**）：

| 文件 | 首期 volatility 序列 | 判定键 | 年化单边 |
|---|---|---|---:|
| 季度 **sel**（基线 NAV 配套） | 0.1418 0.1419 0.1439 … **升序** | **volatility** | **103.8%** |
| 季度 **partial**（B5'/fair_cmp 读的） | 0.2167 0.2154 0.2077 … 降序 | fwd_yield | 142.4% |
| 年度 sel / partial | 同左，降序 | fwd_yield | 52.6% |

🔴 **季度档存在两个孤儿产物**（同窗口、同参数，只有排序键不同，选股 20200108 仅重合 3/12）：
`fair_cmp` / B5' 读 **partial**，审计脚本读 **sel** → 同一档位算出 142.4% 与 103.8% 两个数。
（half / year 档的 partial ≡ sel，只有季度档有此问题。）

**这同时推翻了「B8 换排序键判死」的结论**——那条结论出自复现率只有 4.41/12 的问题实验室。
引擎实测的干净对照是：**同窗口同参数，仅换键 → 142.4%（fwd_yield）→ 103.8%（volatility），降 38.6pp（−27%）**。
方向与官方 930955 的编制逻辑一致（波动率是慢变量），B8 不是死了，是**被坏工具误杀**。

于是形成 2×2 因子矩阵（频率 × 排序键）。🔑 **以后只认 `divlow_turnover_matrix.py` 出的数**——
它用**同一函数、同一窗口**一次算完四格并做因子分解，杜绝"数字汤"：

    venv_ml/Scripts/python.exe divlow_turnover_matrix.py

| 年化单边 | fwd_yield 键 | volatility 键 |
|---|---:|---:|
| **季度** | 142.4% | **103.8%**（现役基线） |
| **年度** | **52.6%** ✅ | 67.6% |

> ⚠️ **本表与 B5'/B7 报告里那套数（48.0% / 50.7% / 51.0% / 60.3%）不可混用**——
> 那几份报告的窗口起点各不相同（2020-12-14 / 2013 起），且未标注排序键。
> 本表统一窗口 `20200101~20260723`、统一方法、显式标注排序键。

### 7.6 🔴 两个杠杆**不叠加**，存在负交互

四格全齐后分解（`divlow_turnover_matrix.py` 自动算）：

| 对照 | 变化 | 效应 |
|---|---:|---|
| 【yield 键】季度 → 年度 | 142.4% → 52.6% | **−89.8pp（−63%）** |
| 【vol 键】季度 → 年度 | 103.8% → 67.6% | −36.3pp（−35%） |
| 【季度档】yield → vol | 142.4% → 103.8% | **−38.6pp（−27%）** |
| 【年度档】yield → vol | 52.6% → 67.6% | 🔴 **+15.0pp（恶化）** |

🔑 **排序键的效果依赖调仓频率**：
- 季频（持有 3 个月）：波动率是**慢变量** → 换 vol 键省 38.6pp ✅
- 年频（持有 12 个月）：持有期拉长后**波动率排名漂移反超股息率**（分红政策黏性强，
  股息率排名在一年内比波动率更稳）→ 换 vol 键**倒亏 15.0pp** ❌

→ **"照抄官方 930955 用 vol 键"只在季频成立**；官方是「年度 + 持仓 100 + ≤20% 上限」的组合，
   我们的「12 只 + 无上限」结构下**单独抄排序键不成优势**。

### 7.7 ✅ 最优格 = 年度 + fwd_yield 键（换手与收益**双优**，不是代价交换）

| 档位 | 年化单边换手 | 总收益 | 年化 | 最大回撤 | 波动 | 夏普 | 卡玛 | 配对 t |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 季度 + vol 键（现役基线） | 103.8% | 41.62% | 5.45% | −18.22% | 13.80% | 0.25 | 0.30 | — |
| 年度 + vol 键 | 67.6% | 48.39% | 6.21% | −16.37% | 11.12% | 0.38 | 0.38 | +0.13 噪声 |
| **年度 + fwd_yield 键** | **52.6%** | **80.28%** | **9.41%** | −17.36% | 14.55% | **0.51** | 0.54 | **+0.97 弱** |
| 〔基准〕中证红利低波 000922 | — | 69.65% | 8.40% | −18.39% | 17.07% | 0.37 | 0.46 | — |
| 〔基准〕中证 800（000906） | — | 39.46% | 5.21% | −38.75% | 19.29% | 0.17 | 0.13 | — |

窗口 20200101~20260723 / 1588 交易日，口径 hfq（`close × adj_factor`，含分红再投）。
🔴 **年度+yield 档是唯一同时跑赢 000922 的配置**（年化 9.41% vs 8.40%、夏普 0.51 vs 0.37），
且换手只有季度基线的 **37%**。

⚠️ **但不能当硬卖点**：只有 **7 个调仓期**，配对 t=+0.97 属"弱"，未过 |t|≥2 的显著线。
按既有红线（|t|<0.9 判噪声），这条是"**弱正信号 + 换手机械性降 63%**"，
**只能卖"低换手"，不能卖"收益更高"**。

### 7.4 结论

> **45% 目标在「季度调仓 + 持仓 ≤50 只」下结构性无解，且没必要硬压。**
> 三条路：绝对门槛（严了更不稳 / 宽了要持仓 1704 只）、扩持仓（极限 115.3%）、
> 硬上限 / 缓冲带 / 取消行业上限（全部判死，见 §三）。
> 唯一有效的是**降频到年度**，但**收益端只有 7 期样本、t 弱**，不能当硬卖点。

---

## 八、✅ 决策记录（2026-09-04 用户拍板：方案 a）

### 8.1 拍板内容

> **采纳 (a)：接受年度调仓的换手水平，卖点表述成"低换手"而非"45%"。**

- ❌ **不再追求"压到 45%"**——§七已证三条路（绝对门槛 / 扩持仓 / 约束形式）全部到不了。
- ✅ **改口径**：对外只讲「**一年调仓一次、年化单边换手 ~53%**」，不再报"45% 目标/达成率"。

### 8.2 落地的权威数字（唯一来源 `divlow_turnover_matrix.py`）

| 项 | 值 | 说明 |
|---|---:|---|
| 配置 | **年度调仓 + fwd_yield 键 + 每行业≤2 + 持仓 12** | `--rebal year`（默认键） |
| **年化单边换手** | **52.6%** | 窗口 20200101~20260723 / 7 期 / 每期单边 52.0% |
| 对照：季度基线 | 142.4% | 同窗口同方法（yield 键口径） |
| **降幅** | **−89.8pp（−63%）** | 机械性确定，不依赖样本量 |
| 摩擦节省 | ≈ 0.31%/年 | 按单边 0.35% 全成本估（52.6%→142.4% 差 89.8pp × 0.35%） |
| 🚨 状态 | **临时值，暂不可发布** | §8.5.1：当前代码复现不出该产物（对拍 0/6 期一致） |

⚠️ **引用时必须带三件套**：`窗口 + 排序键 + 口径（年化单边）`。
本表数字与 B5'/B7 报告的 48.0% / 50.7% / 51.0% / 60.3% **不可混用**（窗口起点不同、未标键）。

### 8.2.1 🔴 窗口敏感性：换手随窗口起点变化，所以只能说"低换手"

同一配置（年度 + yield 键），只换窗口起点：

| 窗口 | 期数 | 每期单边 | **年化单边** |
|---|---:|---:|---:|
| 2020-01 ~ 2026-07 | 7 | 52.0% | **52.6%** |
| 2013-01 ~ 2026-09 | 14 | 63.3% | **63.6%** |

→ 早期（2013-2019）候选池不稳、质量门淘汰更多，换手天然更高（与 B7 §5.6 同向）。
**这正是"低换手"不该写死成 45%（或任何精确值）的实证理由**：
对外只能讲「**一年调仓一次，年化单边 ~53%（2020 起窗口）**」并**标注窗口**。

### 8.3 建议卖点表述（可直接用）

> **低换手**：一年只调仓一次，年化单边换手约 **53%**（2020 起窗口），
> 是季度调仓版本（142%）的 **三分之一**；摩擦成本每年省约 **0.3%**。
> ⚠️ 不承诺收益——年度档在本窗口年化 9.41%、夏普 0.51（优于 000922 的 8.40%/0.37），
> 但**仅 7 个调仓期、配对 t=+0.97 未达显著**，属弱信号，不得当卖点。

### 8.4 由此关闭 / 新开的线

- ❌ **关闭**：45% 目标、B7 缓冲带、B7' ≤20% 硬上限、B9 取消行业上限、合格池绝对门槛、扩持仓至池子。
- ❌ **关闭**："抄官方 vol 键"——只在季频有效，年频反向（§7.6）。
- 🟡 **保留未答**（不再自行推进，涉及策略重定义）：
  1. 是否把年度档设为**落地默认**（现默认仍是季度 `official_compact`）？
     切换会改变 `README_strategy.md` 的对外数字与 `run_backtest.bat` 入口。
  2. 是否实测「**年度 + 持仓 48**」？这是唯一未验证的组合（降频 × 扩持仓，
     但 §7.6 已示杠杆会互相干扰，收益未必叠加）。~1h。
  3. 年度档只有 7 期，收益结论统计力不足；若要坐实需更长窗口（引擎 2013 起有数据，
     但 2020 前候选池不稳，见 §8.2.1：换手从 52.6% 升到 63.6%）。

### 8.5 🚨 未决：年度档存在**确定性失败**（命中优先级⑩"回测非确定性 bug"）

**现象**：同参数（年度 + yield 键）、同一调仓日，两个不同起点窗口的产物**选股不一致**：

| 调仓日 | 2020 起跑批 | 2013 起跑批 | 交集 |
|---|---|---|---:|
| 20201214 | 12 只 | 12 只 | **5/12** |
| 20211213 | 12 只 | 12 只 | **2/12** |
| 20221212 | 12 只 | 12 只 | **4/12** |
| 20231211 | 12 只 | 12 只 | **3/12** |
| 20241216 | 12 只 | 12 只 | **7/12** |
| 20251215 | 12 只 | 12 只 | **6/12** |

→ **逐位一致 0/6 期**。

**先排除"孤儿产物"解释**（本主线已踩过两次）：

| 对比 | 平均交集 | 用池子判键结果 | 定性 |
|---|---:|---|---|
| **季度**：2020 起 vs 2013 起 | 4.26/12（0/27） | 2020起=**volatility**(9.52 vs 4.41)；2013起=**fwd_yield**(5.37 vs 4.30) | ✅ **只是换键，非 bug** |
| **年度**：2020 起 vs 2013 起 | —（0/6） | 两者行序均**非** vol 升序 → 都是 fwd_yield 键 | 🚨 **仍无法解释** |

→ 季度档的分歧**已被解释**：13:12 那批默认一度是 `volatility`，B8 落地后默认改回 `fwd_yield`，
   两个产物只是排序键不同（与 §7.5 的口径地雷同源）。**不是非确定性 bug。**
→ **年度档的两个产物同为 fwd_yield 键却仍不一致** → 分歧真实存在，继续查。

**已排除的两种解释**：
1. ❌ **池子窗口依赖**：预筛 SQL 是 `SELECT ts_code FROM dividend_detail WHERE div_proc='实施'
   AND cash_div>0 AND ex_date <= ? GROUP BY ts_code HAVING COUNT(1)>=3` —— **按 asof 之前全历史计数，
   不受跑批 START 约束** → 两个跑批在 20201214 的候选池应完全相同。
2. ❌ **状态依赖**：`buffer_k=0`、`turnover_cap=0` → 选股无滞后项，与上一期持仓无关。

**剩余最可能**：两个产物产自**不同代码版本**（09-03 15:31 与 17:13，相隔 1h42m，
当天正在改 B8 的 `sort_key` 参数化）。若成立，则 **52.6% 可能来自旧代码产物，未必能被当前代码复现**。

**处置**：备份 4 个年度档产物到 `_pre_rerun_20260904/`（含 md5 留证），
并**移走 partial 强制重新选股**（🔴 否则断点续跑会直接复用旧结果 → 假阳性"复现成功"），
用当前代码重跑同参数同窗口对拍。

#### 8.5.1 🚨 对拍结果：**52.6% 无法复现**（当前代码 ≠ 旧产物）

| 调仓日 | 今天重跑 vs 15:31 产物 | 今天重跑 vs 17:13 产物 |
|---|---:|---:|
| 20200108（首期） | **12/12 ✓** | — |
| 20201214 | 9/12 ✗ | 5/12 ✗ |
| 20211213 | 4/12 ✗ | 5/12 ✗ |
| 20221212 | 5/12 ✗ | 7/12 ✗ |

→ 今天用当前代码的**年度 + fwd_yield 档与两个旧产物都不一致** → **52.6% 暂不可信**。

#### 8.5.2 已逐一排除的假设

| 假设 | 判据 | 结论 |
|---|---|---|
| 池子窗口依赖 | 预筛 SQL `ex_date <= ?` 按 asof 前**全历史**计数 | ❌ 排除 |
| 状态依赖（滞后项） | `buffer_k=0` / `turnover_cap=0` | ❌ 排除 |
| 波动率窗口依赖 | `_calc_volatility` 用 `trade_date<=? ORDER BY DESC LIMIT w+1`，窗口无关 | ❌ 排除（另注：它用**未复权 close**，除权跳空会污染波动率，是另一个待修项） |
| 数据版本变化 | DB 文件 mtime **09-03 16:32**（恰在两次跑批之间）；但同一日期同一股票的 `dv_ttm` / `volatility` 在两产物间**差异恒为 0** | ❌ 排除 |
| 排序键并列 + 不稳定排序 | 20221212 的 12 只 `dv_ttm` / `score` **无重复值** | ❌ 排除 |
| 排序键不同（孤儿产物） | 季度档**确认是**（2020起=vol / 2013起=yield，见上表）；年度档两者行序均非 vol 升序 → 同为 yield 键 | ❌ 年度档排除 |

#### 8.5.3 剩余嫌疑（未定位）

1. **`fwd_yield` 的计算依赖不可见状态**——它不落盘，无法跨产物比对；若它依赖
   `dividend_detail` 之外的窗口/缓存，就会只影响排序不影响 `dv_ttm`/`volatility`。**头号嫌疑**。
2. **候选池大小不同**：日志会打 `候选=N`，但旧跑批的日志已丢失，无法比对。
3. **喂给不稳定排序的输入行序不同**：`sort_values` 默认 `kind='quicksort'` **非稳定排序**；
   若上游 SQL **无 `ORDER BY`**，行序可随查询计划/缓存状态变化 → 即使无并列也可能换序。

#### 8.5.4 处置与影响

- 🔴 **52.6%（及整个 2×2 矩阵）降级为「临时值」**，**不得对外发布**。
- ✅ **决策 (a) 作为方向仍成立**（年度调仓确实比季度低一个量级，降幅机械确定）；
  但**具体数字必须等确定性修好后重新测定**。
- 🔜 **下一步（也是最关键的隔离实验）**：**同一条命令连跑两次、比对 md5**。
  若两次不同 → 存在真随机性（行序/集合迭代）；若两次相同 → 差异来自窗口/状态，
  再逐项剥离。**在修好之前，任何换手数字都只是临时值。**

---

<!--NEXT_STEPS-->
