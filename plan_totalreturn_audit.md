# 视频精华提取与平台借鉴评估

> **来源**：《2020—2024，沪深300竟没跑赢存款？我让50个自己算了一遍》（BV1VZ4k6mE5e，UP：百万红利自由计划，2026-08-30）
> **评级**：内容 B+（数字零差错、方法论诚实）／商业 C（评论区挂"免五"开户返佣，违规话术，已入观察名单）
> **整理**：2026-08-31

---

## 结论先行

这个视频最值钱的不是"红利 16.09 万 > 存款 10.86 万 > 沪深300 10.79 万"那个排行榜——**单点样本，不可外推**。真正值得平台借鉴的是它的**全收益口径自觉**。

而拿这条标尺回头量我们自己，**发现一个正在系统性发生的错误**：

> **平台几乎所有策略的"超额收益"，都是拿「含分红的策略净值(hfq)」去比「不含分红的基准价格指数」，超额收益被系统性高估约 2.3–2.6%/年（复利）。**

这不是理论担忧，是实测数字（见 §1）。**修它的优先级高于视频建议的三个研究方向中的任何一个。**

---

## §1 【P0 发现】全收益口径偏差——我们自己正在犯

### 1.1 事实核查（已验证）

| 检查项 | 结果 | 证据 |
|---|---|---|
| 基准指数用什么 | `000300.SH` / `000906.SH` 的 `index_daily.close` | `run_backtest.py:1691/1693`、`run_all_in_one.py:64`、`run_csi800.py:67` 等 |
| 该 close 是否含分红 | **否**（价格指数，除息即回落不复投） | `index_daily` 表仅 OHLC/pct_chg，无股息处理 |
| 策略净值口径 | **hfq 后复权**（含分红再投） | `run_monthly_rebalance.py` 双轨机制 `raw × adj_factor` |
| 库里有无全收益指数 | **无**。`index_daily` 共 20 个指数，全为价格指数 | 已查询确认 |

**→ 分子含分红、分母不含分红，超额收益必然虚高。**

### 1.2 偏差量化（本平台数据实测）

用 `index_constituent`（成分快照）JOIN `daily_basic`，按 `circ_mv` 加权 `dv_ttm` 算指数股息率：

| 基准 | 区间 | 加权股息率(年化) | 价格指数累计漏计分红 |
|---|---|---|---|
| **沪深300** `000300.SH` | 2014–2026（144 快照） | **2.63%** | **+40.2%**（近 5 年 +15.1%） |
| **中证800** `000906.SH` | 2010–2026（189 快照） | **2.29%** | **+47.1%**（近 5 年 +13.7%） |

逐年股息率（沪深300）：2014 **3.32%**／2015 2.18／2016 2.55／2017 2.27／2018 2.64／2019 2.57／2020 2.35／2021 2.09／2022 2.57／2023 3.00／2024 3.08／2025 3.02／2026 2.62。

> **口径说明（诚实标注）**：`SUM(dv_ttm × circ_mv) / SUM(circ_mv)` 的分母含 `dv_ttm` 为空的个股，故上述股息率是**保守下限**，真实偏差只会更大。与中证官方公布股息率可能有 ±0.3pp 差异。

### 1.3 影响面

- 一个 5 年回测报"跑赢沪深300 +20%"，**真实超额只有约 +5%**；
- 对**红利类策略影响最大**：策略本身股息率高（hfq 端水分足）、基准又漏计分红，两头叠加；
- 可能**翻转部分既有结论**——某些"显著正超额"的策略，在全收益基准下可能只是持平。

### 1.4 修复方案（现有数据即可，不需外采）

自建全收益基准（bottom-up），数据齐备：

| 需要 | 表 | 覆盖 |
|---|---|---|
| 成分股 | `index_constituent` | 000906 **2010+**(195 快照)／000300 **2014+**(150 快照)／000905·000016·000852 可用 |
| 权重 | `daily_basic.circ_mv` | 2010-01-04 起 |
| 分红再投 | `adj_factor`（hfq） | 2010-01-04 起 |

**做法**：每个成分快照日，按 `circ_mv` 定权 → 用 `adj_factor` 折算各股 hfq 收益 → 加权合成指数全收益日序列 → 落库为 `index_total_return` 表 → 全局基准切换。

⚠️ **数据缺口（诚实标注，不绕过）**：
- 沪深300 成分快照**最早 2014-01-30**（非 2010），故 300 全收益只能建 2014 起；2010–2013 段需补 `index_weight`/`index_constituent` 才能覆盖。
- **红利类指数（000922 中证红利、930955 红利低波）在库里只有日线、无成分、无权重**，无法自建全收益 → 见 §2 方向①的替代方案。

---

## §2 三个研究方向的评估

### 方向①：红利 vs 宽基 滚动窗口全样本检验 —— **可做，但必须用 ETF 全收益口径**

- **价值**：★★★★☆ 直接回答"红利跑赢是常态还是 2021–2024 特定 regime"，把视频的单点样本升级成全分布。
- **卡点**：000922/930955 无成分数据，自建全收益走不通。
- **解法（不绕过，用等价真实载体）**：改用**场内 ETF 后复权**作为全收益代理——
  - `510880.SH` 红利ETF（2010-01 起，跟踪上证红利）
  - `510300.SH` 沪深300ETF（2012-05 起）
  - 配 `etf_adj_factor` 折算
- **额外好处（比视频更诚实）**：ETF 净值**已扣管理费等成本**，比视频"指数全收益、不扣基金费用"的口径更接近实盘可得收益。视频自己在不足里声明了这点，我们顺手补上。
- **做法**：所有 5 年滚动窗口（日频滚动约 2500+ 个窗口），统计红利全收益 − 宽基全收益的差值分布：胜率、中位数、分位数、按起点的 regime 归因。

### 方向②：序列收益风险 / 现金桶（cash bucket）—— **可做，价值高，需新框架**

- **价值**：★★★★☆ 视频最有水平的一段（提款期"先跌后涨更惨"）。这是平台**完全空白**的 decumulation 领域，且对"红利自由"目标直接相关。
- **数据齐备（已验证）**：
  - 债券/货币层：`511010.SH` 国债ETF（2013+）、`511990.SH` 华宝添益货币ETF（2013+）、`511260.SH` 十年国债ETF（2017+）
  - 权益层：`510880` 红利ETF、`510300` 300ETF 等 29 只 ETF
- **做法**：建提款模拟框架——每年/月固定提款 X%，对比两种规则：
  1. **无现金桶**：按目标比例再平衡后卖出（下跌期被迫卖股票）
  2. **现金桶**：提款先从现金/债券层出，仅当现金层耗尽或权益超配时才卖股票
  → 输出：终值分布、破产概率、最大提款率（safe withdrawal rate）。

### 方向③：行为折损量化（年末换赢家）—— **低优先级，与已有工作重复**

- **价值**：★★☆☆☆ 规则本质是"追上一年度冠军"，平台已有**动量网格、ETF 轮动 V6、申万板块轮动**三条线覆盖同一命题。
- **判断**：不单独立项。若要量化"行为偏差年化成本"，直接复用现有动量/轮动回测换一个"年末排名换仓"的再平衡触发器即可，边际成本低，但不值得占主线。

---

## §3 视频自身的坑（借鉴时必须修正）

| 视频不足 | 我们的修正 |
|---|---|
| **单点样本**（仅 2020–2024） | 全滚动窗口 + 全分布，不取单一区间 |
| **存款对照组偏弱**（挂牌 1.45–1.75%，未算大额存单/国债同期 3%+） | 用 `511010` 国债ETF / `511260` 十年国债ETF 做真实无风险利率 |
| **未扣红利税** | 建模差别化个税：持股 ≤1月 20%／1月–1年 10%／>1年 0%。红利策略若持有<1年，按 4% 股息率计约 **0.4%/年** 拖累，hfq 口径隐含 0 税，偏乐观 |
| **未扣基金费用** | 用 ETF 后复权（自带扣费），比指数全收益更贴近实盘 |

---

## §4 建议路线（按 cheapest/most-impact 排序）

| 优先级 | 事项 | 数据 | 产出 | 影响 |
|---|---|---|---|---|
| **P0** | **全收益基准工程**：自建 `index_total_return`（000906 2010+、000300 2014+） | 现成 | `build_tr_index.py` + 全策略超额收益重算 | **修正所有历史结论的口径偏差** |
| **P1** | 红利 vs 宽基全滚动窗口（ETF 全收益口径） | 现成 | `run_dividend_vs_broad.py` + 差值分布 | 回答"红利跑赢是否常态" |
| **P2** | 现金桶 / 序列收益风险提款模拟 | 现成 | `run_decumulation.py` + 安全提款率 | 开辟平台空白领域 |
| **P3** | 行为折损（复用动量/轮动改触发器） | 现成 | 低成本对照 | 边际，可跳过 |
| 补数据 | 000300 成分快照补 2010–2013；000922/930955 成分权重 | **需外采** | — | 按"缺数据优先补全不绕过"原则，外采而非降级 |

---

## §5 【已落地】全收益基准建成 + 策略口径重述（2026-08-31）

### 5.1 已建成 `index_total_return`（脚本 `build_tr_index.py`）

| 基准 | 区间 | 官方价格指数年化 | 自建全收益年化 | **隐含股息率** | 日收益相关性 | 校验 |
|---|---|---|---|---|---|---|
| 中证800 `000906.SH` | 2010–2026（16.0年） | +2.78% | +4.97% | **+2.19%/年** | 0.9847 | **✓ 通过** |
| 沪深300 `000300.SH` | 2014–2026（12.1年） | +6.03% | +9.05% | **+3.02%/年** | 0.9810 | **✓ 通过** |

**两条独立路径互证**：bottom-up 重建的隐含股息率（2.19%）与 §1 用 `dv_ttm × circ_mv` 加权独立估出的中证800 股息率（2.29%）**仅差 0.1pp** → 偏差量化可信。

### 5.2 策略口径重述（脚本 `compare_tr_benchmark.py`）

| 策略 | 区间 | 策略年化 | 旧口径超额 | **新口径超额** | 虚高 | 结论 |
|---|---|---|---|---|---|---|
| **红利低波质量复合**（主线 B1-B7） | 2020–2026（6.3年） | +4.40% | +2.27%/年 | **−0.99%/年** | +3.25pp/年（累计+22.6%） | ⚠️ **翻转：跑赢→跑输** |
| 高股息+基本面成长 | 2014–2026（11.5年） | +14.43% | +7.37%/年 | **+4.37%/年** | +3.00pp/年（累计+38.5%） | 方向不变，超额缩水 41% |

**最重要的结论**：主线 **红利低波质量复合策略"跑赢沪深300"是被基准口径缺陷制造的假象**——换成全收益基准后实为 **−0.99%/年 跑输**。这直接影响 B1-B7 主线的取舍判断，应先行修正再继续优化。

### 5.3 踩过的坑（已修复，记录以免重犯）

1. **段间水平重置（致命）**：第 k 段只算到 `s_{k+1}-1`，不含下一快照日 → 下一段 `I[s_{k+1}]` 为 NaN → 回退成 1.0 → **每月把指数重置一次**，累计收益被抹平（表现为年化 +0.20%、TR−PR 仅 0.01%）。修复：段内算到下一快照日（含）。
2. **快照日 `circ_mv` 缺失**（`20160729`/`20240731` 等 6 天）→ 该段权重为空被跳过 → 触发同样的水平重置。修复：回退到最近可用交易日 + 兜底沿用上一段权重（**绝不 continue**）。
3. **`circ_mv` 重复行**：`index_constituent` JOIN 产生重复 `(ts_code, trade_date)`，权重数组 2469 条 vs 成分 1331 只 → 广播报错。修复：`drop_duplicates(subset=["ts_code"])`。
4. **校验锚点选错（方法论）**：起初拿"未复权 close 价格版"去比对官方价格指数，必然失败——未复权价在**送股/转增除权日**会暴跌，而官方指数除权日市值不变、不该回落。正确锚点是 **全收益版 vs 官方价格指数**，差值应落在合理股息率区间。
5. **性能**：全量 `circ_mv` 加载要 9.5 分钟；改为"仅快照日 + 缺失时按需补回退日"后降到 **1.6 分钟**。

### 5.4 【数据缺口已用 Tushare 补全】（2026-08-31，用户指示"缺失数据去下载，不绕过"）

**关键认知修正：此前"Tushare 无红利指数成分数据"是后缀错误造成的假象。**

Tushare `index_weight` 对红利类指数的代码后缀是 **`.CSI`，不是 `.SH`**：

| 代码 | 结果 |
|---|---|
| `000922.SH` | **0 行**（此前据此误判"无数据"） |
| `000922.CSI` | **100 行** ✓ |
| `930955.SH` | **0 行** |
| `930955.CSI` | **100 行** ✓ |

**更大发现：Tushare 提供中证官方全收益 / 净收益指数日线（`H` 前缀 / `CNY020` 后缀）：**

| 本地价格指数 | Tushare 全收益代码 | 名称 |
|---|---|---|
| `000300.SH` | `H00300.CSI` | 300收益（沪深300全收益） |
| `000906.SH` | `H00906.CSI` | 800收益（中证800全收益） |
| `000922.SH` | `H00922.CSI` | 中红收益（中证红利全收益） |
| `930955.SH` | `H20955.CSI` | 红利低波100全收益 |
| `000922.SH` | `000922CNY020.CSI` | 中证红利**净收益**（已扣红利税） |

> 注：`H00955.CSI` 是"中证国企全收益"**不是**红利低波；红利低波全收益是 `H20955.CSI`，别搞混。

**已完成补全（全部幂等，INSERT OR REPLACE）：**

| 项 | 前 | 后 | 脚本 |
|---|---|---|---|
| 沪深300 成分快照 | 150（2014起） | **272（2010-01-29 起）** | `backfill_index_constituent.py` |
| 中证红利 000922 成分 | **0** | **192 快照（2010-2026）** | `backfill_dividend_constituents.py`（新） |
| 红利低波100 930955 成分 | **0** | **107 快照（2017-2026）** | 同上 |
| 上证红利 000015 成分 | 0 | 175 快照（2011-2026） | 同上 |
| 深证红利 399324 成分 | 0 | 197 快照（2010-2026） | 同上 |
| 官方全收益/净收益日线 | 无 | **5 条序列（2010-2026）** | `download_tr_index.py`（新） |

**自建 vs 官方交叉验证（独立互证，自建方法可靠）：**

| 基准 | 官方全收益年化 | 自建全收益年化 | 差异 | 日收益相关性 |
|---|---|---|---|---|
| 沪深300 | +8.83% | +9.22% | **+0.396pp** | 0.9821 ✓ |
| 中证800 | +5.90% | +6.08% | **+0.174pp** | 0.9889 ✓ |

**用官方权威口径重算策略对照（`compare_tr_benchmark.py` 已改为优先官方、回退自建）：**

| 策略 | 旧口径超额 | 新口径超额（官方） | 虚高 | 结论 |
|---|---|---|---|---|
| 红利低波质量复合 | +2.27%/年 | **−0.27%/年** | +2.54pp/年（累计+17.2%） | ⚠️ **仍翻转**（跑赢→跑输） |
| 高股息+基本面成长 | +7.31%/年 | **+4.66%/年** | +2.64pp/年（累计+33.4%） | 方向不变，超额缩水 36% |

> **⚠️ 2026-09-01 更正（见 §8）：上表新口径数字仍不可用作结论。**
> 这两个策略的 NAV 是**未复权价**计算的（`run_dividend_low_vol_quality_bt.py` 用 `daily.close` 成交与估值，
> 分红只用于选股筛选、从不计入 NAV），而基准换成了**全收益指数**。
> 这是**反向口径错配**：raw 净值 vs TR 基准 → 超额被系统性低估约 3.7pp/年。
> 修正复权口径后，红利低波质量复合的超额由 **−0.27%/年 → +3.18%/年（跑赢）**，结论再次翻转。详见 §8。

**红利税拖累（官方全收益 vs 净收益，补上视频未扣税的坑）：**
中证红利 全收益 +8.32%/年 → 净收益 +7.81%/年，**拖累 +0.52%/年**。

### 5.5 下一步（待你拍板）

1. **全局基准切换**：把 `run_backtest.py` / `run_monthly_rebalance.py` 等基准读取改为优先用 `index_tr_official`（官方全收益），失败回退 `index_total_return`（自建），再回退 `index_daily`（需加开关，避免一次改动波及过多）。
2. **重跑并复核**所有报过"正超额"的策略，列出被口径翻转的清单。
3. ~~补数据~~ **已完成**（见 §5.4）。可选项：用新补的 000922/930955 成分做**红利 vs 宽基全滚动窗口**（§2 方向①），现在数据已齐备，可直接开工。

---

## §5.6 全局基准切换实现（2026-09-01）

**结论**：平台基准已从"价格指数(不含分红)"全局切换为"全收益优先(官方→自建→价格)"。所有走 `run_backtest` / `run_monthly_rebalance` 引擎的策略**自动生效**——不逐个改 80 个脚本，集中改两个主干引擎 + 统一真相源。

**实现要点**
- `bench_index.py`（新建，统一真相源）：查找链 `index_tr_official`(官方全收益) → `index_total_return`(自建) → `index_daily`(价格)；含 `BENCHMARK_TR_MAP` 映射、透明度 `meta`、4 种 mode。
- 开关：`config.BENCHMARK_MODE="tr_official"`（默认），或环境变量 `MFS_BENCHMARK`（优先级：函数参数 > 环境变量 > config > 默认）。`mode ∈ {tr_official, total_return, price, net}`。`price` 用于复现旧结论，`net` 用已扣税净收益。
- 接入① `run_backtest.py`：`load_benchmark` 委托 `bi.load_benchmark`（返回 df 向后兼容，用模块级 `_last_bench_meta[code]` 记口径）；两处报告打印 `[表|口径]` 标签。
- 接入② `run_monthly_rebalance.py`（被 30+ 脚本 import）：顶部 `import bench_index as bi`；3 处 `index_daily` 散点（逐年窗口 2120、总涨幅 3027、3703）替换为 `bi.benchmark_return_between` / `bi.benchmark_year_endpoints`，报告打印口径标签。
- 数据缺口补全：`backfill_tr_index_gaps.py` 闭合 `H00906`(2021/2024/2025)、`H20955`(2023)、`000922CNY020`(2022/2024) 整年缺口——根因是 `download_tr_index.py` 按年循环个别年失败被 `continue` 静默跳过。

**验证（全绿）**

| 检查项 | 结果 |
|---|---|
| 单元：4 指数默认 mode → `index_tr_official` | ✓ |
| 单元：000906 2024 单年 → 官方（修复前错误回退 total_return） | ✓ |
| 单元：net fallthrough（000300 无净收益映射 → 官方全收益） | ✓ |
| 集成：run_backtest.load_benchmark 委托生效 | ✓ 1212 行, src=index_tr_official |
| 集成：run_monthly_rebalance benchmark_return_between | ✓ src=index_tr_official, +8.39% |
| 交叉：官方 vs 自建全收益吻合 | 差异 0.14–0.40pp, 相关性 0.98+ |

**待迁移（按"避免波及过多"有意暂缓，非遗漏）**
以下 6 个脚本独立 `def load_benchmark`，仍走旧口径，需后续迁移：
`run_all_in_one.py` / `run_mean_reversion_headfake_ablation.py` / `run_mean_reversion_mfi_obv_ablation.py` / `run_mean_reversion_pctb_divergence_ablation.py` / `run_oversold_bounce.py` / `run_rsi_bb_dual_ablation.py`
迁移方式：头部加 `import bench_index as bi`，把 `load_benchmark(code,s,e,conn)` 调用替换为 `bi.benchmark_return_between(code,s,e,conn=conn)[0]`（取收益）或保留 df 形态用 `bi.load_benchmark(...)`。

## §7 【方向①已落地】红利 vs 宽基 全滚动窗口检验（2026-09-01）

**脚本**：`run_dividend_vs_broad_rolling.py`（官方全收益口径；`--csv` 可导出每窗口明细 11144 行到 `data/div_broad_rolling.csv`）
**回答的核心问题**：红利跑赢宽基，是常态还是 2021–2024 特定 regime？

### 7.1 方法
- 直接读 `index_tr_official` 4 条官方全收益日线（已确认 2010-01-04~2026-08-28 完全对齐，各 4045 行）：
  `H00300`(沪深300)、`H00906`(中证800)、`H00922`(中证红利)、`H20955`(红利低波100)。
- 主检验 **5 年 = 1260 交易日日频滚动**（每配对 2786 个窗口）；交叉检验 **3 年 = 756 交易日**（每配对 3290 个窗口）。
- 每窗口计算 `红利全收益累计 − 宽基全收益累计 = 累计超额`（并几何年化），统计全分布（胜率/中位数/分位数）并按**窗口起点年份**做 regime 归因。

### 7.2 5 年窗口全分布（每配对 2786 窗口）

| 配对（红利 − 宽基，全收益） | 胜率 | 中位累计超额 | 中位年化超额 | 分位 10/25 | 分位 75/90 |
|---|---|---|---|---|---|
| 红利低波100 − 沪深300 | **83.0%** (2313) | +52.34% | +5.05% | −19.74% / +17.63% | +77.26% / +103.96% |
| 红利低波100 − 中证800 | **87.8%** (2446) | +51.35% | +4.91% | −3.67% / +23.39% | +82.65% / +103.83% |
| 中证红利 − 沪深300 | **87.5%** (2439) | +37.42% | +3.44% | −7.35% / +18.67% | +48.23% / +54.79% |
| 中证红利 − 中证800 | **98.1%** (2733) | +35.24% | +3.31% | +6.74% / +22.20% | +47.86% / +56.74% |

### 7.3 5 年窗口按起点年份 regime 归因

红利低波100 − 沪深300（胜率 / 中位累计 / 中位年化）：

| 起点年 | 2010 | 2011 | 2012 | 2013 | 2014 | 2015 | 2016 | 2017 | 2018 | 2019 | 2020 | 2021 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 胜率% | 100 | 100 | 100 | 100 | 100 | 39.8 | 0.0 | 66.4 | 100 | 100 | 100 | 100 |
| 中位累计 | +67.8 | +84.4 | +116.9 | +92.2 | +59.2 | −5.0 | −27.4 | +4.0 | +41.3 | +45.5 | +62.6 | +50.7 |
| 中位年化 | +6.2 | +7.8 | +9.3 | +7.7 | +4.6 | −0.6 | −2.6 | +0.4 | +4.1 | +4.7 | +6.2 | +5.3 |

中证红利 − 中证800（最稳配对，胜率 / 中位累计 / 中位年化）：

| 起点年 | 2010 | 2011 | 2012 | 2013 | 2014 | 2015 | 2016 | 2017 | 2018 | 2019 | 2020 | 2021 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 胜率% | 100 | 100 | 100 | 100 | 100 | 81.6 | 96.7 | 100 | 100 | 100 | 100 | 100 |
| 中位累计 | +22.7 | +28.0 | +51.4 | +49.9 | +48.8 | +6.7 | +6.0 | +23.1 | +37.5 | +46.0 | +49.8 | +36.0 |
| 中位年化 | +2.1 | +2.7 | +4.2 | +4.4 | +4.0 | +0.7 | +0.6 | +2.4 | +3.8 | +4.7 | +5.0 | +3.8 |

**聚焦（起点 2017–2021 的 5 年窗口，1086 窗口）**：红利低波100−沪深300 胜率 92.4% / 中位累计 +44.6% / 年化 +4.55%；中证红利−中证800 胜率 100% / 中位累计 +41.4% / 年化 +4.15%。

### 7.4 3 年窗口交叉检验（关键：揭示 regime 依赖）

3 年（756 交易日）下，红利优势**不再单调**，而是明显受风格周期驱动：

| 配对 | 2015起 | 2016起 | **2017起** | **2018起** | 2019起 | **2020起** | **2021起** |
|---|---|---|---|---|---|---|---|
| 红利低波100−沪深300 胜率% | 100 | 43.4 | **0.0** | **0.0** | 82.0 | **100** | **100** |
| 同上 中位累计% | +16.4 | −1.9 | **−32.4** | **−24.8** | +9.5 | **+53.4** | **+58.5** |
| 中证红利−中证800 胜率% | 100 | 100 | **5.3** | **5.8** | 90.2 | **100** | **100** |

→ 2017–2018 起点（3 年窗口覆盖 2017 白马 + 2019–2020 核心资产/茅指数牛）红利 **0% 跑赢**、中位超额 −20~−32%；2020–2021 起点（覆盖 2021–2024 红利/中特估行情）则 **100% 跑赢**、+50%+ 累计。

### 7.5 结论（直接回答方向①）

1. **不是"纯 2021–2024 特例"，也不是"纯恒定常态"——是结构性长期占优 + 短期 regime 依赖。**
   - 在 **5 年**持有窗口上，红利因子对宽基是**近似单调占优**：全样本 83–98% 窗口跑赢，中位年化超额 **+3.3~+5.1%/年**；除 2015–2016 起点（覆盖 2015 泡沫 + 2019–2020 核心资产牛）外，所有起点年胜率 ≥66%，多数 100%。
   - 但缩短到 **3 年**，优势明显 regime 依赖：2017–2018 起点（成长/核心资产牛）红利 0% 跑赢、中位 −20~−32%；2020–2021 起点（红利/中特估）100% 跑赢。→ **2021–2024 的红利跑赢是一次"成长→价值"的风格轮动反转，而非红利因子的新发现或恒定常态。**

2. **视频标题是单点区间选择偏差。** "2020–2024 沪深300 没跑赢存款"恰好落在红利 favorable regime，既不能外推为"红利长期碾压宽基"（2017–2021 宽基含核心资产曾大幅跑赢红利），也不能外推为"宽基长期不行"。其真正有价值的是那把"全收益口径"尺子（已在 §1 用来自查出 ±2.5%/年基准偏差）。

3. ~~**⚠️ 最该修的真问题：因子有效，但平台主动实现没吃到（约 5pp/年缺口）。**~~
   **【2026-09-01 已被 §8 推翻】** 这条推断建立在"策略 −0.27%/年"之上，而该数字是 **raw 净值 vs 全收益基准**的口径错配产物。
   按 §8 修正复权口径后：策略 hfq **+7.86%/年**，vs 沪深300TR **+3.18%/年（跑赢）**，vs 同赛道红利低波100TR **仅 −0.92%/年**。
   **所谓"5pp/年实现拖累"实际只有约 0.9pp/年**，且策略夏普 **0.57 > 指数 0.43**、回撤 **−11.24% < 指数 −15.24%**——不是"没吃到因子"，而是**以更低风险基本复制了该 beta**。
   §7.3 原始条目保留删除线，作为口径错配如何制造伪结论的记录。

### 7.6 方法论诚实标注（避免误读）
- 滚动窗口**高度重叠**（日频 2786/3290 个窗口共享大量交易日），胜率**不是独立伯努利试验**，不能当显著性检验；仅作"持有期占比"描述。per-start-year 分组已部分解耦。
- 全部用**官方全收益指数**（已含分红再投），未扣红利税/基金费。若按 §3 扣 4% 股息率对应的红利税 ~0.4%/年，红利优势略缩但量级不变。
- 4 序列已 inner-join 对齐到共同交易日，端点用实际交易日累计收益（末/初−1），误差可忽略。
- 脚本刻意不经 `bench_index` 的 `.SH→.CSI` 映射，直接读 `H` 前缀官方序列，避免映射边缘情形干扰纯研究口径。

## §8 【P0 级 bug，2026-09-01】策略 NAV 不含分红 → 全口径审计结论反转

### 8.1 发现

做方向①时试图解释"被动红利低波100TR 超额 +4.10%/年，主动策略却 −0.27%/年"这 4.37pp 缺口。
**在归因之前先查了策略 NAV 的计价来源**——结果发现分子本身是错的：

`run_dividend_low_vol_quality_bt.py` 全程用 `daily.close`（**未复权**）成交与估值，
`dividend` 表**仅用于选股筛选（分红连续性），分红从不计入 NAV**。

实证确认 `daily.close` 未复权：工行 601398.SH **20240716 除息日**，`close` 6.20 → 5.89（−5.0%），
同日 `adj_factor` 2.2360 → 2.3537（+5.26%）。即**策略把每次除息当成真实亏损吃掉，却从不把分红记回来**。

> **后果**：§5.2/§5.4 把 raw 净值拿去比全收益基准，是**反向口径错配**——
> 比 §1 那个"hfq 策略 vs 价格基准（虚高）"更隐蔽，因为它让策略**看起来跑输**，容易被当成"策略不行"而误伤。

### 8.2 修复中踩到的三个坑（都有实证，务必记住）

| # | 坑 | 症状 | 正解 |
|---|---|---|---|
| 1 | **成交价与估值价不在同一价格空间** | 只改估值价为 hfq、成交价仍用 `get_open_price`(raw) → NAV **+70.36%/年、年化波动 777%、MDD −85%** | 成交价必须同源：新增 `bulk_open_prices()` 返回同归一化的 `open×adj/adj_ref` |
| 2 | **hfq 绝对价位被累计因子放大** | 格力 000651.SZ raw 67.9 → hfq **10589**（156倍）；回测按**整股**下单 `int(per//px)`，10万/12≈8333元在 9347 元/股下只能买 **0 股**，资金全趴现金 → 伪现金拖累 | **首日因子归一化**：`px = close_t × adj_t / adj_ref`。首日价格严格等于 raw，量级保持 raw 附近，整股下单不受影响，同时保留分红增长 |
| 3 | **`adj_factor` 整交易日缺行** | 2020–2026 共 **132 天全市场同缺**（782万行中缺 66.2 万行 = 8.5%）。`fillna(1.0)` 会让缺失日价掉回 raw、次日跳回 hfq → 巨额假跳空 | `groupby(ts_code).ffill()`。adj_factor 是**阶跃函数**（仅除权除息日变化），前向填充**数学上精确、不丢信息** |

**归一化后的价格空间校验**（首日应严格等于 raw）：

| 股票 | 首日 raw | 首日 hfq_norm | 末日 hfq 涨幅 | 末日 raw 涨幅 | 分红贡献比 |
|---|---|---|---|---|---|
| 601398.SH 工行 | 5.970 | **5.970** ✓ | +85.00% | +28.64% | 1.438 |
| 600036.SH 招行 | 38.880 | **38.880** ✓ | +35.81% | +0.08% | 1.357 |
| 000651.SZ 格力 | 67.900 | **67.900** ✓ | −11.73% | −40.25% | 1.477 |

### 8.3 顺带根治的回测非确定性 bug

同一参数两次回测结果不同（raw：29.72% vs 30.07%）。根因**又是集合迭代顺序**：

```python
all_codes = set(positions.keys()) | set(rb_target)   # ❌ 无序
for code in all_codes:
    ...
    if cost <= cash and diff > 0:      # 现金约束 → 迭代顺序决定谁能买到
```

买入受 `cost <= cash` 现金约束，`ts_code` 字符串集合的迭代顺序受 `PYTHONHASHSEED` 随机化
→ 成交顺序变 → 组合变 → NAV 变。**与 MEMORY 记录的 livermore v2 是同一类 bug**（该处是 `list(set)[:slots]` 切片）。

修复：`sorted(set(positions.keys()) | set(rb_target))`（`run_nav` 与 `run_nav_weighted` 两处）。
**验证：raw ×2、hfq ×2 各跑两遍，结果逐位一致**（raw 129,550 / +29.55%；hfq 161,124 / +61.12%）。
未钉 `PYTHONHASHSEED`——排序修复后本就与哈希种子无关。

### 8.4 修正后结果（窗口 20200102–20260723，1588 交易日 / 6.30 年）

| | 总收益 | 年化(252) | 年化(365) | 最大回撤 | 年化波动 | 夏普(2%) |
|---|---|---|---|---|---|---|
| 策略 **raw**（旧，不含分红） | +29.55% | +4.19% | +4.03% | −11.24% | 10.30% | 0.21 |
| 策略 **hfq**（新，含分红再投） | **+61.12%** | **+7.86%** | +7.55% | **−11.24%** | 10.21% | **0.57** |
| 沪深300TR | +33.41% | +4.68% | +4.50% | −41.56% | 19.00% | 0.14 |
| 中证800TR | +39.46% | +5.42% | +5.21% | −38.75% | 19.29% | 0.18 |
| 中证红利TR | +69.65% | +8.75% | +8.40% | −18.39% | 17.07% | 0.40 |
| 红利低波100TR | +69.96% | +8.78% | +8.43% | −15.24% | 15.59% | 0.43 |

**同口径超额（策略 hfq − 官方全收益基准）：**

| 基准 | 旧结论（raw 净值比 TR 基准） | 新结论（hfq 净值比 TR 基准） | 修正幅度 |
|---|---|---|---|
| 沪深300TR | −0.27%/年（跑输） | **+3.18%/年（跑赢）** | **+3.45pp** ⚠️ 结论翻转 |
| 中证800TR | — | **+2.44%/年** | — |
| 中证红利TR | — | −0.89%/年 | — |
| 红利低波100TR（同赛道） | — | **−0.92%/年** | — |

**分红贡献**：hfq − raw = **+3.67pp/年**（累计 +31.57pp）。

> **📐 2026-09-02 口径注记（两个数字来源，引用前必看）**
>
> 1. **本表超额是"年化算术差"**（策略年化 − 基准年化）。改用几何比 `(1+r_s)/(1+r_b)−1` 则略低
>    0.07~0.14pp：沪深300 **+3.04%**、中证800 **+2.31%**、中证红利 **−0.82%**、
>    红利低波100 **−0.85%**。**两种口径都不改变任何结论方向**，本文统一算术差以保持行间可比。
>    `compare_tr_benchmark.py` 输出的是**几何比**（其 +3.04% 与本表 +3.18% 是同一事实，非矛盾）。
> 2. **"旧结论"列的 −0.27%/年 是早期用"自建全收益基准"算的**；改用官方 TR（+4.68%/年）后，
>    同口径应为 **−0.49%（算术）/ −0.47%（几何）** —— 即"双重惩罚"比当初记的还狠约 0.2pp，
>    但**翻转结论（真超额 +3.0~3.2%/年 跑赢）不受影响，反而更稳固**。

### 8.5 结论修正

1. **"红利低波质量复合跑输沪深300"是伪结论。** 正确口径下 **+3.18%/年跑赢**。
2. **"主动实现拖累 4.37pp/年"是伪命题，实际仅约 0.9pp/年。** 真正的对照应是同赛道指数：
   策略 +7.86% vs 红利低波100TR +8.78% → **−0.92%/年**。这点差距可由换手成本、12 只持仓 vs 指数 100 只的
   分散度差异、hs300 池 vs 全市场池解释，**属正常实现摩擦，不是"因子没吃到"**。
3. **策略风险调整后优于同赛道指数**：夏普 **0.57 vs 0.43**，最大回撤 **−11.24% vs −15.24%**，
   年化波动 **10.21% vs 15.59%**。以约 0.9pp 收益代价换取回撤降 4pp、波动降 5.4pp——**这笔交易划算**。
4. **§7.3 的"因子有效但实现没吃到"立项理由不成立，予以撤回。** 保留删除线作为口径错配制造伪结论的记录。

### 8.6 ⚠️ 平台级审计：这个 bug 不止一处

对全平台 `run_*.py` 扫描（查 `daily` 表但从不引用 `adj_factor` 的脚本）：

```
run_ab_weight / run_chan_lun_validate / run_daily20_macd / run_darvas / run_darvas_gate
run_ep_neutral / run_etf_rotation_v6_merged / run_industry_sentiment_rotation
run_limitup_reversion / run_magic_formula / run_multifactor / run_peg
run_regime_gate_ab / run_shrink_pullback / run_value_backtest / run_macd_timing（部分）
```

抽查确认：`run_value_backtest.py`、`run_magic_formula.py` 的买卖计价确用 raw close（`adj_factor=0`）。
`run_dogs_annual.py`、`run_monthly_rebalance.py` 有 `get_hfq_price` 双轨机制，**不受影响**。

**影响方向**：这些策略若持有高股息股，其 NAV 被**系统性低估**（本次实测 3.67pp/年）；
低股息/成长风格的策略受影响较小。**所有"策略 vs 全收益基准"的历史结论都需要重跑复核。**

### 8.7 交付物

- `run_dividend_low_vol_quality_bt.py`：新增 `--price-mode {raw,hfq}` 开关（**默认 raw，保持旧结果可复现**）、
  `bulk_open_prices()`、首日因子归一化、adj ffill、集合排序修复。hfq 输出文件名加 `_hfq` 后缀，不覆盖旧文件。
- `compare_tr_benchmark.py`：待接入 hfq NAV（当前仍读 raw 文件）。

---

## §9 【平台级，2026-09-01】主干引擎 NAV 也不含分红 → §5.6 切换引入了系统性低估

### 9.1 发现

排查 §8.6 那 16 个脚本时，先去验了**主干引擎本身**——结果发现问题更大：

`run_monthly_rebalance.py` 的 `get_price()`（第415行）docstring 明写
「获取**不复权**收盘价（**实际使用价格**）」，`get_open_price()`（第466行）同样取 `daily.open` 未复权。
两者共在 **10 处**用于持仓估值与成交。

- 该文件内 `close * adj_factor`（hfq）**只出现在信号/因子计算**（1894、1993、3963 行的区间收益、突破信号），
  **NAV 没有任何 hfq 双轨**，也没有 `--price-mode` 开关。
- 对照：`run_backtest.py` 有 `dual/hfq/raw` 开关（默认 dual，第2150行）；`run_dogs_annual.py` 有双轨。
- **因此 MEMORY 与 §1 中「策略净值用 hfq（含分红再投）」的说法对 monthly_rebalance 系不成立。**

### 9.2 影响：我昨天的 §5.6 切换引入了系统性低估

| 阶段 | NAV 口径 | 基准口径 | 是否一致 | 后果 |
|---|---|---|---|---|
| §5.6 **之前** | raw（不含分红） | `index_daily` 价格指数（不含分红） | ✅ 一致 | 超额 = 纯选股α，无偏差 |
| §5.6 **之后** | raw（不含分红） | `index_tr_official` 全收益（含分红） | ❌ **错配** | **超额被系统性低估** |

**量级**（中证800，20200102–20260723，6.30 年）：

| 口径 | 涨幅 |
|---|---|
| 价格指数 | **+20.68%** |
| 官方全收益 | **+39.46%** |
| **差额** | **+18.78pp ≈ 2.6%/年** |

→ 这 2.6%/年就是 §5.6 之后每个 monthly_rebalance 系策略**凭空背上的"超额亏损"**。
**影响面：30+ 个 import 该引擎的脚本。**

### 9.3 对 §1 结论的修正（重要）

§1 原文：「策略净值用 hfq(含分红再投)，基准用价格指数 → 所有超额收益被系统性高估，沪深300 3.02%/年」。

**这个前提只对一半平台成立：**

| 引擎 | NAV 实际口径 | §1 的"虚高"诊断 |
|---|---|---|
| `run_backtest.py` 系 | hfq（有 dual/hfq/raw 开关） | ✅ 成立 |
| `run_monthly_rebalance.py` 系（30+ 脚本） | **raw** | ❌ **不成立**——旧组合本来就一致、无偏差 |

**正确的表述**：问题不是"基准该用全收益"，而是**两端必须同含或同不含分红**。
只换基准、不动 NAV，等于把偏差从"高估"搬到了"低估"，方向反了但错误量级相当。

### 9.4 已落地：口径自动对齐 + 不一致告警

原则：**让基准跟随 NAV，而不是反过来**（零改动风险，立即消除偏差）。

1. `bench_index.resolve_mode(mode=None, nav_price_mode=None)`
   - `nav_price_mode="raw"` → 基准取 `price`（价格指数）
   - `nav_price_mode="hfq"` → 基准取 `tr_official`（全收益）
   - 显式传 `mode` / `MFS_BENCHMARK` / 非 auto 的 config 值时，**显式优先**。
2. `config.BENCHMARK_MODE` 由 `"tr_official"` → **`"auto"`**（新增哨兵值）。
   ⚠️ 这是默认值变更，但原默认值对 30+ 脚本是**系统性错误**，故属修 bug 而非改偏好。
3. `run_monthly_rebalance.py` 新增模块常量 `NAV_PRICE_MODE = "raw"`（声明式，附改动警示注释），
   3 处基准调用透传该参数。
4. `run_backtest.py` 按其 `--price-mode` 透传（hfq→全收益 / raw→价格指数 / dual→全收益）。
5. `bench_index.check_consistency(nav_price_mode, meta)`：口径错配时在报告里打印告警——
   - `raw NAV + 全收益基准 → 超额被【低估】`
   - `hfq NAV + 价格基准 → 超额被【高估】`

**验证**（2026-09-01）：
- `resolve_mode`：`raw→price` / `hfq→tr_official` / 显式覆盖优先 ✅
- `check_consistency`：4 种组合告警方向全部正确 ✅
- 端到端回测（value, 20240101–20260723）：基准标签 `[index_daily|价格指数(不含分红)]`、
  策略 +41.03% vs 基准 +40.58%（超额 +0.21%）、**无告警**、年度分解与导出正常 ✅

### 9.5 【已落地，2026-09-01】`--price-mode hfq` 开关 + 验证闭环

- `run_monthly_rebalance.py` 新增 **`--price-mode {raw,hfq}`（默认 raw，保持历史结果可复现）**。
  hfq 输出文件自动加 `_hfq` 后缀（`backtest_hfq_*.csv` / `annual_pnl_hfq_*.csv` / `trades_hfq_*_*.csv`），
  不覆盖 raw 结果，保证 A/B 对照留证。
- 实现要点（`_adj_asof` / `_apply_hfq`，第 435–476 行）：归一化基准 = 该股**首次被调用时**的因子，
  bisect 查找 + **ffill**（不用 fillna(1.0)，理由见 §8.2 坑#3）。
- 4 个价格返回点已包裹；**指数分支（第 493 行 `000906.SH`）刻意不包裹**——指数不能被乘个股因子。
- 10 个 `get_price` 调用点全部用于 NAV/成交，无一是选股信号（涨停判定走独立的 `_get_day_pct()`，
  直接读 `daily.close/pre_close`），故改 hfq **不污染选股**。
- 顺带修掉 **`_apply_backadjust`（原第 2057 行）的 `fillna(1.0)`**：突破信号路径与 §8 坑#3 同源，
  现改为 `groupby(ts_code).ffill().bfill().fillna(1.0)`，缺失率告警阈值 5% → 2%。
  这是**信号路径**（假跳空 → 假突破），不是 NAV，但同源同害。

**验证（三重交叉，均通过）：**

| 验证 | 方法 | 结果 |
|---|---|---|
| 单元测试 | 5 类不变量（首日归一化 / 分红贡献 / 指数未污染 / 缺行 ffill / 成交估值同空间）| 全 PASS |
| 受控买入持有 | 固定篮子，排除路径效应 | 高股息蓝筹 Top30 **6.76%/年**；中证800 随机100 **3.31%/年** |
| 端到端 A/B | value top20, 2020-01-02→2026-07-23 | raw +2.87%/年 → hfq **+15.23%/年** |

受控实验的 3.31%/年 与「中证800 全收益 − 价格 = 2.23%/年」同量级 → **价格空间实现正确**。

**四象限拆解（几何口径，复利正确）：**

| 象限 | 总收益 | 年化 | MDD | Sharpe | 交易 |
|---|---|---|---|---|---|
| raw + 止损ON | +19.49% | +2.87% | −39.22% | 0.13 | 234 |
| **hfq + 止损ON** | **+144.31%** | **+15.23%** | **−22.52%** | **0.62** | 210 |
| raw + 止损OFF | +97.27% | +11.38% | −30.63% | 0.46 | 212 |
| hfq + 止损OFF | +219.71% | +20.25% | −30.63% | 0.77 | 200 |

- 纯股息贡献（止损OFF，无路径干扰）= 1.2025/1.1138 − 1 = **+7.96%/年** → 与受控实验同量级 ✅
- 止损路径效应 = 12.02% − 7.96% = **+4.06%/年**
- 止损代价：raw 口径 −8.51pp/年，hfq 口径 −5.02pp/年 → **差额 3.5pp 是纯除息伪信号**

### 9.6 遗留（待你拍板）

- `compare_tr_benchmark.py` 仍读 raw NAV 文件，待接入 hfq。
- 6 个独立 `def load_benchmark` 脚本（run_all_in_one.py 等）迁移到 `bench_index`。
- 方向②（序列收益风险/现金桶 decumulation）与方向③（行为折损）尚未开工。
- 16 个独立脚本的**精确分诊已完成** → 见 §11。

---

## §10 【🔴 P0 级新发现，2026-09-01】raw 口径回测从不处理送转股 → 比漏计分红严重得多

### 10.1 发现

在验证 §9.5 的 hfq 开关时，发现 `20210610` 单日 NAV 比值跳变 **+5.29%**。股息不可能单日 5%，
追查后发现根因不是分红，是**送转股**：

| 代码 | 收盘 06-09 → 06-10 | adj_factor | raw 记录 | 真实经济损失（hfq） | 凭空亏损 |
|---|---|---|---|---|---|
| 300853.SZ | 72.36 → 37.55（**−48.11%**）| 1.0040 → 1.8170（+80.98%）| −48.11% | −6.09% | **−42.02pp** |
| 003025.SZ | 44.93 → 31.17（−30.63%）| 1.0000 → 1.4170（+41.70%）| −30.63% | −1.70% | −28.93pp |
| 603737.SH | 227.91 → 164.02（−28.03%）| 2.6360 → 3.7000（+40.36%）| −28.03% | +1.02% | −29.05pp |
| 600276.SH | 84.15 → 73.00（−13.25%）| 53.3560 → 64.1780（+20.28%）| −13.25% | +4.34% | −17.59pp |

**机制**：回测的持仓 `shares` 是买入时锁定的整数，**从不随送转增加**。送转后 raw 价按除权比例下跌，
而股数不变 → 持仓市值凭空蒸发 `1 − 1/送转比例`。hfq 价因 `close × adj` 中 adj 同步上调而**天然免疫**。

### 10.2 为什么这个 bug 比 §8 的「漏计分红」更严重

| | 现金分红 | 送转股 |
|---|---|---|
| 单次幅度 | 1–8%（个别超级分红 18%）| **17%–45%** |
| 频率 | 每年 1–2 次 | 不定期，成长股/次新股高发 |
| 是否触发 −15% 止损 | 偶尔（大额分红）| **几乎必然**（单次就远超 −15%）|

送转单次就能击穿止损线。raw 口径下：送转 → 假跌 −30% → 触发 −15% 止损 → 认赔离场 → 换股。
**三重伤害叠加。**

### 10.3 量化（value top20，2020-01-02→2026-07-23，止损OFF 口径）

从 `trades_value_*.csv` 重建 106 段持仓 / 57 只股票，扫描持仓期内 adj_factor 事件：

| 类型 | 次数 | 累计（持仓市值占比）| 占比 |
|---|---|---|---|
| 分红（含超级分红）| 51 | −161.57% | 77.5% |
| 送转/拆股 | 3 | −46.96% | 22.5% |

单次亏损 Top5：

| 代码 | 日期 | 类型 | 因子跳变 | 持仓市值损失 |
|---|---|---|---|---|
| 300677.SZ | 20220613 | 送转 | +23.03% | **−18.72%** |
| 601919.SH | 20221214 | 送转 | +18.34% | −15.50% |
| 601919.SH | 20230628 | 送转 | +14.61% | −12.75% |
| 601006.SH | 20220707 | 分红 | +7.80% | −7.80% |
| 601000.SH | 20220518 | 分红 | +7.52% | −7.52% |

> 注：按持仓市值占比直接求和，未做组合权重加权与时间复利，仅用于判断相对重要性。
> 分类器阈值 10% 会把中远海控那种 18% 的**超级现金分红**归入送转；
> 但对本 bug 而言两者无差别——都是 raw 的凭空亏损，hfq 都能正确保留。

### 10.4 ⚠️ 附带发现（策略层面，非口径）：value 策略实际只持 ~5 只

`--top-n 20` 只是**上限**，不是目标。质量门（破净 + PE 纵向分位 + 质量门槛 Tier）把候选过滤到个位数：

| 年份 | 2020 | 2021 | 2022 | 2023 | 2024 | 2025 | 2026 |
|---|---|---|---|---|---|---|---|
| 平均持仓只数 | 4.3 | **1.5** | 10.5 | 4.7 | 2.4 | 5.0 | 3.0 |

全程：均值 **5.3** / 中位 **4** / 区间 **0–20**（出现过完全空仓）。

**这直接放大了所有口径问题**：单只权重 ~20%，一次除权事件直击 1/5 的 NAV。
交叉验证：20221214 比值跳 +4.2451%，601919.SH 因子跳 18.34% → 隐含权重 **23.1% ≈ 1/4.3**，
与当日实际持 4 只吻合 ✅。

**结论**：hfq 与 raw 差 12pp/年 中，约 2/3 是真实股息（~8%/年，该组合持仓股息率确实高），
约 1/3 是除息/送转伪信号触发止损的路径伤害。**raw 口径的回测结果系统性不可用。**

### 10.5 修复状态

| 项 | 状态 |
|---|---|
| `run_monthly_rebalance.py --price-mode hfq` | ✅ 已实现并三重验证 |
| `_apply_backadjust` 的 `fillna(1.0)`（信号路径）| ✅ 已改 ffill+bfill |
| hfq 输出文件独立命名（不覆盖 raw）| ✅ 已加 `_hfq` 后缀 |
| 其余 16 个独立脚本 | ⏳ 待分诊 |
| 默认值改为 hfq | ❌ **未做，也不建议一刀切**——30+ 脚本 import 该引擎，改默认会让所有历史结论一次性漂移 |

---

## §11 【2026-09-01】16 个独立脚本精确分诊（粗筛 `adj_factor=0` 的逐个人工复核）

### 11.1 ⚠️ 先更正我自己的错误（§8.6 / §9.6 写过）

> 旧结论：「**`run_multifactor.py` 查 `daily` 只为算动量因子、与 NAV 无关，误改会引入新 bug**」

**这是错的。** 复核源码：
- `run_multifactor.py:309` `positions = {}  # code -> {shares, buy_price, last_price}`
- `run_multifactor.py:378` `cost = max_shares * op`（成交金额）
- `run_multifactor.py:397/412` 遍历 `positions` 逐日估值
- 价格来自 `_ep._px()` / `_ep._exec_price()`，即**复用 `run_ep_neutral` 的实现**

→ **multifactor 确实算 NAV，且是 raw。** 它非但不能"不要改"，反而是
**改 `ep_neutral` 一处即可顺带修复**的高杠杆目标。

### 11.2 分诊方法（不用粗筛）

`adj_factor=0` 只能说明"没引用复权因子"，无法区分"算 NAV"与"只算信号"。
改用三层判据：
1. **是否存在「股数 × 价格」表达式**（NAV 的充要条件）
2. **价格来源**：主干引擎 `get_price` / 自定义函数 / 直连 `daily`
3. **标的类型**：个股（受分红送转影响）/ 指数（不受影响，且两端同口径）

分诊器：`_triage_price_sites.py`（v1，有 `conn.close()` 误报已修）、`_triage_nav_mul.py`（v2，股数×价格判据）。

### 11.3 分诊结果（16/16 已逐个复核）

| 脚本 | 算NAV | 标的 | 价格来源 | 口径 | 处置 |
|---|---|---|---|---|---|
| `run_daily20_macd.py` | ✅ | 个股 | `load_closes()` **（根）** | raw | 🔴 **杠杆点 A** |
| `run_ab_weight.py` | ✅ | 个股 | 复用 `D.load_closes()` | raw | 🔴 随 A 修 |
| `run_ep_neutral.py` | ✅ | 个股 | `_ensure_day()` **（根）** | raw | 🔴 **杠杆点 B** |
| `run_multifactor.py` | ✅ | 个股 | 复用 `_ep._px()` | raw | 🔴 随 B 修 |
| `run_value_backtest.py` | ✅ | 个股 | `get_stock_price()` | raw | 🔴 独立改 |
| `run_etf_rotation_v6_merged.py` | ✅ | ETF | `_query_price()` | raw | 🔴 独立改（ETF 也分红）|
| `run_limitup_reversion.py` | ✅ | 个股 | 直连 `daily` | raw | 🟡 改（短线 1-2 日，量级小）|
| `run_chan_lun_validate.py` | ✅ | 个股 | 直连 `daily` | raw | 🟡 改 |
| `run_darvas.py` | ✅ | 个股 | 引擎 + 自取 | 混合 | ⚪ 待确认 NAV 那一路 |
| `run_darvas_gate.py` | ✅ | 个股 | 引擎 + 自取 | 混合 | ⚪ 待确认 |
| `run_peg.py` | ✅ | 个股 | 引擎 + 自取 | 混合 | ⚪ 待确认 |
| `run_shrink_pullback.py` | ✅ | 个股 | 引擎 + 自取 | 混合 | ⚪ 待确认 |
| `run_magic_formula.py` | ✅ | 个股 | **引擎 `get_price`** | raw→可调 | 🟢 **已获 hfq** |
| `run_regime_gate_ab.py` | ✅ | 个股 | **引擎 `get_price`** | raw→可调 | 🟢 **已获 hfq** |
| `run_macd_timing.py` | ✅ | 个股 | **自带 `adj_factor`** | ✅ 已复权 | 🟢 无需改 |
| `run_industry_sentiment_rotation.py` | ✅ | **行业指数** | `sw_l1_index_daily` | 价格指数 | 🟢 **不适用**（两端同口径）|

### 11.4 关键结论

**16 个中 4 个已 OK / 不适用，12 个需改或待确认。**

**两个杠杆点（改 1 处惠及 2 个）：**
- **A. `run_daily20_macd.load_closes()`** —— docstring 明写「zz800 全部历史成分日收盘（**不复权**）」，
  SQL 为 `SELECT ts_code,trade_date,close FROM daily`。改它 → 同时修 `daily20_macd` + `ab_weight`。
- **B. `run_ep_neutral._ensure_day()`** —— `SELECT ts_code, open, close, high, low, amount, vol, pct_chg FROM daily`。
  改它 → 同时修 `ep_neutral` + `multifactor`。

**不适用此 bug 的两类：**
- `run_macd_timing.py`：自带 `adj_factor`，已含复权。
- `run_industry_sentiment_rotation.py`：策略与基准**同为申万一级行业价格指数**，
  两端都不含分红 → **口径一致，无偏差**。这是个重要提醒：**不是所有 raw 都是 bug，
  关键是两端是否同口径。**

### 11.5 踩坑：分诊器自身的两个误报

1. `\.close\b` 会匹配 `conn.close()`（无参方法调用）→ 产生大量假 NAV 点。修正：`\.(close|open)\b(?!\s*\(\s*\))`。
2. 自定义取价函数（如 `self.get_stock_price`、`_ep._px`）不在 `get_price` 模式内 → **漏判**。
   这正是我一度误判 `value_backtest` / `multifactor` "不算 NAV" 的原因。
   → **自动分诊只能定位，不能定性，必须人工复核源码。**

### 11.6 待你拍板

- 是否动这两个杠杆点（A / B）？改动会让 `daily20_macd`、`ab_weight`、`ep_neutral`、`multifactor`
  四个脚本的**历史结论一次性漂移**，建议先跑 raw/hfq 双跑留证再切。
- 4 个"混合"脚本（darvas / darvas_gate / peg / shrink_pullback）需确认 NAV 那一路走引擎还是自取，
  再决定是否需要改。

---

## §12 【2026-09-01 落地】杠杆点 A/B 双跑留证：**红利/价值策略的超额被低估约 2.0–2.4%/年**

### 12.1 做了什么

§11 分诊出两个高杠杆点，本次为它们加了 opt-in `--price-mode {raw,hfq}`（**默认 raw，历史可复现**）：

| 杠杆点 | 改的函数 | 惠及脚本 |
|---|---|---|
| **A** | `run_daily20_macd.load_closes(hfq=)` | `run_daily20_macd.py` + `run_ab_weight.py` |
| **B** | `run_ep_neutral._ensure_day()` + `_px()` + `_exec_price()` | `run_ep_neutral.py` + `run_multifactor.py` |

**改 2 处 = 覆盖 4 个脚本**，比逐个改 16 个脚本高效得多。

### 12.2 三条实施纪律（与 §8 同源，缺一即产生虚假损益）

1. **归一化** `px = close_t × adj_t / adj_ref`（`adj_ref` = 该股首个已知因子），
   否则绝对价位被累计因子放大数十倍 → 整股下单买 0 股 → 伪现金拖累。
2. **adj 缺行必须 ffill+bfill**，绝不能 `fillna(1.0)`（该表 2020-2026 有 132 个**整交易日全市场同缺**）。
   `_ADJ_LAST` 跨交易日 ffill 状态与 `_ADJ_REF` 归一化基准必须**跨日存活**——
   `run_multifactor` 每日清 `_DAY_PX` 省内存，但**绝不能**清这两个（清了 hfq 会退化成 raw）。
3. **信号侧恒定锁 raw，只让 NAV 侧切换**。
   `load_closes()` 的矩阵同时喂【波动率因子】+【逐只 MACD 金叉】+【NAV 估值成交】三处，
   若一起改，**双跑差异无法归因**（分不清是口径修正还是选股改变）。

### 12.3 双重验证（单元测试 + 端到端）

**单元测试 11 项不变量全 PASS**（`_test_ep_hfq.py` / `_test_daily20_hfq.py`）：

| 不变量 | 实测 |
|---|---|
| hfq 首日 == raw | 差 7.11e-15（浮点误差）|
| hfq/raw 末日比值 ≥ 1 | 最小 1.0000，中位 **2.1358**（2010-2025 累计分红+送转贡献 113%）|
| 归一化有效性 | raw 最大价 2601 → hfq 最大 5530（**2.13×**，而非 156×）|
| 逐日比值无整表跳变 | 最大单日 1.03%（若 ffill 失效会出现 10%+）|
| 送转案例 300853.SZ @ 20210610 | raw **−48.11%** vs hfq **−6.09%** → 凭空亏损 **42.02pp**（与 §10 实测完全一致 ✅）|
| 送转案例 000001.SZ @ 20130620 | raw −41.89% vs hfq −6.21% → 凭空亏损 **35.68pp** |
| `pct_chg` 不受污染 | 两口径下均为 −6.125（涨跌停判定必须基于实际成交价）|
| 分红贡献量级 | 600036.SH **+4.81%/年**（合理区间 2~7%）|

### 12.4 端到端双跑结果

#### (1) `run_daily20_macd` — 红利低波 top20 / zz800 / 满仓·月度重选 / 2020-01-02~2025-12-31

| | raw | hfq |
|---|---|---|
| 总收益 | +39.59% | **+82.79%** |
| 年化 | +5.72% | **+10.58%** |
| 最大回撤 | −14.83% | −10.73% |
| Sharpe | 0.50 | **0.86** |
| 基准(中证800) | +17.67%<br>`价格指数` | +34.30%<br>`[index_tr_official\|官方全收益]` |
| **超额(几何年化)** | **+2.89%/年** | **+5.27%/年** |

+MACD 档（51.5% 时间空仓）：raw +2.22% → hfq +15.45%（分红贡献 2.04%/年 ≈ 满仓档 4.60% 的一半，**与空仓比例自洽** ✅）

#### (2) `run_ab_weight` — NEW(zz800,无MACD)[equal] / N=5 / 2015-01-01~2025-12-31

| | raw | hfq |
|---|---|---|
| 总收益 | +135.11% | **+260.42%** |
| 年化 | +8.08% | **+12.36%** |
| 最大回撤 | −24.52% | −23.77% |
| Sharpe | 0.63 | **0.92** |
| 基准(中证800) | +29.53%<br>`价格指数` | +61.30%<br>`[index_tr_official\|官方全收益]` |
| **超额(几何年化)** | **+5.57%/年** | **+7.58%/年** |

#### (3) `run_ep_neutral` — 行业中性 EP / hs300 / top30 / 2020-01-02~2026-07-23

| | raw | hfq |
|---|---|---|
| 总收益 | −9.45% | **+20.03%** |
| 年化 | −1.56% | **+2.94%** |
| 最大回撤 | −57.88% | −49.27% |
| Sharpe | −0.09 | **0.12** |
| 沪深300 基准 | +13.87%<br>`价格指数` | +33.41%<br>`[index_tr_official\|官方全收益]` |
| **超额(几何年化)** | **−3.43%/年** | **−1.62%/年** |

#### (4) `run_multifactor` — VQM 三因子 / zz800 / top30 / 季度 / 同期

| | raw | hfq |
|---|---|---|
| 总收益 | +17.74% | **+64.22%** |
| 年化 | +2.62% | **+8.19%** |
| 最大回撤 | −46.59% | −37.98% |
| Sharpe | 0.12 | **0.35** |
| 沪深300 基准 | +13.87%<br>`价格指数` | +33.41%<br>`[index_tr_official\|官方全收益]` |
| **超额(几何年化)** | **+0.52%/年** | **+3.25%/年** |

### 12.5 核心结论：红利/价值策略的"股息超额"此前一直被吞掉

| 脚本 | 策略分红贡献 | 基准(沪深300/中证800)分红 | **净低估** |
|---|---|---|---|
| daily20 红利低波 | **+4.60%/年** | +2.23%/年 | **+2.38%/年** |
| ab_weight 红利低波 | **+3.96%/年** | +2.01%/年 | **+2.01%/年** |
| ep_neutral (hs300) | **+4.43%/年** | +2.47%/年 | **+1.96%/年** |
| multifactor (zz800) | **+5.25%/年** | +2.47%/年 | **+2.78%/年** |

四脚本**同向同量级**（+2.0~2.8%/年），交叉互证。

**机制**：红利/价值组合的股息率**高于宽基**（4.5% vs 2.2%），
raw 口径把这部分股息记成"亏损"，而基准端只损失自己那 2% 的股息
→ **两端损失不等，差额 2.0~2.4%/年 被算进了"选股能力不足"。**

这与 §5.6 是**同一枚硬币的两面**：
- §5.6 只改基准不改 NAV → 策略凭空背上 −2.2%/年 的假亏损
- §12 两端都改 → 策略真实的**股息优势** +2.0~2.4%/年 才显现

**一句话**：此前所有"红利低波选股 alpha"的结论都**低估了约 2pp/年**。
不是策略变好了，是账算对了。

### 12.6 本轮新踩的坑（已在代码里固化防御）

| 坑 | 症状 | 处置 |
|---|---|---|
| **数据缓存上限硬编码** | `load_closes` 只到 **20251231**，跑 2026 区间时价格全 NaN → 估值归零，跑出 **−92.90%** 的假结果 | 加 `CLOSES_MAX_DATE` 硬校验 + "末日整行 NaN" 二次防御，命中即 `sys.exit(2)` |
| **基准没跟着换** | ab_weight 首轮双跑基准**完全相同**（+29.53%）→ 一眼看出漏改 | 4 个脚本的基准全部改走 `bench_index`；判定标志 = raw/hfq 两跑基准数字是否不同 |
| **`global` 位置** | SyntaxError（同一项目里两种写法都存在，踩了 2 次）| `if __name__` 模块级块**不加** global；`def main():` 函数内**必须加** |
| **hfq 覆盖 raw 输出** | 静默覆盖，A/B 丢证据（第 3 次踩到）| 4 个脚本输出全部加 `_hfq` 后缀 |
| **既有 `_rm` NameError** | `run_ep_neutral._report()` 访问 `run_backtest()` 的局部 import → 报告打印时崩溃 | 补局部 import（改动前就存在的 bug，非本次引入）|

### 12.7 ⚠️ 数据缺口：中证全指（000985.SH）无可用全收益，该列超额仍被高估

**这不是代码 bug，是数据源缺失**。按"缺数据优先补全不绕过"原则，先做了补全尝试：

| 尝试 | 结果 |
|---|---|
| 本地 `index_tr_official` 表 | 只有 `000300 / 000906 / 000922 / 930955` 四个，**无 000985** |
| Tushare `pro.index_daily("H00985.CSI")` | **0 行**（该代码不存在）|
| Tushare `pro.index_daily("H20985.CSI")` | 有数据，但名称是 **"500LBLV全收益"**（张冠李戴，❌ 不可使用）|
| 本地 `index_daily` 的 `000985.SH` | 完整价格指数（2010-2026，4046 行）✅ 可用但**不含分红** |

**结论**：Tushare 未提供中证全指的官方全收益/净收益序列，自建 bottom-up 需全成分股分红数据（成本高，见 §5）。
当前处置 = **如实告警、不伪造**：

```python
# run_ep_neutral.py / run_multifactor.py 均改为逐基准保留 meta 并逐个告警
bench_meta[idx] = meta          # 原先只在循环末尾留一个 → 会漏掉中证全指
...
for idx, meta in bench_meta.items():
    w = bi.check_consistency(meta, PRICE_MODE)      # 逐个打告警
```

**读报告时的正确姿势**：`ep_neutral` / `multifactor` 的 hfq 那一跑，
**中证全指列会打 `[价格指数]` 标签 → 该列超额被高估约 2.5%/年，请改看沪深300 列**。
§12.4 的 (3)(4) 两张表已经只列沪深300 基准，就是为了避开这个坑。

> 注：这也是"两端必须同含或同不含分红"原则的边界情况——
> 当一端数据拿不到时，**显式标注 + 只读对齐的那一列**，好过静默回退后让读者误判。

### 12.9 ❌ 更正我自己：怀疑的"第二类非确定性 bug"——**已证伪**

我在收尾核查时发现 `multifactor` 的 raw 终值与我改动前记下的数字对不上
（旧 **1,224,516** → 新 **1,177,361**），而我的改动在 raw 分支应完全无副作用，
故怀疑它是继 `run_livermore_v2` / `run_dividend_low_vol_quality_bt` 之后的**第三例非确定性 bug**。

**验证方式**：各脚本用同一命令连跑两次，比对终值/收益率/交易次数。

| 脚本 | 第 1 次 | 第 2 次 | 判定 |
|---|---|---|---|
| `run_multifactor` raw（20250102–20260723, zz800, top30）| 1,263,580 / +26.36% / 218 笔 | 1,263,580 / +26.36% / 218 笔 | ✅ **逐字节一致** |
| `run_ep_neutral` raw（同期, hs300, top30）| 6,545,239 / +30.90% / 236 笔 | 6,545,239 / +30.90% / 236 笔 | ✅ **逐字节一致** |

**结论：非确定性假设被证伪，raw 路径是确定性的。**

代码层面也自洽——raw 分支 provably 无副作用：
- `PRICE_MODE != "hfq"` → `_scale()` 直接 `return v`，一行短路；
- `_ensure_day()` 仅在 hfq 时查 `adj_factor`，raw 下 `adj_map={}` → `a` 恒为 `None`；
- `run_multifactor` 不引用 `run_monthly_rebalance` 的 `get_price`/`get_open_price`
  （全文仅 L272 `_rm.SLIPPAGE_RATE = slippage`），共享引擎的 hfq 改造碰不到它。

**那 1,224,516 从哪来？** —— 是**不同参数的运行**，不是随机性。
已核实：历史 CSV 里 20100101–20260715 那次（改动前、无 `VQM_q` 标签的旧版本）末值是 **543,170**，
也不是 1,224,516。可能来自更早一次 `stock_pool=all` 或不同区间的运行，出处未追回。

**二次复现（全区间，§12.4-4 配置）**：
```bash
./venv_ml/Scripts/python.exe run_multifactor.py 20200102 20260723 --top-n 30 --stock-pool zz800 --price-mode raw
```
→ **1,177,361 / +17.74% / 794 笔**，与 §12.4 (4) 表**完全一致** ✅

> **诚实标注**：我无法重建 1,224,516 的出处（历史 CSV 里也对不上），只能确认它
> **不是**非确定性、**不是**本次改动引入。§12.4 表里的数字已用可复现命令二次确认。
> **教训**：发现数字对不上时，**先做两次复现再下非确定性结论**，别靠记忆里的旧数字反推。

### 12.10 🔴 连带发现：`bench_index.check_consistency()` 的告警**从未真正生效**

验证 §12.7 的"逐个告警"是否真的会触发时，我发现它对中证全指**一声不响**：

```
000985.SH nav=hfq ret=+27.22%  [index_daily|价格指数(不含分红)]     ← 没有任何 ⚠️
```

**根因（双重）**：

| # | 问题 | 后果 |
|---|---|---|
| 1 | `check_consistency` 读 `meta.get("source")`，但实际键名是 **`source_table`** | `src` 恒为 `""`，判定退化 |
| 2 | 退化后只能看 `meta["mode"]`，而 `load_benchmark` 回退时**保留请求口径**（`tr_official`）不改写成实际口径 | 回退到价格指数却自称"全收益" → `bench_div=True` → 与 `nav_div=True` 相等 → **静默放行** |

也就是说：**任何一个无全收益数据而回退到价格指数的基准，在 hfq 那跑都不会告警**。
这不是 000985 独有的问题，是整个安全网的失效——§9.4 建立的"口径错配告警"此前**从未真正生效过**。

**修复（两处，`bench_index.py`）**：

```python
# ① check_consistency：以 source_table（实际来源）为准
src = str(meta.get("source_table") or meta.get("source") or "").lower()
if not src:
    return ""                      # 来源未知时不静默放行，直接判不出
if "index_daily" in src or src == "price":
    bench_div = False
elif ("tr" in src or "total_return" in src or "net" in src):
    bench_div = True

# ② load_benchmark 回退分支：mode 改写为实际口径 "price"
quoted_mode = mode                 # 保留请求值仅用于 note
...
return df, dict(mode="price", source_table="index_daily", resolved_code=code,
                note=f"价格指数(不含分红){_suffix}")   # _suffix 仅当请求≠price 时才写"已回退"
```

修复后告警矩阵（`20200102–20260723`）：

| 基准 | nav=raw | nav=hfq |
|---|---|---|
| 000985.SH（中证全指，无全收益）| +27.22% `[价格指数]` **OK** | +27.22% `[价格指数·已回退]` **⚠️ 超额被【高估】** ✅ |
| 000300.SH | +13.87% `[价格指数]` **OK** | +33.41% `[官方全收益]` **OK** |
| 000906.SH | +20.68% `[价格指数]` **OK** | +39.46% `[官方全收益]` **OK** |
| 000922.SH | +22.26% `[价格指数]` **OK** | +69.65% `[官方全收益]` **OK** |
| 930955.SH | +25.39% `[价格指数]` **OK** | +69.96% `[官方全收益]` **OK** |

**对既有结论的影响**：§12.4 的 (3)(4) 两张表里我只列了沪深300 基准（当时是"碰巧"避开了这个坑），
**结论本身不受影响**；但**在此之前跑出、且依赖中证全指列的任何 hfq 结论，都缺少了这层保护**，需要复查。

### 12.11 状态：全部 UNCOMMITTED，未 push

改动文件：`run_ep_neutral.py`、`run_multifactor.py`、`run_daily20_macd.py`、`run_ab_weight.py`
新增测试：`_test_ep_hfq.py`、`_test_daily20_hfq.py`
**默认值仍为 raw，历史结论零漂移**；hfq 需显式 `--price-mode hfq` 触发。

---

### 12.12 【2026-09-01】中证全指全收益**已补全** + 红利低波脚本的基准列此前一直是价格指数

#### (1) 数据缺口闭合：中证全指自建全收益落库

§12.7 判过"中证全指无可用全收益"。按**缺数据优先补全不绕过**原则再查一层，结果是**第三次踩到 `.CSI` 后缀坑**：

| 代码 | `index_weight` 结果 |
|---|---|
| `000985.SH` | **0 行** ← 此前据此判"无数据" |
| **`000985.CSI`** | **3400–5200 行**（2019→2026 全可用）✅ |

与 `000922` / `930955` 是完全同一个坑。`backfill_index_constituent.py` 里那条
"000985 已知无 index_weight 数据"的注释**是后缀错误造成的假象**，已更正。

补全链路：
```bash
./venv_ml/Scripts/python.exe backfill_dividend_constituents.py --only 000985.SH --start 201901
#   → 88 个快照 / 375,308 行，区间 20190131~20260831
./venv_ml/Scripts/python.exe build_tr_index.py --index 000985.SH --start 20190101 --end 20260828
```

**自建结果交叉校验通过**：

| | 年化 |
|---|---|
| 官方价格指数 | +6.80% |
| **自建全收益** | **+8.92%** |
| 隐含股息率 | **+2.11%/年** |

日收益相关性 **0.9921**、跟踪误差 3.14% → 落在校验通过区间。

`bench_index` 已自动拾取（无需改代码）：`000985.SH` 在 hfq 下
**+27.22%（价格）→ +47.69%（自建全收益）**，告警消失。

#### (2) 顺带修掉：`run_dividend_low_vol_quality_bt.py` 的基准列此前恒为价格指数

这是**你的主线策略（红利低波质量复合）**。它的 `benchmark_nav()` **直连 `index_daily`**，
与 NAV 口径脱钩 → hfq 那跑打印的基准列是**价格指数**，超额被系统性高估。

已改走 `bench_index`（`raw→价格 / hfq→全收益`）+ 逐基准口径标签 + 错配告警。

**修复后验证**（窗口 20200101–20260723）：

| 基准 | nav=raw | nav=hfq |
|---|---|---|
| 中证全指 000985 | +27.22% `[价格指数]` | **+47.69%** `[自建全收益]` |
| 中证800 000906 | +20.68% `[价格指数]` | **+39.46%** `[官方全收益]` |
| 中证红利低波 000922 | +22.26% `[价格指数]` | **+69.65%** `[官方全收益]` |

**此前被高估的幅度**（几何年化，6.56 年窗口）：

| 基准 | 价格指数年化 | 全收益年化 | **超额被高估** |
|---|---|---|---|
| 沪深300 | 2.00% | 4.49% | **2.49 pp/年** |
| 中证800 | 2.91% | 5.20% | **2.30 pp/年** |
| 中证全指 | 3.74% | 6.13% | **2.39 pp/年** |
| **中证红利低波** | 3.11% | 8.40% | **🔴 5.28 pp/年** |

**中证红利低波 5.28pp/年 远超其它**——因为它本身就是红利指数，股息率约 5%。
用它做"同赛道参考"时，口径错误造成的虚高最大。

#### (3) ⚠️ 哪些结论受影响、哪些不受

| 结论 | 是否受影响 | 说明 |
|---|---|---|
| **§8.4 的表与结论**（+3.18%/年 vs 沪深300TR）| ✅ **不受影响** | 那张表的基准列是**手工用 `bench_index` 算的**，已是全收益（沪深300TR +33.41% / 中证800TR +39.46% / 中证红利TR +69.65%，与本次重算**完全一致**）|
| **脚本自身打印的报告** | 🔴 **此前是错的** | 任何人跑 `--price-mode hfq` 后读打印出来的超额，都会被高估 2.3pp（vs 中证800）或 **5.3pp/年（vs 中证红利低波）** |
| §12.4 的四张表 | ✅ 不受影响 | 只列了沪深300，且其基准已走 `bench_index` |

**一句话**：数据缺口补全了，脚本的基准列修好了；**历史结论恰好躲过一劫**（因为当初是手工算的基准），
但**脚本自己打印的数字一直在骗人**——这条如果不修，下一个人跑就会得出"策略跑赢同赛道指数"的假结论。

---

### 12.13 【2026-09-01】`run_dogs_annual.py`（狗股/年度调仓）：三轨道共用一个价格基准 → **hfq/real 超额被高估约 2.1pp/年**

#### (1) 问题

§11 分诊时漏了它：狗股是**双轨**（real / hfq / raw 三轨道），但基准只有一个 `index_daily` 价格指数：

| 轨道 | 修复前基准 | 口径 | 状态 |
|---|---|---|---|
| raw（不含分红） | 价格指数 | ✅ 同口径 | 本来就对 |
| hfq（分红当日再投） | **价格指数** | 🔴 失配 | 超额被高估 |
| real（分红趴账） | **价格指数** | 🔴 失配 | 超额被高估 |

狗股是高股息策略，虚高幅度按中证800 口径差（2.30pp/年，§12.12 表）计。

#### (2) 修法：`get_bench_pair()`——**整段同空间**取数

改法中先写了一版 `get_index_close_tr()`（单点取全收益、缺数据回退价格指数），自测时**当场抓住一个自己引入的严重 bug**：
000985 的自建全收益只覆盖 2019+，用 2014 年请求 → 一端取价格、另一端取全收益 → **跨价格空间相除，算出 −99.94%/年 的荒谬数字**。

教训固化为 `get_bench_pair(index_code, d0, d1)`：
- **单点取数绝不跨空间回退**（`_tr_level()` 取不到返回 None，不回退价格）；
- **整段层面决定**：全收益序列覆盖 [d0, d1] → 用全收益；不覆盖 → **整段**回退价格指数并在 note 里标注。

#### (3) 修复后复跑（20140102~20260723，--strategy dogs，配置=默认 20 万）

```
[口径] 基准(TR)=官方全收益(000906→H00906) → 供 真实/hfq 轨道；基准(价格)=不含分红 → 供 raw 轨道
全程   策略(真实) +189.50%  策略(hfq) +233.76%  策略(raw) +72.55%  基准(TR) +161.30%
[注] 基准(价格)= +102.61% → raw 轨道超额 -30.06%；基准(TR)= +161.30% → hfq 轨道超额 +72.46%
```

- `超额(raw) = −30.06%`（vs 价格指数）——**本来就算对了**，本次未动；
- `超额(hfq) = +72.46%`（vs 官方全收益）——修复前会打印 **+131.15%**（vs 价格指数），
  年化口径 **+4.02%/年 → +1.95%/年**，虚高 **≈2.1pp/年**，与中证800 口径差 2.30pp/年 吻合；
- `超额(real) = +28.20%` ★主口径（分红趴账总回报 vs 全收益基准）。

#### (4) 诚实标注两点

1. **绝对数字与 7 月诊断（raw +50.48% / hfq +106.72%）不可比**：那组数字出自 `diag_r_ratio.py`
   （独立简化引擎），与 `run_dogs_annual.py` 正式引擎不同源。本节只声称**超额口径修正**
   （raw 轨道收益 provably 未被本次 diff 触碰，`git diff` 逐行核对过：策略收益三轨道路径零改动）。
2. **real 轨道 vs 全收益基准是"最近似"而非严格同口径**：real 的分红趴账不复利，
   全收益基准的分红再投，两者对分红的处理仍有细微差异，已在打印中标注 ★主口径 供读表人自查。

#### (5) 至此的「正在骗人」清单状态

| 脚本 | 状态 |
|---|---|
| `run_ep_neutral` / `run_multifactor` | ✅ 已修（§12.4） |
| `run_daily20_macd` / `run_ab_weight` | ✅ 已修（§12.4） |
| `run_dividend_low_vol_quality_bt` | ✅ 已修（§12.12） |
| `run_dogs_annual` | ✅ 已修（本节） |
| 其余 raw NAV 策略（`run_backtest` / `run_all_in_one` / 4 个 ablation / `run_oversold_bounce` 等） | ⚪ 现在不算骗（raw+价格=同口径），**未来切 hfq 时必须先接 `bench_index`**，否则重演本节 |

---

### 12.14 【2026-09-01】`run_value_backtest.py` 接入 NAV 侧 hfq：**破净价值组合的收益被低估 ≈5.8%/年**

#### (1) 改了什么

`--price-mode {raw,hfq}`（默认 raw，opt-in）。采用 §12.13 同款**逐持仓 `buy_factor` 归一化**：
- 买入价、整手判定（1手检查、`max_shares`）**恒为 raw**（真实世界 affordability 必须用真实价格）；
- 卖出 proceeds 与每日估值 = `shares × raw(今) × f(今)/f(买入)`；
- `_adj_factor()` 用 `trade_date<=? DESC LIMIT 1` 的 **as-of 查询**（天然 ffill，132 个整日同缺自动继承）；
- **买入日因子缺失 → 该持仓永久锁 1.0**——绝不拿 `f(今)/1.0`（否则绝对因子 ~7.8× 整段虚增，同 §12.13 跨空间教训）；
- 输出文件加 `_hfq` 后缀；`--portfolio-layer` 的 equity_csv 同步带后缀。

本脚本**无基准列**（不打印超额），不存在 §12.12/§12.13 类失配；它的问题是 NAV 本身漏分红（§8 同款）。

#### (2) 双跑结果（默认窗口 20220102~20231229，5 万，zz800 破净价值）

| 口径 | 期末资产 | 总收益 | 年化 |
|---|---|---|---|
| raw | 53,563 | +7.13% | +3.56% |
| **hfq** | **59,943** | **+19.89%** | **+9.94%** |

#### (3) 差额拆解（诚实诊断）

hfq/raw 比值：首日 1.0000 → 末日 **1.1191**（累计 +11.91% ≈ **+5.8%/年**）。
- ✅ 不变量成立：**hfq/raw 恒 ≥ 1**（逐日核验）；
- 比值单日跳变 >1.5% 仅 2 次（+3.0%、+2.5%），**跳变 >8%（送转特征）= 0 次**——本窗口持仓未触发 §10 送转蒸发，**+11.91% 全部来自现金分红**；
- +5.8%/年 高于 §12.4 的 2.0~2.8pp：破净价值组合选出的正是银行/煤炭/钢铁等 5-8% 股息率板块，量级自洽。**2022-2023 熊市里，raw 口径显示 +3.56%/年 的"平庸"策略，真实总回报是 +9.94%/年。**

#### (4) 遗留标注（不改，仅记录）

`run_value_backtest.py` 的年化公式硬编码 `total_return / 2`（"2年"）——窗口非 2 年时打印的年化是错的（本节表中数字按 2 年窗口计算，恰好正确）。属既有问题，未动。

#### (5) 下一杠杆点：`run_etf_rotation_v6_merged.py`（已摸底，未动工）

- ETF 价格走 `etf_daily`（除息日同样下跌，同款漏分红）；
- **`etf_adj_factor` 表已存在且 29/29 全覆盖**（2005→2026）——数据现成，无需补下载；
- 改造点比价值策略复杂：持仓含货币基金（511260）+ VaR 部分减持（`keep_shares`）+ 现金缓冲，money site 约 6-8 处；信号侧（动量/RSRS）须继续锁 raw。

---

### 12.15 【2026-09-01】`run_etf_rotation_v6_merged.py` 接入 hfq：**结论方向反转——ETF 轮动的 raw 口径被份额合并凭空【抬高】，+73.82% 里有 ~47pp 是假财富**

#### (1) 改了什么

`--price-mode {raw,hfq}`（默认 raw，opt-in）。逐持仓 `buy_factor` 归一化，数据源 **`etf_adj_factor` 表（29/29 全覆盖，2005→2026，数据现成无需补下载）**。
5 个 money site 全部接入：timing trim 卖出、轮动卖出、期末平仓、两处买入（记录 buy_factor）、每日市值。
买入价/整手判定/信号侧（动量、RSRS、折溢价）恒为 raw。成交明细导出 `_hfq` 后缀。返回字典补 `daily_vals`。

#### (2) 双跑结果（20200101~20251231，10万，mixed 池，dual 方法）——⚠️ hfq 反而更低

| 口径 | 期末资产 | 总收益 | 年化 | 最大回撤 | 夏普 |
|---|---|---|---|---|---|
| raw | 173,817 | **+73.82%** | +10.05% | −25.20% | 0.38 |
| **hfq** | **121,594** | **+21.59%** | **+3.44%** | −33.69% | 0.14 |

hfq < raw，违反股票世界的 hfq≥raw 不变量——**先诊断，结论见下：这次 raw 才是错的。**

#### (3) 根因：份额合并（股票没有的坑）

因子单调性检查发现 3 只 ETF 因子**下降**：159901（2014）、510500（2015）、**512100（2022-09-05，恰在回测窗口内）**。

512100 份额合并实锤：
```
2022-09-02 close 0.982 → 2022-09-05 close 2.713（×2.76 份额合并）
因子 1.0000 → 0.3622（= 1/2.7607，完全对应）
```

策略恰好持有穿过事件：**2022-08-05 买入 512100 @1.014 × 29,400 份 → 2022-10-14 卖出 @2.512**。

| 记账方式 | 卖出所得 | 说明 |
|---|---|---|
| raw | 29,400 × 2.512 = **73,853** | 🔴 引擎 shares 不随合并调整 → 凭空 ×2.76 假财富 |
| hfq | 73,853 × 0.3622 = **26,749** | ✅ 与真实世界一致（真实持有者股份 ÷2.76、价格 ×2.76，总值不变） |

**假财富 = 47,103 元**。两跑终值差 52,224 ≈ 假财富 47,103 + 再投入复利 ✓ 自洽。

#### (4) 与股票送转的镜像关系（审计框架更新）

| 公司行为 | 因子 | raw 价 | 引擎 shares 不调整的后果 | hfq 修复 |
|---|---|---|---|---|
| 股票送转/分红（§10） | 升 | 跌 | 市值凭空**蒸发** 17~45% | ✅ |
| **ETF 份额合并**（本节） | **降** | **跳升 ×n** | 市值凭空**虚增 ×n** | ✅ |

> **不变量修正**：`hfq ≥ raw` 只在"持仓期无因子下降事件"时成立。正确的不变量是
> **hfq 连续（无公司行为伪跳变）且含分红；raw 双向都可能错**（送转→低估、合并→高估）。

#### (5) 影响面（重要）

- 🔴 **ETF 轮动全家族的 raw 历史数字都可能有这个污染**：只要在 2022-08-05~2022-10-14
  持有 512100（mixed 池含它），raw 终值就被抬高 ~47pp（10万本金口径）。
  **主线②（ETF 轮动 V6 复刻，BV1uPu86sENG）的 raw 口径历史结论需复查**——
  `run_etf_rotation_v6.py` / `_phase3` / `_phase3_sens_clean` / `_oos` / `_survivor_mc` 同引擎同池。
- 修复前 raw 口径"+10.05%/年"的体面成绩，真实（hfq）是 **+3.44%/年**——
  这不属于"分红低估"，属于**记账错误**，量级远大于全部分红贡献。
- 额外收益：hfq 还捕获了持有期分红（159928 因子 1→4 等），方向同前几节。

#### (6) 遗留标注

- `calc_win_rate` 的 FIFO 用 trade 里的 raw 价格——两跑的胜率都是 raw 空间（跨合并日的一对买卖在 raw 空间会被记成暴利）。**胜率数字同样被污染**，后续如需准确胜率应传有效价。
- `timing trim` 的 `cur_equity` 门（L980 附近）保持 raw——只作 >0 判断，不影响金额。

---

### 12.16 【2026-09-01】ETF 轮动家族影响面排查：**只有 `_oos` 被污染，且污染在「选择阶段」——512100 被 +182pp 假排名虚选进 top-4；修正后 OOS 结论更强**

#### (1) 家族逐一排查（用 §12.15 的因子单调性扫描 + 引擎口径核对）

| 脚本 | 池 | 引擎口径 | 是否受 512100 合并影响 |
|---|---|---|---|
| `run_etf_rotation_v6.py`（主线②主复刻） | 4只：红利/创业50/纳指/黄金 | **全链路 close_adj/open_adj**（已复权） | ✅ 不受影响（池里没有 512100；池内无合并事件） |
| `_phase3` / `_phase3_sens_clean` | 扩展20只 / 后视镜4只 | 复用 V6 adj 引擎 | ✅ 不受影响（池无 512100；510500 合并在 2015 窗口外） |
| `_survivor_mc` | DB 动态池（可能含 512100） | 复用 `V6.load_etf`（adj，maxaf 归一化在合并日仍连续） | ✅ 不受影响 |
| `run_etf_rotation_v6_merged` | mixed 20只（含 512100） | raw | 🔴 已修（§12.15） |
| **`run_etf_rotation_v6_oos.py`** | 23只候选（**含 512100**） | **raw，且选择窗口 2018-2023 覆盖合并日** | 🔴 **选择阶段被污染（本节）** |

#### (2) 污染量化：假排名 +182pp，把平庸 ETF 顶到第 1 名

2018-2023 in-sample 排名（raw vs adj）：

| 标的 | raw 累计 | adj 累计 | 差 |
|---|---|---|---|
| **中证1000 512100** | **+185.7%（第1名）** | **+3.5%（跌出前7）** | 🔴 **−182.2pp（全是份额合并假收益）** |
| 消费 159928 | −63.7% | +45.3% | +109.0pp（raw 反向低估——分红+折算） |
| 医药 512010 | −75.0% | −2.3% | +72.7pp |

**top-4 翻转**：raw 版选中 {512100, 黄金, 国债, 新能源车}；adj 正确版选中 {**消费159928**, 黄金, 国债, 新能源车}。

#### (3) 修正后 OOS 结果（已改脚本重跑：`load()` LEFT JOIN `etf_adj_factor`，排名与持有收益一律 close_adj）

| 版本 | OOS top4 组合毛收益 | 说明 |
|---|---|---|
| 污染版（raw） | +52.50% | 假排名选中的 512100 在 OOS 恰好 +68.9%——**"作弊者赢了彩票"，过程不可信** |
| **修正版（adj）** | **+34.88%** | 仍 < 沪深300 +42.59% → **"主动选4 跑输被动" 的证伪结论更强了** |

脚本尾部对照注释已更新为修正数字。

#### (4) 诚实标注

- **净==毛的公式怪癖**（既有，非本次引入）：`ri_net = (exit−exit_cost)/(entry−entry_cost) − 1` 中，
  比例成本（0.025%+0.1%滑点）在分子分母同比例出现 → **几何比中精确抵消**，¥5 最低佣不触发时"净"列恒等于毛。
  该脚本历史输出里的"净(扣成本)"数字从来就没扣过成本（大资金口径）。未修，仅标注。
- OOS 窗口（2024-09 起）本身无合并事件（因子下降仅 2014/2015/2022 三次），持有段 raw==adj 对无分红标的成立；
  修正版的持有收益同时含了持有期分红（如黄金/新能源车无分红，消费/国债因子增长已计入）。

---

### 12.17 【2026-09-02】收尾两个剩余 raw-NAV 脚本：**对照脚本自身的 NAV 侧没对齐（差点把主线①误判成跑输）**；**新工具 `_measure_raw_exposure.py` 零改造量化暴露，4 个脚本里只有 PEG 值得改**

#### (1) `compare_tr_benchmark.py` 读的是旧 raw NAV 文件

这个脚本的**职责就是做口径对照**，但它自己的 NAV 侧却没对齐：Case 1（红利低波质量复合 2020-2026）
指向 `bt_quality_nav_..._hs300_12.csv`（**raw**），而同参数早已产出 `_hfq` 兄弟文件。

**后果（量级实锤）**：`nav_official_compact` raw +29.55% vs hfq +61.12% → **差 +3.67pp/年**。
用 raw NAV 配全收益基准 = 「策略漏分红 + 基准含分红」的**双重惩罚**：

| NAV 口径 | 策略年化 | 沪深300TR 年化 | 超额（几何比） | 判定 |
|---|---|---|---|---|
| raw（脚本旧行为） | +4.19% | +4.68% | **−0.47%/年** | ❌ 误判「跑输」 |
| **hfq（已修）** | **+7.86%** | +4.68% | **+3.04%/年** | ✅ 实际跑赢 |

**修复**：Case 指向 hfq 文件；并加自动告警——**存在 `_hfq` 兄弟文件却仍读 raw 则报警**，防复发。
脚本现在同时输出两行（hfq 主口径 + raw 旧口径仅作对照），避免"改了就没证据"。
（§8 表格中的 −0.27%/年 是早期"自建 TR 基准"的旧值，官方 TR 下同口径为 −0.49%/年，已加注记。）

#### (2) 新工具：`_measure_raw_exposure.py` —— 零改造量化任意策略的 raw 暴露

**思路**：不碰回测代码，只读既有 trades CSV（`date,action,code,price,shares`）→ FIFO 配 round-trip →
逐笔查 `adj_factor` 算 `f(卖)/f(买)` → 直接得到「这笔交易 raw 口径漏记了多少钱」。

**三档指标（关键方法论）**：

| 指标 | 定义 | 用途 |
|---|---|---|
| `drag` | 漏记 ÷ 部署资本·年 | ⚠️ **会被超短持仓年化放大**，仅作参考 |
| **`drag_nav`** | 漏记 ÷ 初始资本 ÷ 年数 | ✅ **判定主指标** |
| `phantom` | 绝对漏记额 | 直觉校验 |

**踩过的坑**：darvas 中位持仓仅 6 天，`drag` 高达 **9.54%/年** 看着吓人，
但 `drag_nav` 只有 **0.61pp/年**、绝对额仅 ¥8,057 —— **是年化放大效应，不是真严重**。
**判定阈值：`drag_nav` <1pp/年 → 不值得改造**，挂账标注即可。

方法校验：抽查 002219.SZ 20151028 单日因子 ×2.4993（真实送转）✓。

#### (3) 4 个脚本的测量结果：只有 PEG 值得改

| 脚本 | 中位持仓 | 漏记额 `phantom` | `drag`（参考） | **`drag_nav`（主）** | 判定 |
|---|---|---|---|---|---|
| **run_peg.py** | 243 天 | **¥1,587,573** | 大 | **+13.15pp/年** | 🔴 **改造** |
| run_darvas.py | 6 天 | ¥8,057 | 9.54% | +0.61pp/年 | 忽略 |
| run_darvas_gate.py | 短 | 小 | — | +0.0xpp/年 | 忽略 |
| run_shrink_pullback.py | 短 | 小 | — | +0.01pp/年 | 忽略 |

PEG 数据自洽性校验（基于旧代码 trades，341 笔）：逐笔 P&L 求和 682,360 ≈ NAV 增益 635,162（差额 = 费 + 现金拖累）✓。
旧代码 raw 基线：1,000,000 → 1,635,162 = **+63.52%（+4.35%/年）**；加回漏记的一阶下界约 **+10.66%/年**（线性近似，见 (6) 复利反推）。

#### (4) `run_peg.py` hfq 接入（5 个资金站点全覆盖，买入侧恒用 raw）

沿用 `buy_factor` 模式：卖出价/估值 = `raw(今) × f(今)/f(买入)`，
**买入成交价与整手判定（1手检查 / max_shares / affordability）恒用 raw**。

- 资金站点：卖出点1 `var_guard_off`、卖出点2 `rebalance`、卖出点3 `backtest_end`、每日估值、买入点（记 `buy_factor`）
- CLI `--price-mode {raw,hfq}`（默认 raw）；输出文件名加 `_hfq` 后缀；头部打印计价口径行
- **性能**：初版每日估值对每仓做一次 SQL（19 仓 × 3046 天 ≈ 5.8 万次）→ 改为
  **按 code 预载整条因子序列 + `bisect` as-of 查找**（1 次查询/股票）。实测 2 万次 as-of 仅 **0.009s**
- raw 模式下 `_hfq_ratio` 短路返回 1.0、不查因子 → **raw 结果与改动前逐字相同**，双跑差异可归因

双跑结果见 §12.17(6)。

#### (5) ⚠️ 诚实标注：旧 PEG 结果（+63.52%）已不可复现

同参数重跑 raw 得 **−59.38%**（原 CSV 是 +63.52%），交易日数 3046 vs 2910。排查结论：

- `get_trade_dates` **未被我改动**（git diff 确认）
- `_rebalance_dates` 逻辑未变：`freq="annual"` 按 `td[:4]` 分组取 `ds[4]` = **每年第 5 个交易日**，
  旧 trades 首笔 20140108 ✓ 一致。（⚠️ `REBALANCE_MONTH=5` 只出现在**打印文案**里，跟 5 月无关）
- **定位**：新跑「最终入选 254 只次 / 13 次」≈ 19.5 只/次 vs 旧跑 341 笔成交 →
  疑似提交 `973659b` 引入的 **ST 护栏**缩减了候选池

⇒ 旧 +63.52% 出自更早的代码版本，**当前代码复现不了**。
但 `drag_nav +13.15pp/年` 是**基于漏记比例**的结论，与选股池无关，**仍成立**。

🔴 **第 4 次踩「同名静默覆盖」**：原 `backtest_n30_...csv` 与 `trades_...csv` 被覆写，
且 `data/results/` 被 .gitignore 忽略 → **git 救不回来**。已备份至 `_peg_backup/20260902_pre_hfq/`。

#### (6) 双跑结果（当前代码，同选股路径，仅 NAV 口径不同）

选股逻辑对 `price_mode` 不敏感（信号侧锁 raw），故 raw 与 hfq 两跑选出的股票序列完全一致，差异纯为 NAV 计价口径：

| 口径 | 末值 | 总收益 | 年化 | 相对倍率 |
|---|---|---|---|---|
| raw（未复权·漏分红+送转） | 406,199 | **−59.38%** | −7.18% | 1.00x |
| **hfq（后复权·含分红再投）** | **2,084,176** | **+108.42%** | **+6.26%** | **5.13x** |

- **总收益差 +413pp，末值 5.13x**；复利反推年化口径差 **+14.49pp/年**，
  与 (3) 暴露测量的 `drag_nav` **+13.15pp/年** 高度吻合（差 1.3pp 来自当前 vs 旧选股路径的微小差别）→ **方法互证，结论可信**。
- 解读：**raw 口径把 PEG 策略记成"巨亏 −59%"是假象**，真实（hfq）是 +108%（+6.26%/年）。
  这比 naive 的"漏记分红"严重得多——根因是 **raw 不处理送转股**：每次持仓踩到送转，
  raw 价除权下跌但股数不变 → 市值凭空蒸发（§10 同机制），12.5 年复利后把正 alpha 吃成了负数。
- ⚠️ **汇报时的口径提醒**：当前代码下 raw = −59.38%（因为 ST 护栏已改变选股，旧 +63.52% 不复现）。
  比较"hfq +108.42% vs raw −59.38%"说明的是**口径差异量级**，不是"策略本身变差了"——
  真实策略表现以 hfq 口径为准，且 +108.42% 与旧代码 +63.52% 同属"PEG 长期有效"区间（差异来自 ST 护栏净化候选池）。

---

## §6 一句话结论

**视频给的最值钱的东西不是结论，是那把"全收益口径"的尺子。拿它量我们自己，先量出 ±2.5%/年 的基准偏差（§5），再量出策略 NAV 漏计分红（§8），再量出最狠的一条——raw 口径回测从不处理送转股，单次凭空蒸发 17%–45% 持仓市值（§10）。按尺子修到杠杆点上（§11→§12）才发现：红利/价值策略的真实选股 alpha 一直被低估 2.0–2.8%/年——不是策略变好了，是账算对了。而量两次还不够：以为撞见第三例非确定性，跑两次是逐字节一致（§12.9 证伪）；以为告警在岗，其实它读错了键名、从未真正响过（§12.10）。这把尺子量出的最后一件事是——最该被审计的不是收益，是审计本身。**
