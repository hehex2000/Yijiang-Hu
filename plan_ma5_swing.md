# 5日均线「五句话」短线纪律策略 · 开发方案

> 来源：抖音 掌柜肖肖《一根线五句话》（评级 C 偏 B｜方法可用，标题"跑赢90%散户"无据）
> 归档：`~/.workbuddy/skills/bilibili-critical-summary/SKILL.md` §5.6
> 日期：2026-08-05　状态：**已实现 run_ma5_swing.py 并接入 bat[G]，初测通过**

---

## 〇、先定性：这是「纪律」不是「alpha」——决定了怎么测

作者自述：**"或许不能让你每一次都赚，但一定能让你在亏的时候不至于亏到爬不起来。"**

这句话是整个策略的定位声明。它意味着：

| 如果把它当 | 验收指标 | 测出来大概率 |
|---|---|---|
| ❌ 选股 alpha | 总收益 vs 基准 | 跑输（高换手摩擦） |
| ✅ 止损纪律 | **最大回撤 / 亏损尾部 / 单笔亏损分布** | 可能有效 |

**所以核心测试不是"能不能跑赢指数"，而是「轨道B 纪律隔离消融」**（见 §6）——同一批买入信号，只换退出方式，看纪律本身值不值。这是本方案最重要的设计决策，先说在前面。

---

## 一、模糊概念量化表（逐条钉死）

### 规则1｜"连一根像样的放量阳线都没有的票，直接划走"

| 子概念 | 视频原话 | 量化定义 | 默认 | 候选 |
|---|---|---|---|---|
| 阳线 | "阳线" | `close > open` | — | 可加严 `close > pre_close` |
| 像样 | "像样的" | 实体涨幅 `(close-open)/open ≥ body_min` | **5%** | 3 / 5 / 7% |
| 放量 | "放量" | `vol ≥ vol_mult × MA(vol,20)` | **2.0×** | 1.5 / 2.0 / 2.5 |
| 观察窗 | "连一根都没有" | 过去 `lookback` 日内**至少 1 根**满足 | **20日** | 10 / 20 / 60 |

**工程陷阱**
- `MA(vol,20)` 必须**排除当日**，否则大量日自己抬高基准 → 永远测不出放量。
- 停牌日 `vol=0` 要从均量样本剔除，否则均量被拉低 → 假放量。
- 一字涨停板 `open==close` 不算阳线（实体为 0），但它确实是强势信号 → 单独用 `pct_chg ≥ 9.8%` 兜底纳入（可开关 `--limit-up-as-body`）。

### 规则2｜"回踩5日线、踩住不破" = 买点

| 子概念 | 量化定义 | 默认 |
|---|---|---|
| MA5 | 5日收盘 SMA，**必须用后复权价** | — |
| 均线向上（口诀硬要求） | `MA5[t] > MA5[t-1]` | 必须 |
| 回踩（触及） | `low[t] ≤ MA5[t] × (1 + touch_tol)` | tol = **1%** |
| 踩住不破（收回） | `close[t] ≥ MA5[t]` | — |
| 启动时效 | 大阳线后 `signal_valid` 日内出现回踩才算数 | **10日** |
| **不能已经飞了** | `close/MA5 - 1 ≤ entry_dev_max` | **5%** |

> 最后一条视频没说，但**必须加**：若股价已偏离均线 8%，那是"加速中"不是"回踩"，此时进场直接吃规则3的止盈耳光。

**买点成立** = 上述全部同日满足（T 日收盘确认）→ **T+1 开盘买入**。

### 规则3｜"远远甩开5日线 → 先落袋一部分"

| 子概念 | 量化定义 | 默认 |
|---|---|---|
| 远远甩开 | `close/MA5 - 1 ≥ dev_tp` | **10%** |
| 落袋一部分 | 卖出当前持股 `tp_ratio`，100 股向下取整 | **50%** |
| 触发上限 | 每笔持仓最多触发 `tp_max` 次 | **1 次** |

> 限 1 次是防碎股：连续触发会把仓位切成 50%→25%→12.5%，最后剩几百股，佣金 5 元起收吃光。

### 规则4｜"收盘跌破5日线没站回来 → 减仓"（**用户已定**）

- 判定：`close < MA5` —— **不设跌幅阈值**（用户明确："只要收盘价低于5日均线，不用设计跌幅"）
- 动作：减仓 **50%**
- T+1 约束：若买入当天即跌破，最早**次日开盘**执行

### 规则5｜"连续三天收不回5日线 → 走人"

```
counter c:  close <  MA5  →  c += 1
            close >= MA5  →  c  = 0     # 站回即重置
            c >= 3        →  全部清仓
```

**与规则4的叠加设计**（视频没说清，需拍板）：

| 方案 | c=1 | c=2 | c=3 | 说明 |
|---|---|---|---|---|
| **A（默认·推荐）** | 减50% | 不动 | 清仓 | 防碎股，两级足够 |
| B（阶梯 `--stair`） | 减50% | 再减50% | 清仓 | 更保守，但末段仓位极碎 |

### 补充规则｜"一买就跌、一卖就涨连续两次 → 换票"

| 子概念 | 量化定义 | 默认 |
|---|---|---|
| 失败交易 | 一笔完整交易（买→全平）**净收益 < 0**（含成本） | — |
| 连续次数 | 同一 code 连续 `fail_n` 笔失败 | **2 笔** |
| 拉黑时长 | `cooldown` 个交易日内不再买入该票 | **60日** |
| 重置 | 成功一笔 → 计数清零 | — |

> 这条是视频里最实在的一句（"找到你的指标能发挥作用的战场，而不是在不配合的票上死磕"），
> 但它**天然带幸存者偏差风险**：拉黑逻辑用的是已实现结果，本质是一种事后适应。
> 必须做消融（开/关对比），若增益全来自它 → 警惕过拟合。

---

## 二、视频完全没说、但不定就跑不了的（工程必答题）

这是"还有什么问题"的正面回答。共 10 条，每条都会实质改变回测结果。

| # | 问题 | 决策 |
|---|---|---|
| 1 | **T+1** | 买入当日不可卖。信号 T 日收盘 → T+1 开盘买 → 最早 T+2 可卖。硬约束写进状态机 |
| 2 | **未来函数** | 全部信号用 T 日**收盘**判定，T+1 **开盘价**成交。禁止当日信号当日收盘价成交 |
| 3 | **涨跌停不可成交** | 买入日 `open` 一字涨停（`high==low` 且 ≥ pre_close×1.098）→ **放弃该笔**；卖出日跌停封死 → **顺延次日** |
| 4 | **停牌** | 日线缺失 = 停牌。持仓期冻结估值，复牌后 counter 继续（不清零） |
| 5 | **复权（最易翻车）** | **MA5 必须用后复权价**。用原始价的话，除权日跳空 → 假跌破 → 假清仓。复用 `mr._factor(code, td)` |
| 6 | **同时持仓上限** | `max_pos` 默认 **10**（短线不可能持 800 只） |
| 7 | **信号超额排序** | 同日信号 > 空位时，按 `放量倍数` 降序（主力力度优先）；**同分用 code 字典序兜底**——平台踩过 set 哈希非确定性的坑（净值漂移 20pp），必须确定性排序 |
| 8 | **资金分配** | 等权固定份额 `capital/max_pos`，**不用**"可用现金/剩余仓位"（避免早期运气被复利放大） |
| 9 | **准入过滤** | 剔除 ST/\*ST（`stock_basic.name` 含 ST）；上市满 `lookback+60` 日；20日均成交额 ≥ **5000万**（百万资金买得进）；剔除退市整理 |
| 10 | **基准** | 池对应指数买入持有（hs300→000300.SH，all→000985.SH），复用 `_bench_of()` |

---

## 三、🔴 成本口径：平台有两套，差 5.4 倍（必须先解决）

排查发现平台存在**两套并存的成本假设**：

| 口径 | 位置 | 买入 | 卖出 | 单次往返 |
|---|---|---|---|---|
| **完整版** | `run_monthly_rebalance.py` | 佣金 0.025%(min ¥5) + 滑点 0.1% | 佣金 0.025% + 滑点 0.1% + 印花税 0.1% | **0.35%** |
| 简化版 | `run_macd_regime.py` `BUY_MULT/SELL_MULT` | 0.02% | 0.045% | **0.065%** |

对月换手 3–5 次的短线策略，这个差异是**决定性的**：

```
完整口径年化摩擦 ≈ 0.35% × 4次/月 × 12 = 16.8%
简化口径年化摩擦 ≈ 0.065% × 4次/月 × 12 =  3.1%
```

> **决策：本策略必须用完整口径 `calc_fee()`（含 0.1% 滑点）。**
> 用简化 MULT 会把结论从"亏"美化成"赚"，属于自欺。
> 附带发现：`STAMP_DUTY_RATE = 0.001` 是**旧税率**，2023-08-28 起实际已降至 0.05%。
> 建议保持现值（与平台其他策略可比），但在报告里标注"印花税按 0.1% 计，2023-08 后偏保守"。

### 成本审计报告（用户特别要求：程序单列）

```
━━━━━━━━━━ 摩擦成本审计 ━━━━━━━━━━
交易笔数        买 N1 / 卖 N2（含减仓 N3 次）
持仓天数        中位数 X 日 / 平均 Y 日
年化换手率      Z 倍
──────────────────────────────
佣金合计        ¥xxx   (占初始资金 x.xx%)
印花税合计      ¥xxx   (x.xx%)
滑点合计        ¥xxx   (x.xx%)
成本总计        ¥xxx   (x.xx%)
──────────────────────────────
毛收益(零成本)  +xx.xx%
净收益(实际)    +xx.xx%
成本吞噬比例    成本/毛收益 = xx.x%     ← 关键数字
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**同时跑一遍零成本对照回测**（同信号、费率全设 0）：两条净值曲线之差 = 摩擦的真实代价。这是最有说服力的呈现方式。

---

## 四、状态机（每只持仓的完整生命周期）

```
   [空仓] ──规则1过滤(近20日有放量大阳线)──> [观察池]
      ↑                                          │
      │                              规则2(回踩MA5+踩住+均线向上
      │                                    +未偏离>5%)
      │                                          ↓
      │                              [T+1开盘买入] ← 受 max_pos/资金/涨停约束
      │                                          ↓
      │                                      [持仓中]
      │                                          │
      │        ┌────────────────┬────────────────┼──────────────┐
      │        ↓                ↓                ↓              ↓
      │  规则3 偏离≥10%    规则4 c=1        规则5 c>=3      (持有)
      │   减50%(限1次)     减50%             全清             │
      │        │                │                │              │
      │        └────────────────┴────────────────┘              │
      │                         ↓                               │
      └───────── [全平] ──→ 记录盈亏 ──→ 连续2笔亏? ──→ [拉黑60日]
```

---

## 五、文件与复用

**新建 `run_ma5_swing.py`**，复用现成 machinery：

| 复用 | 来源 |
|---|---|
| `_conn / _load_code / _PX / _factor` | `run_macd_regime` |
| `index_close / _metrics / _yearly / _POOL_INDEX` | `run_macd_regime` |
| `_get_pool_constituents` | `run_magic_formula` |
| `calc_fee`（**完整成本口径**） | `run_monthly_rebalance` |
| `_universe / _bench_of`（含 `all` 池支持） | `run_macd_timing` |

**架构选择（重要）**：用**组合级资金池 + 持仓上限**，**不用** `run_macd_timing` 那种"逐股子账户等权"。
理由：MACD timing 对全池每只票都开子账户（1/N 资金），但本策略有严格选股条件，绝大多数票永远不触发 → 资金被稀释到几乎全是现金（MACD timing 全样本只有 +10.72% 就是这个原因）。短线策略必须让资金**在信号之间竞争流转**。

### CLI 设计

```bash
run_ma5_swing.py START END --pool zz800 --capital 1000000 \
  --ma 5 --body-min 0.05 --vol-mult 2.0 --lookback 20 \
  --touch-tol 0.01 --entry-dev-max 0.05 --signal-valid 10 \
  --dev-tp 0.10 --tp-ratio 0.5 --cut-ratio 0.5 --exit-days 3 \
  --max-pos 10 --amount-min 50000000 \
  --fail-n 2 --cooldown 60 \
  [--stair] [--no-blacklist] [--zero-cost] [--hold-days N] [--buy-hold]
```

---

## 六、验证计划（分阶段，每阶段有 go/no-go）

| 阶段 | 内容 | 通过标准 |
|---|---|---|
| **0 冒烟** | hs300 + 2024 全年 + max_pos=10 | 不崩、有交易、成本表输出正常、无 0 交易癌症 |
| **1 基准对照** | zz800 + 2020–2026 全周期 vs 中证800 买入持有 | 出数即可（不预设） |
| **2 🔴 纪律隔离** | **同信号三种退出**：①五句话纪律 ②固定持有 N 日(10/20) ③买入后持有到期末 | 见下方判定 |
| **3 参数敏感性** | `vol_mult{1.5,2,2.5}` × `body_min{3,5,7%}` × `max_pos{5,10,20}` | 结果不能反号 |
| **4 成本消融** | 零成本 vs 完整成本 | 量化吞噬比例 |
| **5 市场状态拆解** | 按指数年度涨跌分组看表现（**仅诊断，不加过滤器**） | 回答"震荡市多惨" |

> 阶段 5 对应用户的"震荡市试错呗"——不加前置过滤，但**用数据把代价算清楚**。

---

## 七、失败判定标准（Jim 规矩③：先定，防事后找补）

| 结果 | 结论 | 后续处置 |
|---|---|---|
| 净收益 < 0 **且** 零成本版 > 0 | **成本吞噬** | 逻辑或许有效，但 A股 T+1 摩擦下不可行 → 归档不上菜单 |
| 零成本版也跑输基准 | **信号无 alpha** | 五句话不含超额信息 → 归档 |
| 收益跑输**但回撤 < 基准一半** | **纪律有效 / alpha 无效** | ✅ 抽成**退出模块**，复用给其他策略 |
| 参数敏感性反号 | **过拟合** | 不可信，归档 |
| 增益全来自"拉黑"规则 | **事后适应** | 关掉该规则重测 |

---

## 八、我的先验判断（诚实说在前面）

**大概率结果：净收益跑输基准，成本吞噬 30–60%。**

依据：
1. 5日线是极高频信号，A股 T+1 下单次进出至少 2 天，月换手 3–5 次。
2. 完整成本口径双边 0.35% → 年化摩擦 **16.8%**，需要极强的 alpha 才能覆盖。
3. **平台历史证据（`MEMORY.md`）**：迄今所有"加过滤器/加择时"的尝试——杠杆过滤、VAR 动态止损、KDJ 确认门、多因子等权打分——**无一跑赢无过滤基线**。5日线纪律先验上属同一类。

**但仍值得测**，因为：
- 测出"纪律有效但 alpha 无效"（回撤显著改善）本身就是**可复用的结论**——那样它能作为退出模块装进现有策略。
- 阶段 2 的纪律隔离消融是平台上**第一次**正面检验"止损纪律"的价值，方法论价值高于单个策略结果。

---

## 九、需要你拍板的 4 件事

| # | 问题 | 我的建议 |
|---|---|---|
| 1 | 规则4+5 叠加用 **A（c=1减50%/c=3清仓）** 还是 B（阶梯三段）？ | ✅ **A**（用户拍板） |
| 2 | 默认参数：`放量2.0× / 大阳线5% / 观察窗20日 / 持仓上限10` 是否合理？ | ✅ 按建议起步（用户拍板"按你建议"） |
| 3 | "拉黑不配合的票"要不要默认开？机制？ | ✅ **默认开，机制改为"清仓(放弃)之日起 20 交易日不再入选"**（用户拍板，非原方案"连续2笔亏冷却60日"） |
| 4 | 回测区间与池？ | ✅ **先 hs300 尽快出可测版**（用户：zz800/小盘可能更好，留待后续） |

拍板后已实现 `run_ma5_swing.py` + bat `[G]`，阶段0冒烟通过。初测（hs300 2020-2026）：
完整成本 +17.04% vs 基准 +19.92%（−2.88%）；**零成本 +63.92%**（信号有 alpha+熊市抗跌），
成本吞噬 ~47pp（滑点28%+印花11%）。结论：纪律有效但 A股高换手成本吃掉 alpha。
详见下方"实现备注"。

---

## 十、实现备注（run_ma5_swing.py · 2026-08-05）

**架构**：组合级资金池 + 持仓上限 `max_pos`(默认10)；信号间资金竞争流转（非逐股子账户）。
MA5 用**绝对后复权**（`raw × adj_factor`，两数组同尺度，且消除除权跳空假跌破）。
T+1：T 日收盘判定 → T+1 开盘成交；涨停买不进/跌停卖不出顺延。完整成本 `calc_fee`（含分段印花税）。

**决策3 黑名单落地方式**：规则4+5 的 A 方案清仓（c≥3 或止盈后末次）即"放弃"，
`blacklist_until[code] = 清仓日全局索引 + 20`，期间不再入选。默认开（`--no-blacklist` 关）。

**踩坑修复**：
1. MA5 滚动窗口算错（原 `cumsum` 错位成 4 元素/5）→ 改用 `np.convolve(hc, ones(5)/5, 'valid')`；
   `amount20` 同理修正。修复前 MA5 永远滞后 5 天 → 0 交易。
2. 买入排序 lambda 对停牌日 `idx_of[td]` 缺失 KeyError → 改 `.get(td)` 安全访问，当日无数据者排末尾并被跳过。

**复用**：`run_macd_regime`(_conn/_load_code/_PX/_factor/index_close/_metrics/_yearly/_POOL_INDEX)、
`run_magic_formula`(_get_pool_constituents)、`run_monthly_rebalance`(calc_fee)。

**后续待做（计划阶段1-5）**：zz800/小盘对照、纪律隔离消融（同信号换退出方式）、参数敏感性扫描、市场状态拆解。
