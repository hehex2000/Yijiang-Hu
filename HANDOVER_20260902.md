# 交接文档 —— 2026-09-02 收盘 → 2026-09-03 续作

> 写于 2026-09-02 17:48。今天完成了 22 条视频规律的全部 8 条可测项（0 落地），并回到主线①落地了 B7 缓冲规则代码、A/B 回测跑到一半。明天从这里接。

---

## 一、今天做了什么（全部已 commit，本地未 push）

| commit | 内容 |
|---|---|
| `351e318` | ⑳高股息熊市超额：规律真、择时落地失败（恒持红利更优） |
| `5e4d924` | 22条可测性盘点 `video_22rules_feasibility.md`（判定×状态×结论总表置顶） |
| `49aad0b` | ann_date/pit_ann 防御性修复（SQL 侧虚惊已证伪） |
| `0e6385a` | 台账重构：结论先行 |
| `49e5790` | 补宏观三件套 `cn_cpi/cn_ppi/cn_m`（下载脚本 `download_macro_cn.py`） |
| `14f0998` | ⑩20日新高占比：不落地（阈值 82% 天数满足；双向 40/40 全负；2012 盲区第三例） |
| `3c3cc9d` | ④M1/⑲PPI-CPI：双双不落地（M1 拐点中位 +1 月滞后；overlay 12/12 输恒持） |
| `963540d` | ㉑必选vs可选：不落地（2021-24 反转=时代风格；防御被红利支配 +75.7pp） |

**22 条总收敛**：8 条可测全测毕，0 条择时落地。所有"防御"类规律收敛到同一答案：**恒持红利（000922/930955）**——与主线①完全咬合。

---

## 二、当前进行中：主线① B7 缓冲规则 A/B（🔴 明天第一件事）

### 已完成的代码改动（⚠️ 未 commit）

`run_dividend_low_vol_quality_bt.py`：
- `select_targets_official(..., buffer_k=0)`：指数编制法 rank buffer——上期持仓在新一期排名仍 ≤ `top_n + buffer_k` 名内则保留，空位按排名递补，行业 cap 在递补时同步执行。
- `run_official_backtest(..., buffer_k=0)` 透传；partial 文件名带 `_bk{k}` 防串档。
- **CLI**：`--buffer-k`（默认 0=关闭，opt-in，不静默改默认）。
- 选股日志每期打印 `[buffer] 保留X 递补Y` 诊断。

`divlow_turnover_ab.py`（新建，未 commit）：从各 `_bk{k}` partial 文件读持仓序列，算调仓换手率，输出 A/B 四档对照表。

### 🔴 后台 A/B 任务状态（明天先检查）

启动的命令（4 档顺序跑，单档约 8-10 分钟，共 30-40 分钟）：

```bash
cd /c/Users/99395/WorkBuddy/multi_factor_selection
for k in 0 6 12 18; do
  venv_ml/Scripts/python.exe run_dividend_low_vol_quality_bt.py \
    --mode official_compact --pool all --top-n 12 --price-mode hfq \
    --no-div-channel-overlay --buffer-k $k
done > data/results/dividend_low_vol/_buffer_ab_run.log 2>&1
```

**17:51 已按用户指示杀掉后台进程**（TaskStop + 确认 0 个 python 存活）。现场残留：k=0 档 partial 只写了 8 期（6617 字节）即中断，日志 16.5KB。**明天直接重跑上面命令**——done_set 断点续跑只对已完整选股的期生效，k=0 会从头重跑（partial 会被覆盖，无脏数据风险）。

**明天接手步骤**：
1. `ls data/results/dividend_low_vol/ | grep bk` —— 若 4 个 partial（bk0/6/12/18）齐全则直接进第 3 步。
2. 不齐就重跑上面命令（日志会从 done_set 断点续跑各档）。
3. 跑 `venv_ml/Scripts/python.exe divlow_turnover_ab.py` 出换手对照表。
4. 从各档日志尾部抓 NAV 指标（总收益/年化/回撤/夏普），拼四档对比表。

### A/B 判定标准（B5 目标：换手 72.3% → ~45%）

| 看什么 | 标准 |
|---|---|
| 年化收益 | 相对 k=0 基线损失 ≤ 0.5pp/年 可接受（缓冲是正统降换手手段，小幅收益换大幅换手是划算的） |
| 换手率 | k=6/12/18 哪档落在 ~45% 附近 |
| 回撤 | 不应劣化 |
| **终判** | 选中 k 后必须跑**含 overlay 的完整档**复核（今天 A/B 全关了 overlay 隔离选股层变量） |

⚠️ **坑（今天已踩）**：日志是全缓冲的，tail 看不到实时进度——用 `grep -c "^\[select\]" 日志` 数选股次数、看 partial 文件 mtime 判断在不在跑。全A 预载每进程重复 ~3-4 分钟、占 1.7-2.9GB 内存，正常。

### B7 之后的队列（主线①剩余）

1. **B5 收尾**：选定 k 档 → 更新 `README_strategy.md`（`data/results/dividend_low_vol/`）的换手指标 → commit。
2. **P1 ETF 溢价保护**：尚未动工。思路：实盘执行层用 512890.SH/515080.SH 等 ETF 时加溢价率 filter（`etf_daily` 有数据），回测层暂不阻塞。
3. B7 缓冲规则若 k 档收益损失 >0.5pp，回头检讨 buffer 拓扑（如"只保 rank 不保 weight"变体）。

---

## 三、数据资产增量（今天落库）

- `cn_cpi`（1951-12~2026-07，511 行）、`cn_ppi`（1978-12~，418 行）、`cn_m`（1978-01~，583 行），month 0 重复，2010 后各 199 个月，口径抽查全吻合。
- 🔴 接口名是 **`cn_m`** 不是 `cn_money_supply`；数值列 TEXT 存储 → `pd.to_numeric` 强转。
- 🔴 **PIT 红线**：月度宏观发布滞后约次月中旬 → 信号在 month+1 月 15 日后才可用。

## 四、今天踩的坑（已记日志，防复发）

1. `sw_industry_daily.trade_date` INTEGER → 必须 `astype(str)`。
2. 🔴 基准 close 是绝对点位 → perf 必须 `nav/nav.iloc[0]` 归一化，否则年化出 +74.9% 荒谬值。
3. `(1+r).cumprod()` 首行 NaN 传播 → `fillna(0)`。
4. `bench_index.load_benchmark` 必须传完整 ts_code（`'000906.SH'`），裸码静默返回 None。
5. `net_break_screen.py` 忘传 `--signal-csv` 会**静默回退默认破净率源**（⑩第一轮网格白跑）。

## 五、未 commit 清单（明天收尾时一起批 commit）

- `run_dividend_low_vol_quality_bt.py`（B7 buffer 改动）
- `divlow_turnover_ab.py`
- A/B 产出与日志（`data/results/dividend_low_vol/_bk*` partial + `_buffer_ab_run.log`，若被 gitignore 则不硬塞，脚本可重建）

## 六、更远的队列（未开工，备忘）

- 🟡 代理类 6 条（①②⑨⑮⑯⑰）：口径链长，优先级最低。
- 补数据性价比榜：🥇 `margin`（⑥两融，需 key 权限）🥈 10Y 国债收益率（⑨正口径）🥉 `moneyflow_hsgt`/`stk_holdernumber`/`repurchase`。
- 主线其余：⑦ETF轮动V6抗过拟合、③申万31行业轮动 head-to-head、④multi_ic A/B 定位、⑤MACD插件 vs 止损层对照、⑧VP 因子 commit。
