# 缠论笔和线段 → 量化翻译与计划补充

> 续 `plan_zhongshu_consolidation.md`（中枢篇）。
> 用户 2026-07-29 在中枢篇后补充缠论第二讲（笔和线段）科普，要求「补充缠论量化内容，生成新 md」。
> 本篇处理缠论"地基的地基"：分型 / 笔 / 线段，及其机器化路线。

---

## 0. 一句话结论

缠论「笔 + 线段」在量化里**就是 Zigzag 指标 + 摆动结构（HH-HL 链）**，完全可机器实现；但**阈值即玄学**（作者亲承定义吵架、老笔新笔各立山头），所以只能当**确认层**，不能当唯一信号。

本篇把原计划 §6 的「备选 C（中枢方向替代 MA200）」具体化——**它就是缠论的"线段"**；并新增「笔作入场细化」作为可选层。

---

## 1. 概念 → 量化映射（完整四层）

| 缠论层 | 几何含义 | 量化等价 | 机器可实现 |
|---|---|---|---|
| K 线（一地碎石） | 原始价格 | OHLC 序列 | — |
| 分型（顶/底候选） | 3 根 K 线中间最高/最低 | **Fractal 分形**（N 根两侧极值） | ✅ 平凡 |
| 笔（一步） | 一坡底→一坡顶，不回头，需走出距离 | **Zigzag 腿**（阈值过滤的微拐连接） | ✅ 阈值=唯一旋钮 |
| 线段（一程路） | 至少三笔，回头不破关键位 | **摆动结构 / Swing structure**（HH-HL 或 LH-LL 链） | ✅ |
| 中枢（驿站） | 三段重叠 | **区间 / 波动率收缩**（见主计划 A） | ✅ |

> 关键认知：缠论整套 = 一个**多尺度分形分解**。量化对应 = Fractal → Zigzag(笔) → Swing structure(线段) → Range(中枢)。四层全可代码实现，争议只在「阈值取多少」。

---

## 2. 三层的具体实现

### 2.1 分型（Fractal）—— 平凡，作积木

```
is_top[i]    = high[i] > high[i-1] and high[i] > high[i+1]   # 顶分型
is_bottom[i] = low[i]  < low[i-1]  and low[i]  < low[i+1]    # 底分型
```

（可加 2-bar 间距要求，过滤贴脸噪声；Bill Williams Fractal 即用此定义。）

### 2.2 笔（Zigzag leg）—— 阈值是唯一旋钮

- 取交替分型，要求相邻拐点**价格变动 ≥ 阈值**（如 5% 或 `k×ATR`），否则合并/忽略（"贴脸不算笔"）。
- 这就是标准 **Zigzag 指标**。阈值网格：`{3%, 5%, 8%, k×ATR}`。
- 用途：**入场时机细化**（顺趋势的向上笔确认 → 上车点），**不宜**作独立买卖信号（"被一笔吓下车"）。

### 2.3 线段（Swing structure）—— 即原计划的「备选 C」

- 由笔链出**更高高点 + 更高低点（上涨线段）**或**更低低点 + 更高高点（下跌线段）**。
- 这正是原计划 §6-C「中枢方向替代 MA200」的**精确量化形态**：用结构而非单 MA 判定"客栈上移/下移"。
- 比 MA200 更贴缠论原意，且对"前进两步退半步"的容忍天然内建（回撤不破前低 = 线段延续）。

---

## 3. 对原计划（中枢篇）的补充与升级

- **主计划 A（consolidation filter / 中枢）**：不变。它用"中枢 = 布林带宽低位"的波动率视角，参数轻、鲁棒，仍优先落地。
- **备选 C 升级为具体方案**：线段 = Zigzag swing structure（HH-HL / LH-LL 链），作为 v2 可选趋势腿，替代或并列 MA200。作者承认"线段划分千人千笔" → 我们用**结构规则**（不破前低=延续）而非主观画，参数走 ATR 网格。
- **新增 P2：笔作入场细化**：在趋势腿（MA200 或线段）看多时，仅当价格处于"向上笔"（Zigzag 自最近底分型回升）才允许建仓，避免在向下笔末端接飞刀。对应缠论"看线段定方向，看笔找上车点"。
- **分型**：作为以上两层的积木，不单独成模块。

---

## 4. 作者诚实劝退的量化启示（很重要）

1. **"笔定义吵架（老笔/新笔）"** → 量化含义：**阈值不可证伪**。对策：对所有阈值做网格搜索，报告最佳/最差区间，**不宣称某阈值"正确"**。
2. **"软件自动画不可靠，不同软件不一样"** → 量化含义：Fractal/Zigzag 输出对参数敏感。对策：当**信号层**而非**真理层**，必须叠量能/ATR（作者本人也建议结合布林、ATR、成交量）。
3. **"被一笔吓下车"** → 量化含义：**笔太灵敏**，单独用作信号会过度交易。对策：方向看线段（结构）、时机看笔，二者分离——对应"趋势腿(线段/MA200) + 入场细化(笔)"的分层设计。

---

## 5. 落地优先级建议

| 模块 | 对应缠论 | 优先级 | 备注 |
|---|---|---|---|
| A 中枢回避滤镜 | 中枢 | **P0（先建）** | 主计划已详，参数轻 |
| C 线段趋势腿 | 线段 | P1 | 替代/并列 MA200，ATR 网格 |
| D 笔入场细化 | 笔 | P2 | 仅趋势看多时过滤建仓点 |
| 分型 | 分型 | 内置 | 不作独立模块 |

> 这与主计划 §0「笔/段不碰」**不矛盾**：我们**不手工画笔段**，而是用**规则化的 Zigzag + 摆动结构**实现其"精神"，参数透明、可回测。主观画线才是不碰的。

---

## 6. 决策记录

- 2026-07-29（续）：用户补充笔和线段科普（缠论第二讲），要求补充量化内容、生成新 md。
- 本篇将"笔/线段"明确映射为 Zigzag + 摆动结构，将主计划 §6 备选 C 具体化为"线段趋势腿"，新增 P2「笔入场细化」。
- 结论强化：缠论四层全可量化，但阈值即玄学 → 只能作确认层 + 网格搜索，不宣称正确性。
- 与原计划关系：本篇是 `plan_zhongshu_consolidation.md` 的续篇，落地顺序 A → C → D。
