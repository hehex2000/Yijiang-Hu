# B7' 判死 + B8 立项：换手的真杠杆是「最后一段筛子的排序键」

> 生成时间：2026-09-03　窗口：2020-01-08 起（统一窗口）　标的池：全A　持仓 12　`official_compact`
> 上游：`divlow_b5_rebal_demystify.md`（降频三档）· `divlow_b7_demystify.md`（缓冲规则 B7 判死）

---

## 零、一句话结论

**B7'（每次调整 ≤20% 硬上限）代码是对的，但打错了靶子：换手只从 142.3% 降到 83.5%（半年档），
年度档甚至从 52.7% 反弹到 58.0%。**

核对官方编制方案后找到两处更致命的偏差：

| # | 偏差 | 后果 |
|---|---|---|
| 🔴 ① | **对标错了指数**：全程拿 H30269（红利低波 **50**，**年度**调仓、**有** ≤20% 换手上限、股息率加权）的规则，去复制 **930955（红利低波 100，100 只、季度调仓、无换手上限、股息率/波动率加权）** | "降频对齐官方""加 20% 硬上限"两条建议的前提**都不存在** |
| 🔴 ② | **最后一段筛子的排序键与官方相反**：官方是「股息率前 300 → **波动率升序**取前 100」（波动率 = 慢变量）；我们在低波候选池上又按 **fwd_yield 降序**取前 12（股息率 = 快变量） | **这才是换手高的真正根因** |

→ 换手的真杠杆是 **B8：把最后一段排序键换成波动率**（官方口径），不是降频、也不是硬上限。

---

## 一、B7' 做了什么

`run_dividend_low_vol_quality_bt.py` 新增 `--turnover-cap`（opt-in，默认 0 = 旧行为）：
以**上期持仓**为起点，在「新进只数 ≤ `int(top_n × cap)`」的预算内把组合往本期最优改进。

```
① 老持仓（仍在本期候选池内）按 rank 保留，受行业上限 ind_cap=2 约束；
② 老持仓坐不满 → 强制递补（被动换仓，无法避免，计入新进）；
③ 剩余预算内做改善型替换：最好的候补 ↔ 应被踢出者
   （目标行业已满 → 踢同行业最差；否则踢全局最差），换不出改善即停；
④ 恒真：持仓 = top_n、行业 ≤ ind_cap；每笔替换严格降低 rank 之和 → 必然收敛。
```

### 🔴 两种已自证判死的实现错法（勿回退）

**(a)「全量重排 → 砍新进者 → 回补老持仓」**
砍掉的新进者要回补老持仓，但老持仓大量是银行股，被 `ind_cap=2` 挡住 → **组合从 12 只塌缩到 5 只**
（2023 冒烟实测 20230109→20230410→20230707→20231013 = 12→11→**5**→6）。

**(b) 拿 `select_stocks` 的 score 序当 rank**
🔴 裸档最终排序是 **fwd_yield**（`_cap_industry` 内部 `sort_values("fwd_yield", ascending=False)` 重排），
不是 `select_stocks` 的 score 序。拿 score 序当 rank → 连**首期**（无老持仓）都选出完全不同的 12 只。
另外候选池必须用**行业上限前**的全量 48 只（`_cap_industry` 会把池子砍到 26 只 →
老持仓大量掉出池外、被动换仓激增）。

---

## 二、自证：代码正确性（硬闸门）

冒烟脚本 `divlow_b7p_smoke.py`（2023 短窗口 4 期，避免浪费 24min 的全窗口跑批）：

| 判据 | 类型 | 结果 |
|---|---|---|
| ① `cap=1.0`（等价不限）与 `cap=0` **md5 相同** | 🔴 **硬闸门**（行为中性） | ✅ `5769aed2cd4f1667d9ca02536e18d30a` 两侧一致 |
| ② 持仓只数恒 = 12 | 🔴 **硬闸门** | ✅ |
| ③ 每期新进 ≤ 2 | 🟡 **观测项**（超预算是策略结构导致，不是代码 bug） | ⚠️ 3/3 期超预算 |

> ①③ 任一不过 = cap 分支写错，绝不能拿去跑全窗口。② 已降级为观测项：
> 见下节，超预算本身**就是本轮的结论**。

---

## 三、五档统一窗口实测（`divlow_rebal_fair_cmp.py`，窗口 20200108 起 / 1583 交易日）

| 档位 | 期数 | 总收益 | 年化 | 最大回撤 | 波动 | 夏普 | 卡玛 | 年化单边 | 每期双边 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 季度（基线） | 26 | 51.08% | 6.52% | −12.61% | 13.31% | 0.34 | 0.52 | **142.3%** | 71.1% |
| 半年 | 13 | 47.05% | 6.08% | −17.31% | 14.02% | 0.29 | 0.35 | 98.0% | 96.7% |
| 年度 | 6 | 82.49% | **9.64%** | −17.36% | 14.56% | **0.52** | 0.56 | 52.7% | 104.0% |
| 半年 + 20% 上限 | 13 | 52.33% | 6.65% | −19.17% | 13.90% | 0.33 | 0.35 | 83.5% | 82.4% |
| 年度 + 20% 上限 | 6 | 32.91% | 4.45% | **−13.83%** | 13.06% | 0.19 | 0.32 | 58.0% | 114.4% |

**配对检验 vs 季度基线（日收益，n=1583）**：半年 t=−0.13 ／ 年度 t=+0.82 ／ 半年+20% t=+0.08 ／ 年度+20% t=−0.87
→ **|t| 全部 < 0.9，收益差异全判噪声**。

⚠️ 口径提醒：本表统一窗口起点是 **20200108**（引擎对低频档插入了期初建仓日），
季度档年化因此是 6.52%；上一轮 B5' 报告的 8.77% 是 **2020-12-14** 起口径。**两轮数字不可混用**。

**读法**：
- 年度裸档年化 9.64% / 夏普 0.52 最高，但那是 6 期样本 + 2020-2025 红利窗口，**不显著**（t=+0.82）；
- 年度+20% 年化 4.45% 五档最差、回撤 −13.83% 五档最小 → 典型**减震器**特征（把组合锁死在过期持仓上），不是 alpha；
- 🔴 **硬上限在年度档反而把换手推高**（52.7% → 58.0%）：上一期被硬留下来的边缘标的，
  这一期掉出候选池后被**强制**换掉，制造了额外的被动换手。

---

## 四、为什么压不住：结构性换手的下界

年度 + 20% 档 6 期新进只数 = **5 / 7 / 8 / 7 / 7 / 6**（上限 2），半年档 **13/13 期全部超预算**（新进 3~7）。
超预算时改善型替换（步骤③）已失效 → 新进量 ≈ **上期持仓中掉出本期候选池的只数**。

→ **年度间隔**下，上期 12 只里平均只有 5.3 只仍落在本期 48 只合格池内（44%），结构性换手 ≈ 56%/期。

这个换手是**不可压缩**的：标的掉出候选池就必须卖，任何调整比例上限都只能靠"留下边缘标的"
把换手往后推一期，推不动总量。

⚠️ **口径提醒**：上面这组数是**年度**间隔（间隔 12 个月）。同一口径在**季度**间隔下只有 22%
（见 §七：上期持仓仍在本期池内 11/7/10，平均 9.3 只 = 78%）—— 间隔越长，池子漂移越大。

各档每期平均留存（直接从 partial 交集算）：

| 档位 | 平均留存率 |
|---|---:|
| 季度裸档 | 64.4% |
| 半年裸档 | 51.9% → 半年+20% **61.5%** |
| 年度裸档 | 50.0% → 年度+20% **44.4%**（反而更差） |

---

## 五、🔴 规则书核对：我们一直在对标错误的指数

| 维度 | **930955 红利低波 100**（我们的复制目标） | H30269 红利低波（B5'/B7' 全程引用的） | 我们的实现 |
|---|---|---|---|
| 成分数 | **100** | 50 | 12 |
| 调仓频率 | **每季度**（3/6/9/12 月第二个周五下一交易日） | 每年 1 次（12 月） | 季度 ✅（与 930955 一致） |
| 换手上限 | **无** | 有（每次调整 ≤20%） | 无 → **B7' 加的是 H30269 的规则** |
| 加权 | **股息率 ÷ 波动率** | 股息率 | 股息率（=H30269 口径） |
| 行业约束 | **中证二级行业权重 ≤20%** | 单样本权重 ≤15% | 单行业最多 2 只（两个都不是） |
| 选样 | 成交额剔除后 20% → 连续 3 年分红且股息率 >0 → **股息率前 300** → **波动率升序前 100** | 市值/成交额前 80% → 剔支付率前 5%/增长非正 → 股息率前 **75** → 波动率前 **50** | 门禁 → 均息前 **75%** → 低波前 **50%** → 低波前 **48** → 🔴**按股息率取 12** |
| 股息率口径 | 3 年分红总额/3 ÷ 市值 | 3 年平均税后股息率 | `fwd_yield` / `dv_ttm` |

**结论**：B5' 的 🥇「降频到年度对齐官方」和 B7' 的「加 ≤20% 硬上限」，
**都是把 H30269 的规则往 930955 上套**。930955 本来就季度调仓、且没有任何换手上限 —— 这两条建议的前提不成立。
（数字本身仍然有效：它们是有意义的策略实验，只是"对齐官方"的名义要摘掉。）

---

## 六、🔴 更要命的一处：最后一段筛子的排序键反了

**官方 930955**：`股息率前 300` → **`波动率升序` 取前 100**　→ 最后一段排序键 = **波动率**
**我们的实现**：`低波前 48`（候选池）→ `_cap_industry()` 内部 **`fwd_yield` 降序** → 取前 12　→ 最后一段排序键 = **股息率**

```
官方：宽池（高息 300） ──[波动率 慢变量]──> 100 只    留存率高、换手低
我们：窄池（低波 48）  ──[股息率 快变量]──> 12 只     留存率低、换手高
```

**为什么这个差异是决定性的**：
- **波动率是慢变量** —— 一年的日收益标准差，年度之间高度自相关。今年低波动的股票，明年大概率仍低波动。
- **股息率是快变量** —— 分子（分红）每年变、分母（市值/价格）每天变。股息率排名的逐期漂移远大于波动率。
- 用快变量做**最后一段**筛子，等于每期都在用一个噪声最大的键重排 → 留存率天然低。

已实现 opt-in 开关 `--final-key {fwd_yield,volatility}`（默认 `fwd_yield` = 旧行为，产物标签 `_kv`）：
`_cap_industry(df, cap, sort_key)`，`volatility` 时按波动率**升序**取前 cap 并重排。

---

## 七、🔴 B8 被证伪：换排序键**没有用**（2023 短窗口 4 期实测）

诊断脚本 `divlow_b8_key_smoke.py`：包一层 `_cap_industry` 把每期**行业 cap 前的 48 只候选池**录下来，
跑一趟选股同时复算两种键（选股占 95% 耗时，不跑两遍）。自证：录得的池子复算出的 yield 键持仓
与实际跑出的**逐位一致** ✅。

```
【一、候选池（48 只）自身逐期留存】
  池∩上期池        : [41, 31, 30]   平均留存率 = 70.8%
  上期持仓仍在本期池: [11, 7, 10]   ← 结构性换手下界只有 22%（不是 56%，那是年度间隔）

【二、两种排序键的逐期新进（持仓 12）】
  日期          yield键新进   vol键新进   两键重合
  20230109            12         12          4
  20230410             6          5          3
  20230707             6          6          4
  20231013             3          4          4
  平均                5.00       5.00
  → yield 键平均留存率 58.3% ／ vol 键平均留存率 58.3%   （差 +0.0%）
```

**两条假设同时被证伪**：
1. ❌「池子每期翻转极大」—— 池自身留存 **70.8%**，上期持仓仍有 **78%** 在本期池内，池子其实相当稳；
2. ❌「排序键换成波动率（慢变量）能压换手」—— **两键留存率完全相同**。
   注意两键选出的组合**差异极大**（每期只重合 3~4/12），却得到一模一样的留存率
   → **留存率不是由排序键决定的，而是由下游结构决定的。**

### 真正的嫌疑：行业约束的**形式**

日志里一直有个被忽略的信号：候选池 48 只 → `_cap_industry(ind_cap=2)` 之后只剩 **27~31 只**，
**每期砍掉 35~44%**，而"砍掉谁"每期都在变（48 只里银行扎堆，只留 2 只 → 是哪 2 只高度不稳定）。

| | 约束形式 | 超限时怎么办 |
|---|---|---|
| 官方 930955 | **中证二级行业权重 ≤20%** | **调权重**（权重因子缩到 0~1 之间）→ **不必换股** |
| 我们的实现 | **每行业最多 2 只** | **踢股票** → 每期强制换手 |

→ **B9：把"每行业最多 2 只"换成"行业权重 ≤20%"**（顺带对齐官方）。
候选池已落盘，这个实验是**离线秒算**的（`divlow_b9_pool_lab.py`，见 §八）。

---

## 八、下一步

<!--NEXT_STEPS-->
