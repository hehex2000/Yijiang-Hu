# MACD 背离诊断器：开发计划

> 状态：计划阶段（用户 2026-07-31 要求编写；对应 MACD 科普视频的"元方法论 = 对照实验验证指标"）
> 性质：**纯研究诊断脚本，不修改任何现有策略**。目标不是"用 MACD 交易"，而是回答"MACD 顶/底背离在 A 股到底有没有 edge"。

---

## 0. 结论先置（TL;DR）

- 视频最值钱的一句话：**"把全部符合条件的背离找出来，按趋势/震荡分组，观察背离后 5/10/20 天，加交易摩擦，用相近背景的对照组比较"**——这就是本诊断器的全部设计。
- 本工具 = 一个**指标验证器**：扫描全市场 MACD 背离 → 分型（趋势/震荡）+ 看前向收益 + 对照组相减 → 输出"有无 edge、在哪类状态下有"。
- 前置已确认：`daily` 表含 `high/low`（背离极点必需），`talib 0.6.8` 可用，hfq 缓存与 MA200 状态判定可直接复用 `run_magic_v2.py` 的模式。
- 下游：若证有 edge → 才考虑接入 `TF2_MACD`（regime-gated，P1）或重做 MACD 策略（P2，**绝不用金叉死叉当按钮**）；若证无 → 记档，归档插件保持归档。

---

## 1. 背景与目标

视频反驳"金叉就买、死叉就卖、看到背离就猜顶底"的散户思维，给出正确姿态：
- 背离 = **"还在跑，但没起跑时那么有劲了"**，是动力衰减提醒，**不是反转按钮**。
- 背离后三件事：① 固定周期 ② 配合价格结构 ③ **趋势和震荡分开看**。
- 元方法论（本工具核心）：找**全部**背离 → 按状态分组 → 看 5/10/20 天 → **加摩擦** → **对照组**比较。

**本工具目标**：用上述方法，在 A 股实证 MACD 背离是否含可量化 edge，并按状态拆解（避免"整体无效但某状态有效"被平均掉）。

---

## 2. 平台现状盘点（实测）

| 现有 MACD 用法 | 位置 | 与视频的关系 |
|---|---|---|
| MACD+RSI 金叉买/死叉卖 | `backtest/_archive/macd_kdj_plugin.py` | **正是视频批评的误区**，已归档（正确，不复活） |
| `TF2_MACD` = MACD 原始值正向因子 | `src/factor_calculator.py` | 比金叉好，但"裸值当按钮"，未分状态 |
| MACD 背离逻辑 | 全平台**无** | **本工具要补的缺口** |

**可复用基础设施**（来自 `run_magic_v2.py` 与 `src/factor_calculator.py`）：
- `hfq_price(code, td, kind)` 的 bisect 缓存 + `adj_factor` 后向填充口径 → 直接复刻做背离的前向收益。
- `index_above_ma()` / `_stock_above_ma(code, td, 200)` → 趋势状态判定（复用）。
- `mf._get_pool_constituents(pool, td)` → **时点成分**（避免幸存者偏差）。
- `talib.MACD(close, 12, 26, 9)` → 指标计算。
- DB：`D:/tu-shareData/astock_daily.db`；`daily(ts_code, trade_date, open, high, low, close, ...)`。

---

## 3. 设计

### 3.1 背离检测（顶 / 底）

- 对每只股票，在 hfq `close` 上算 MACD（默认 12/26/9），取 `DIF` 与 `MACD Hist`。
- **摆动极点（pivot）**：以 `pivot_window`（默认 10）为半窗，`high[i]` 为 `[i-W, i+W]` 最大 → 顶极点；`low[i]` 最小 → 底极点。
- **顶背离（ bearish ）**：最近两个顶极点 `P2 > P1`（价格更高），但对应 `DIF2 < DIF1`（MACD 没新高）→ 价升量（动能）衰减。
- **底背离（ bullish ）**：最近两个底极点 `L2 < L1`（价格更低），但 `DIF2 > DIF1`（MACD 没新低）→ 动能改善。
- 可选收紧：要求 `P2` 是 `lookback`（默认 60）日价格新高/新低，降低噪声。
- **去重**：每股票每次信号间隔 ≥ `min_gap`（默认 10 交易日），避免连续重复触发。

### 3.2 前向收益（含摩擦）

- 在背离日 `T`，用 hfq 算前向 `T+K` 收益，`K ∈ {5, 10, 20}` 交易日。
- **毛收益**（gross）与**净收益**（net = gross − 往返摩擦 `cost`，默认 0.2%，对齐平台 `BUY_MULT/SELL_MULT`+印花税口径）。
- 顶背离预期净收益 **负**（动能衰减→走弱）；底背离预期 **正**。

### 3.3 状态分组（视频要点③：趋势/震荡分开看）

每个背离事件在 `T` 打状态标签，分桶报告：
- **趋势**：`_stock_above_ma(code, T, 200)` → 上升 / 下降。
- **震荡（中枢期）**：20 日布林带宽滚动分位 < 25%（**复用计划 `plan_zhongshu_consolidation.md` 的中枢判定**）→ 中枢期 / 非中枢期。
- 最终桶：`上升趋势 / 下降趋势 / 中枢期`（三桶，互斥优先级：中枢期优先于趋势，因盘整中背离最易失效）。

### 3.4 对照组（视频要点④：相近背景比较）

- 构建"非背离"样本池：所有（股票, 交易日）中**不满足背离条件**且处于同一 `状态桶 + 同月` 的点。
- 对每个背离事件，匹配同桶、日期就近（±30 日）的非背离样本，取其前向收益均值作为该事件的**对照收益**。
- 报告指标 = `背离组前向收益 − 对照组前向收益`（即"背离带来的增量 edge"），而非孤立看背离组。

### 3.5 输出与判定

- **逐事件 CSV**：`data/results/macd_study/macd_div_events_<pool>_<start>_<end>.csv`（code, T, type, regime, fwd5/10/20 gross/net, ctrl_fwd...）。
- **汇总表**（命令行 + CSV）：按 `类型 × 状态桶`：
  - `n`（事件数）、`avg_fwd5/10/20 gross`、`avg_net`、`win_rate`、`vs_control_diff`、`t 值`（粗略）。
- **结论判定**：
  - 某桶 `vs_control_diff` 在 5/10/20 天持续为正（底背离）或为负（顶背离）且 `n` 充足（≥50）、`t` 显著 → **该状态有 edge**。
  - 否则 → **无显著 edge**，记档。

---

## 4. 接线（CLI，镜像已有脚本风格）

```
venv_ml/Scripts/python.exe macd_divergence_study.py --pool hs300 \
    --start 20140301 --end 20260731 \
    --macd-fast 12 --macd-slow 26 --macd-signal 9 \
    --pivot-window 10 --lookback 60 --min-gap 10 \
    --fwd-days 5,10,20 --cost 0.002 \
    --regime-bb-win 20 --regime-bb-th 0.25
```
- 输出目录 `data/results/macd_study/`，**不触碰任何现有策略/结果**。

---

## 5. 回测验证方案（本工具自身的"自测"）

- 默认跑 `hs300` + `2014–2026`（时点成分，避免幸存者偏差）。
- **健全性检查（sanity）**：随机打乱 MACD 序列后再跑一遍背离检测，增量 edge 应≈0；若仍显著，说明检测逻辑有前视 bug。
- **参数网格**：`pivot_window ∈ {8,10,15}`、`lookback ∈ {40,60}` 各跑，看结论是否稳健（不被单一参数绑架，呼应视频"阈值即玄学"）。
- **对比池**：再跑 `zz500` 验证结论跨池一致性。

---

## 6. 风险与已知坑

- **幸存者偏差**：必须用 `mf._get_pool_constituents(T)` 时点成分；`all` 池含退市股会污染 → 默认 hs300/zz500，all 仅作可选。
- **A 股向上漂移**：长期多头漂移会让"底背离后涨"显得有效 → **对照组是必需品**，不可省略。
- **重叠信号**：未去重会虚增 n → 强制 `min_gap`。
- **极点窗敏感**：`pivot_window` 太小噪声大、太大漏信号 → 走网格。
- **边缘样本**：上市 < 200 日（MA 不足）的票剔除趋势桶判定。`_stock_above_ma` 已对数据不足返回 None，需把这些事件归入"未知"不计入分母。
- **可能结论为负**：A 股 MACD 背离大概率"看着漂亮但 edge 微弱/负"（与 `--breadth-confirm` 教训一致）→ 预期管理：本工具价值在**证伪/证实**，不在证明有效。

---

## 7. 实施步骤（任务拆解）

1. 新建 `macd_divergence_study.py`，复刻 `hfq_price` + `adj_factor` 缓存（从 `run_magic_v2.py` 搬）。
2. MACD 计算：`talib.MACD`（fallback numpy EMA）。
3. 摆动极点检测器（pivot high/low）。
4. 背离分类器（顶/底 + 去重）。
5. 前向收益引擎（hfq，5/10/20，gross/net）。
6. 状态分类器（MA200 趋势 + BB 带宽中枢期，复用计划口径）。
7. 对照组匹配 + 聚合（按 类型×状态桶）。
8. 报告 + 双 CSV 落盘。
9. 跑默认（hs300, 2014–2026）+ sanity（打乱 MACD）+ 网格 + zz500，出结论。
10. 按 §3.5 判定 → 若有 edge 写 P1/P2 建议；若无 → 记档"MACD 背离在 A 股无显著 edge"。

---

## 8. 下游衔接（条件触发，不在本计划实现）

- **有 edge（且状态依赖）** → P1：把 `TF2_MACD` 从"裸值正向因子"改为 **regime-gated**（仅上升趋势桶正向，中枢/下降桶中性或反号）。→ P2：若重做 MACD 策略，用"背离 + 价格结构 + regime"组合，**绝不用金叉死叉当按钮**，且须先在 §5 证伪/证实。
- **无 edge** → 不接任何策略；`macd_kdj_plugin.py` 保持归档。

---

## 9. 决策记录

- 2026-07-31：用户贴 MACD 科普视频（反鸡汤干货），问平台 MACD 策略借鉴点；经实测盘点给出 P0/P1/P2 建议。
- 用户要求编写本开发计划（骗子指纹库已由另一 AI 完成，本计划不涉及）。
- 前置已确认：`daily` 含 high/low、`talib 0.6.8` 可用。
- 本工具定位：**指标验证诊断器**，非策略；零风险、不改现有代码；结论驱动 P1/P2 是否落地。

---

## 10. 实证结果（2026-07-31，已落地运行）

### 10.1 诊断器 `macd_divergence_study.py`（hs300 2014-2026，800 成分，12121 背离事件）
两套对照并存（关键改进：极点对照剥离"买在价格极点"的机械反弹）：
- **背景对照**（原 plan 设计，同状态桶+同月任意非背离日）：牛背离 +6%~+8%、熊背离 −6%~−9%，t 极大（40+）。**但这是假象**——背离事件坐在 pivot 极点，而背景日含随机日，差值=极点均值回归。
- **极点对照**（同状态桶+同月"普通极点"，MACD 未确认）：牛背离 −0.66%~+0.58%、熊背离 −0.10%~−0.44%，|t| 大多 <2；少数显著者（牛背离/盘整 t=−2.6）**反为负**。
- **参数网格**（pw∈{8,10,15}×lb∈{40,60}）全部一致：背景差大、极点差≈0 或微负。
- **sanity**（打乱 MACD 重跑）：极点差量级与真跑重叠（熊背离真实 −0.25% vs 打乱 −0.57%），证明 MACD 贡献≈0（差异来自 `lookback` 极值过滤的结构偏差，非信号）。

**结论：MACD 顶/底背离在 A 股无正增量 edge。** 那 ±8% 的漂亮数字纯粹是"事件坐在价格极点→均值回归"，与 MACD 无关。这与视频核心一致：**背离是提醒，不是按钮**。

### 10.2 新策略 `run_macd_regime.py`（平台 MACD 能力的"优化补充"，替代淘汰的旧金叉死叉插件）
严格按视频正确姿态：指数 MA200 趋势门控 + 底背离 + 突破近期高点 + 非中枢期 + 价>MA60，月度调仓、hfq 记账。修复了"用 T 日收盘信号却在 T 日开盘买"的未来函数。
对照买入持有(指数 +107.71%)：
| 版本 | 总收益 | 年化 | 最大回撤 | 夏普 | 超额 |
|---|---|---|---|---|---|
| 趋势门控开 | +148.02% | +8.25% | −27.22% | 0.46 | +40.30% |
| 趋势门控关(ablation) | +266.19% | +12.00% | −23.03% | 0.56 | +158.47% |

- 虽总收益跑赢指数，但**夏普(0.46~0.56)低于指数**，且极度集中（常持 1~4 只、多数月份空仓）→ 少数幸运年份（如 2018 策略+40% vs 指数−25%）靠个股集中运气驱动，非稳健 alpha。
- 反直觉发现：**关掉趋势门控反而全面更好**（总/年化/夏普↑、回撤↓）。因本策略已是"个股级选转强股"，指数 MA200 门控冗余且带来震出（与 v2 年度价值组合里 MA200 有用形成对照——门控价值取决于策略形态）。

### 10.3 最终处置（对应 §8 决策）
- **不接任何策略、不进主菜单**：背离诊断器与新策略均作为独立研究脚本（`data/results/macd_study/`、`data/results/macd_strategy/`），不污染现有引擎。
- `TF2_MACD` 因子**保持现状**（裸 DIF 值因子，默认 `enable_technical=False` 未启用）；不改为 regime-gated（无证据支持其改善）。
- 归档的 `macd_kdj_plugin.py`（金叉死叉）**保持归档**——实证再次证明金叉死叉思路无 edge，与视频批评互证。
- 本计划从"计划"转为"已验证结论档案"：MACD 背离在 A 股 ≈ 无增量 edge，与 `--breadth-confirm` 教训同属"看起来有理、实证无贡献"的一类。


