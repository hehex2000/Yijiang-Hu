# 仓位管理实验计划：凯利 vs 固定风险预算 vs 满仓

> **来源**：B站《量化的阳谋》(BV1BHtR69EbW · ApexQuant顶点量化 · 2026-09)
> **日期**：2026-09-04 立项　**状态**：P0 / P1 / P2 全部跑通，**P3 落地建议已出（待用户拍板）**
> **前置**：`docs/bollinger_enhancement_report.md` §11（基线单笔期望已验证）
> **最终结论**：凯利与风险预算**均无增量信息**；真正的杠杆是**并发/分散度**。
> 建议把单笔比例 20% → **10%**（=1/10），8/8 窗口 CAGR / Sharpe / MDD 三维度全胜。见 §1.1 与 §3.5。

---

## 1. 结论速览（先把话说在前面）

视频**数学全对**、**行业事实有硬伤**，但它抛出了一个我们平台**恰好缺的模块**：仓位管理（sizing）。

它的致命缺陷是——**凯利（下注比例）与 2% 风险预算（单笔风险）是两套东西，视频并列讲却没建桥**。而这座桥是可以被我们的真实数据量化的，量化之后立刻暴露出视频的处方有多危险：

| 算法 | 用哪个"止损距离" | 算出的单笔仓位 |
|---|---|--:|
| 2% 风险预算 | 名义止损 **3%**（视频的隐含用法） | **66.7%** ⚠️ |
| 2% 风险预算 | 实测平均亏损 6.66% | 30.0% |
| 2% 风险预算 | 实测亏损 P95 = 18.74% | 10.7% |
| 2% 风险预算 | 实测亏损 P99 = 25.70% | 7.8% |
| 全凯利 f\*=(bp−q)/b | 实测 p=0.6266 / b=1.60 | **39.3%** |
| 半凯利 | — | 19.7% |
| 平台当前默认 | 配置 0.55/1.5 → f\*=25% 被 `max_position` 夹住 | **20.0%** |

**三个立刻可读的结论：**

1. **"2% 止损 + 2% 风险预算"在名义口径下算出 66.7% 仓位——比全凯利还激进 70%。** 视频让观众同时接受"凯利说买 20%"和"2% 规则说止损 2%"，这两句话在观众脑子里是安全的，连起来执行却是重仓。这不是学术瑕疵，是会亏钱的误导。
2. **名义止损 3% 是假的**：533 笔里 **26.64%**（142 笔）亏损超过 3%，占全部亏损笔的 **71%**。止损用前一日收盘判定、次日执行，跳空+延迟让实际亏损距离是名义值的 2~6 倍。**用名义止损做风险预算 = 系统性低估仓位风险。**
3. **平台默认 20% 恰好落在半凯利上（19.7%）**——是巧合不是设计（config 里 0.55/1.5 是"经验预设非回测实测"，见 config 注释）。这次实验会给出它该不该改的答案。

**⚠️ 2026-09-07 重大修正：上表「全凯利 39.3%」是公式误用。**

`f*=(bp−q)/b` 隐含假设**亏损时亏掉 100% 的下注额**（a=1），但本策略实测**平均亏损只有 6.657%**（a=0.0666）。换成与分布匹配的广义凯利：

| 凯利口径 | 公式 | 结果 |
|---|---|--:|
| 标准（错用，隐含 a=1） | `(bp−q)/b` | 39.3% |
| **广义（正确，a=实际亏损幅度）** | `(p·b − q·a)/(a·b)` | **590.8%** |
| 连续近似 | `μ/σ²` | **326.3%** |

实测输入：p=0.6266、平均盈 +10.652%、平均亏 −6.657%、μ=+4.189%、σ=11.331%。

→ **凯利在这套分布上给出 f\* ≈ 326%~591%，即「应加 3~6 倍杠杆」**。标准公式把亏损幅度高估了 **15 倍**，仓位被低估同量级。
→ 两个后果：
  ① 「平台默认 20% 恰好落在半凯利 19.7%」这个"巧合"**建立在错误公式上，不成立**；
  ② **凯利公式在本策略上给不出可执行答案**（>100%）。视频"凯利说买 20%"与本计划原写的"39.3%"同属**简化公式误用**——这一点视频没说错公式，是它（和当时的我）默认了 a=1 的赌场式设定，而 A 股日频策略的亏损幅度远小于 100%。

---

## 1.1 ★ P2 最终结论（2026-09-07）：真正的杠杆是**并发/分散度**，不是仓位算法

P2① 已证伪"仓位算法"这条路（凯利/风险预算皆无增量）。于是把火力转到 P2① 暴露出的真正变量——**能同时持有几个仓位**。三步实验（§3.3.2 ~ §3.3.4）：

| 步骤 | 结论 |
|---|---|
| P2② 并发上限扫描 | 信号供给上限：理论并发**中位 6 / P90 17 / 均值 7.47**。约束**只有 `skip_cash`**（f=0.5 时 350/536 信号因没钱被跳过） |
| P2③ walk-forward | **f=0.10 vs 默认 0.20：8/8 窗口 CAGR / Sharpe / MDD 三维度同时胜出** |
| P2④ 归因 | 固定成交笔数后 f=0.125 仍 12.38% vs 9.20% → **分散化红利真实，占改善的 64%** |

**平台真实口径（无硬 cap，每笔投 equity×f，靠现金自然饱和）全样本 2019-2026：**

| f | 成交/536 | 均并发 | 暴露% | CAGR% | Sharpe | MDD% |
|--:|--:|--:|--:|--:|--:|--:|
| 0.50 | 186 | 2.53 | 68.4 | 4.98 | 0.35 | −49.80 |
| 0.25 | 260 | 3.44 | 62.6 | 10.40 | 0.67 | −32.82 |
| **0.20（当前默认）** | 291 | 3.88 | 60.0 | 9.20 | 0.63 | −31.59 |
| 0.167 | 318 | 4.23 | 57.4 | 11.75 | 0.81 | −28.83 |
| **0.10（建议值）** | **419** | **5.73** | **50.4** | **13.95** | **1.05** | **−19.29** |
| 0.083 | 448 | 6.23 | 46.5 | 13.44 | 1.06 | −18.10 |
| 0.05 | 516 | 7.23 | 34.6 | 11.03 | 1.10 | −13.63 |

→ **降 f 不是"变保守"，是"变分散"**：暴露度反而从 60.0% 降到 50.4%，成交信号从 291 涨到 419。
→ **过度分散有代价**：f=0.05 时 3 个独立窗口里 2 个 CAGR 下滑（资金闲置），故不推荐极端值。

**⚠️ 一条必须诚实标注的限定**：各窗口的 **CAGR 峰值 f 并不稳定**（0.30 / 0.05 / 0.125 / 0.167 / 0.05 / 0.083 / 0.100）。因此本计划**不声称"f=0.10 是最优点"**，只主张稳健得多的命题——**f 从 0.20 降到 0.10 在 8/8 窗口的配对比较中三维度全胜**。配对比较不依赖峰值识别，证据强度高于"峰值位置一致"。

---

## 2. 三个核心设计决策（决定了这个实验有没有信息量）

这三条如果做错，实验会跑出一堆漂亮但零信息的数字。**先定死，再写代码。**

### D1 · 必须做**组合级共享资金池**，不能沿用"每只股票独立 10 万"

现有布林带 ablation 脚本是每只股票独立跑 10 万本金。在这套口径下比仓位是**彻底无效的**——
单标的、无并发时，仓位只是把资金等比例缩放：`f=100%` 与 `f=20%` 的**收益率百分比完全相同**，唯一区别是 80% 资金闲置。这样跑出来的对比表全是假数字。

凯利/风险预算真正要解决的问题是 **"多个同时到达的信号之间怎么分钱"**——只有在共享资金池、信号竞争资金的场景下才存在。

→ **因此 P1 需要新建一个组合级 sizing 引擎**（复用现有 `load_stock_prices` + as-of 快照管线）。

### D2 · 必须在**已验证有正期望**的策略上比，不能用负期望策略

在负期望策略上比仓位，答案永远是"越少越好"，零区分力，还会误导成"凯利有效"。

→ 载体选 **`mean_reversion` 基线**（不带任何视频候选开关）。已在 §11 验证：
单笔净期望 **+4.189%**、胜率 62.66%、盈亏因子 2.69、持仓窗口 skill **+3.670pp**（vs 等长随机窗口）。
这是平台上少数被实测证明有 alpha 的入场信号，是唯一合格的实验载体。

### D3 · 风险预算的"止损距离"必须用**实测分布**，不能用名义 3%

见 §1 第 2 条。两种口径都要跑，差异本身就是结论的一部分。

---

## 3. 实验设计

### 3.0 共用口径（所有档位一致，隔离单一变量 = 仓位算法）

| 项 | 取值 | 说明 |
|---|---|---|
| 载体策略 | `mean_reversion` 基线 | 全部视频候选开关 `False` |
| 股票池 | 沪深300 as-of 20190101 快照前 40 只 | 消除幸存者偏差 |
| 区间 | 2019-01-01 ~ 2026-07-31 | 与布林带系列同窗口 |
| 初始资金 | 组合级 **100 万**（共享池） | 非每只 10 万 |
| 成本 | 真实分科目（`real_cost=True`）为主口径，简单口径对照 | 佣金万2.5+滑点0.1%+过户费+日期感知印花税 |
| 出场/止损 | 完全沿用基线（Z>2 或 RSI>70 + 3% 硬止损） | **只改仓位，不改信号与出场** |
| 并发上限 | 由仓位算法自然决定（`floor(1/f)`） | 不额外设限 |

### 3.1 P0 · Bootstrap 蒙特卡洛（**先做，零回测成本**）

**输入**：现成的 533 笔 round-trip（`data/results/mean_reversion_trade_expectancy/round_trips.csv`，字段 `net_pct` / `hold`）。

**方法**：
- 按历史信号到达率（533 笔 / 40 只 / 7.5 年 ≈ **1.78 笔/只/年** → 组合约 **71 笔/年**）用泊松过程生成到达序列；
- 每笔从经验分布抽 `(net_pct, hold)`，占用 `权益 × f`，同时并发上限 `floor(1/f)`，**资金不足则跳过**（保留真实约束，这正是仓位算法的代价所在）；
- 重采样用 **stationary block bootstrap（块长 ~20 笔）**，保留 2022 熊市这类局部聚集，避免 iid 假设低估尾部；
- 每档 5000 条路径，seed 固定。

**档位**：f ∈ {满仓1.00, 0.667, 0.393, 0.30, 0.20, 0.107, 0.078, 0.05}

**输出**：每档的 终值 P5/P50/P95、**破产概率**（跌破初始 50% / 20%）、最大回撤 P50/P95、几何增长率。

**为什么先做它**：几分钟出结果、不需要新引擎，且能直接回答"满仓的尾部到底有多惨"。若 P0 就显示满仓破产概率显著、而中位数仍最高 → 结论就是"风险预算的价值在尾部不在均值"，P1 只需确认机制，不必全押。

### 3.1.1 P0 执行结果（2026-09-04，已跑通）

**设置**：533 笔 → block bootstrap（块长 20，保留聚类）/ iid 对照；5000 路径 × 8 档；M=1775 笔（≈25 年）；并发上限 `floor(1/f)`；共享资金池。
**脚本**：`run_sizing_bootstrap.py`｜**结果**：`data/results/position_sizing/sizing_bootstrap.csv`

| label | f | cap | 实现笔数 | 终值中位(x) | 破产50% | MDD_P95 | 最差权益P5 | CAGR_P50 |
|---|--:|--:|--:|--:|--:|--:|--:|--:|
| FULL | 1.000 | 1 | 229 | 7404.2 | 0.00% | −29.8% | 71.5% | 42.8% |
| **RISK_2PCT_NOM**（视频处方） | 0.667 | 1 | 229 | 532.6 | 0.00% | −19.9% | 81.2% | 28.5% |
| KELLY_FULL | 0.393 | 2 | 454 | 1765.5 | 0.00% | −13.7% | 89.2% | 34.9% |
| RISK_2PCT_MEAN | 0.300 | 3 | 673 | 4582.4 | 0.00% | −12.1% | 91.2% | 40.1% |
| **FIXED20**（平台默认） | 0.200 | 5 | 1077 | 5997.0 | 0.00% | −10.3% | 92.9% | 41.6% |
| RISK_2PCT_REAL | 0.107 | 9 | 1608 | 1065.8 | 0.00% | −6.4% | 96.4% | 32.2% |
| RISK_2PCT_P99 | 0.078 | 12 | 1756 | 271.6 | 0.00% | −4.7% | 97.4% | 25.1% |
| QUARTER_KELLY | 0.050 | 20 | 1775 | 38.9 | 0.00% | −3.1% | 98.3% | 15.8% |

（iid 对照：FULL MDD_P95=−31.4% / 最差权益P5=64.5%；FIXED20=−10.3% / 94.0%——与 block 差仅 1~2pp，见下。）

**核心结论（P0 能定的）：**

1. **破产概率 = 0%（全部 8 档）**。策略强正期望（单笔 +4.189%）兜底，25 年内终值从不低于初始 50%/20%。满仓档最差权益 P5 也只跌到 **71.5%** 起始资金——**视频"满仓连亏两次归零、概率 16%"是稻草人**（其 16% 建立在"每次 all-in 全亏光"的荒谬假设上；本策略最差单笔 −29.84%，永不归零）。
2. **真实风险轴 = 尾部回撤，随 f 单调**：MDD_P95 从 −3.1%（f=0.05）到 −29.8%（f=1.0）；最差权益 P5 从 98.3% 到 71.5%。风险预算/低仓位的唯一真实价值是**压尾部回撤**，不是防破产。
3. **视频处方 RISK_2PCT_NOM（66.7%）被平台 FIXED20（20%）全面压制**：终值中位 532x vs 5997x（差 11 倍）、MDD_P95 −19.9% vs −10.3%、最差权益 P5 81.2% vs 92.9%。**增长与风控双维度都劣于现状** → 视频的"2% 风险预算 + 名义 3% 止损 = 66.7% 仓位"实锤无效。
4. **iid 与 block 尾部几乎无差**（MDD 差 1~2pp，最差权益差 3~7pp，且 iid 反而略差）。计划担心的"iid 低估尾部"在 round-trip 聚合层**不成立**——每笔已是完整 round trip，日频聚类被平滑。诚实标注：P0 用 iid 也够，block 仅更稳。

**⚠️ 方法学修正（供 P1，重要）：**

- **终值不能跨 f 直接排序**：上表终值非单调（5997@f0.20 > 4582@f0.30 > 1765@f0.393 < 532@f0.667 < 7404@f1.0）。根因是**并发上限 `floor(1/f)` 把"成交笔数"与 f 耦合**：f 越小→并发越多→实际成交笔数越多（1775→1077→454→229），而笔数本身驱动终值。所以"哪个 f 终值最高"被容量机制主导，不是凯利分数主导。
- → **P1 必须用真实日历引擎算 Sharpe / 年化收益**，并强制报告「成交笔数」与「平均在市资金占比（暴露度）」。沿用布林带教训：若某档收益高但暴露度也高，须看风险调整后才成立。P0 因无日历时间轴，无法算 Sharpe，故 P0 只回答"破产/尾部回撤/视频处方是否劣于现状"三件事。
- **回撤指标 bug 已修**：首版用「全局最低权益 ÷ 全局最高」误把"起点 1.0 vs 终点 82x"算成 −99% 回撤；已改为逐时点 running-peak 相对回撤的最小值（标准最大回撤）。修正后 MDD 单调合理。

**⚠️ 2026-09-07 复核修正（P1 跑完后回看 P0，发现两处口径问题）：**

- **①「MDD_P95」被误读为尾部**：mdd 是负数，`np.percentile(mdds, 95)` 取的是**浅尾**（95% 路径好于此），不是深尾。上表该列实为浅尾；**中位回撤 `mdd_p50` 才是中心估计**：
  FULL **−39.85%** / RISK_2PCT_NOM −27.61% / KELLY_FULL −19.32% / RISK_2PCT_MEAN −16.43% / FIXED20 **−13.29%** / RISK_2PCT_REAL −8.08% / RISK_2PCT_P99 −5.91% / QUARTER_KELLY −3.84%。
  → 原结论「尾部回撤 −3.1%~−29.8%」应改为**中位 −3.84%~−39.85%**。**随 f 单调的结论方向不变**（幅度约 1.33 倍）。真正的深尾需 p5，P0 未存，待补。
- **② P0 的回撤是「round-trip 颗粒度」，系统性低估约 2 倍**：`simulate_correct` 只在**平仓时点**更新 `tot = cash + deployed`（第 94/117 行），持仓期间 `deployed` 恒定、不做逐日盯市 → 持仓内的大幅浮亏完全看不见。P1 用真实日历逐日盯市后，实现回撤普遍是 P0 中位的约 2 倍：

  | 档位 | P0 中位 MDD | P1 实现 MDD（逐日盯市） | 倍数 |
  |---|--:|--:|--:|
  | FULL | −39.85% | −66.75% | 1.7× |
  | RISK_2PCT_NOM | −27.61% | −47.55% | 1.7× |
  | FIXED20 | −13.29% | −28.02% | 2.1× |

  → **不影响的结论**：破产概率 0%、随 f 单调、视频处方劣于现状（偏误随 f 单调放大，档间差距反被**低估**）。
  → **必须注意**：P0 的绝对回撤/破产数字只可用于**档间相对排序**，不可当真实风险水位引用；「破产 0%」同样建立在 round-trip 颗粒度上。

### 3.2 P1 · 组合级 sizing 引擎 + 6 档 A/B

新建 `run_position_sizing_ablation.py`（组合级）+ 在 `base_strategy.py` 加 opt-in 的 `risk_budget` 分支（默认关，零行为变更）。

| 档位 | 仓位算法 | 名义仓位 | 机制 |
|---|---|--:|---|
| **FULL** | 每笔投入全部可用权益 | 100% | 集中，实际同时只持 1 只 |
| **FIXED20** | 固定 20% 权益 | 20% | 平台当前默认口径 |
| **KELLY_FULL** | 滚动估计 p/b → f\* | ~39% | 随胜率/盈亏比变动 |
| **KELLY_HALF** | f\*/2 | ~20% | 数值≈FIXED20 但**机制不同**（自适应 vs 固定） |
| **RISK_2PCT_NOM** | 2% ÷ 名义止损 3% | **66.7%** | 视频的隐含处方 |
| **RISK_2PCT_REAL** | 2% ÷ 滚动实测亏损距离 | ~11~30% | 修正版风险预算 |

**子扫描（敏感性）**：风险预算 ∈ {0.5%, 1%, 2%, 3%} × 距离口径 ∈ {名义3%, 滚动均亏, 滚动P95}。

**关键约束**：凯利的 p/b 必须**滚动估计**（只用 t 之前的已平仓交易），全样本 p/b 是前视。若 t 之前样本不足（<20 笔），回退到保守先验 p=0.5/b=1.5。

### 3.2.1 P1 执行结果（2026-09-07，已跑通）

**设置**：组合级共享资金池 100 万；**信号流只抽一次**（1e8 本金 + `use_kelly=False`，6 档共用同一条 536 BUY / 533 SELL）→ 真正的单变量隔离；真实分科目成本；并发上限 `floor(1/f)`；期末强平；凯利/风险预算**滚动估计**（最小样本 20，不足回退先验 p=0.5 / b=1.5、距离 6%）。
**脚本**：`run_position_sizing_ablation.py`｜**结果**：`data/results/position_sizing/sizing_ablation.csv` + `sizing_nav.csv`

| 档位 | f均值 | 成交/信号 | 部署率 | CAGR% | Sharpe | MDD% | 暴露度 | CAGR/暴露 | 均笔% | 胜率% |
|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| FULL | 1.000 | 43/536 | 99.7% | −0.01 | 0.13 | −66.75 | 66.9% | −0.01 | +0.554 | 48.8 |
| FIXED20（平台默认） | 0.200 | 223/536 | 99.5% | 9.14 | 0.63 | −28.02 | 59.0% | 15.49 | +1.780 | 53.4 |
| KELLY_FULL | 0.243 | 182/536 | 99.1% | 7.02 | 0.51 | −38.69 | 55.9% | 12.56 | +2.077 | 54.9 |
| **KELLY_HALF** | 0.151 | 276/536 | 99.3% | **11.46** | **0.84** | −28.79 | 51.8% | **22.13** | +2.600 | 56.5 |
| RISK_2PCT_NOM（视频处方） | 0.667 | 50/536 | 99.9% | 5.46 | 0.38 | −47.55 | 47.9% | 11.39 | +1.816 | 50.0 |
| RISK_2PCT_REAL | 0.248 | 166/536 | 99.6% | 7.56 | 0.56 | −28.39 | 52.9% | 14.30 | +1.398 | 51.2 |

**结论：**

1. **部署率自查 ✅ 全档 ≥98%**（99.1%~99.9%）——没有现金/整手约束被静默吸收。这是最难自查的假数字来源，过了才敢信表格。
2. **视频处方 RISK_2PCT_NOM（66.7%）再次垫底**：CAGR 5.46 / Sharpe 0.38 / MDD −47.55%，相对平台 FIXED20（9.14 / 0.63 / −28.02%）**三维全劣**。P0（bootstrap）与 P1（真实日历）两套独立方法同向证伪 → 实锤。
3. **满仓 FULL 是灾难，但不是因为破产**：CAGR −0.01% / Sharpe 0.13 / MDD −66.75%。根因是**容量饥饿**——`floor(1/1.0)=1` 使 536 个信号只成交 43 个（成交率 8.0%，skip_cap=454），单票集中暴露度 66.9%。P0「满仓不破产」成立，P1 补上后半句：**但会饿死且波动巨大**。
4. **KELLY_HALF 是唯一「收益更高且暴露度更低」的档位**：vs 平台 FIXED20 → CAGR +2.32pp、Sharpe +0.21、**暴露度 51.8% < 59.0%**；CAGR/暴露 22.13 vs 15.49。不是靠加大暴露换来的 → 按 §4 判据**初步胜出**。
   ⚠️ **2026-09-07 已被 §3.3.1 证伪**：优势实为 `floor(1/0.151)=6` 比 FIXED20 的 5 更多并发（＝更分散），朴素 `cap=8 / f=0.107` 在四个维度上全面胜过 KELLY_HALF。本条作废。
5. **⚠️ 但「成交笔数」仍是未分离的混淆变量**：各档成交率 8.0%~51.5%（并发上限 `floor(1/f)` 造成），且**均笔% 随档位变（+0.554 ~ +2.600）** → 仓位算法同时改变了「投入多少」和「能成交哪些信号」，各档选到的交易样本本就不同。**这限制了档间直接比较**，P2 须用「固定成交笔数 / 信号随机丢弃至同笔数」做二次隔离后，才能对 KELLY_HALF 下最终结论。

### 3.3 P2 · walk-forward + 部署度审计

- **滚动窗口**：5 个不重叠 ~18 个月窗口（与 OBV walk-forward 同切分），各窗口独立统计 Δ。
- **暴露度对照（沿用布林带教训）**：仓位算法本质就是**改变暴露度**。必须报告每档的「平均在市资金占比 / 平均并发持仓数」，否则"满仓收益最高"可能只是 beta。
  → 判据：**若某档收益更高但暴露度也更高，必须看风险调整后（Sharpe / 收益÷暴露度）才成立。**
- **部署率自查（方法论第13条）**：新引擎必查每次建仓实际部署额/可用资金，目标 ≈100%。等权/比例仓位最容易踩 `if cost<=cash` 整只跳过或 `cash` 循环内递减衰减的坑，这类 bug 会**悄悄改善回撤**（回撤被 bug 美化是最难自查的假数字）。

### 3.3.1 P2① · f × cap 解耦网格（2026-09-07，已跑通）

**为什么做它**：P1 里 cap 恒等于 `floor(1/f)` ⇒ f 同时改变「每笔投多少」和「能成交哪些信号」，两者纠缠。原计划的"固定成交笔数"做不干净（把笔数拉平＝抹掉仓位算法的核心后果）。改用**钉死 cap**：同一 cap 下成交的信号集合完全相同，档间差异纯粹来自 f。
**自证**：同 cap 各 f 的 `n_taken` 必须一致且 `skip_cash=0`。cap=2/3/8 ✅ 完全一致；cap=1 的 f=1.000（skip_cash=39，成交 43 vs 50）与 cap=5 的 f=0.200（skip_cash=4，223 vs 224）有现金约束介入，已标注。
**结果**：`data/results/position_sizing/sizing_grid.csv`

| cap | f | 成交 | CAGR% | Sharpe | MDD% | 暴露度 | CAGR/暴露 | 均笔% |
|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 1 | 1.000 | 43 | −0.01 | 0.13 | −66.75 | 66.9% | −0.01 | +0.554 |
| 1 | 0.667 | 50 | 5.46 | 0.38 | −47.57 | 48.0% | 11.39 | +1.816 |
| 1 | 0.393 | 50 | 3.85 | 0.40 | −30.52 | 28.1% | 13.70 | +1.816 |
| 1 | 0.300 | 50 | 3.11 | 0.40 | −23.93 | 21.4% | 14.53 | +1.816 |
| 1 | 0.200 | 50 | 2.18 | 0.41 | −16.37 | 14.2% | 15.34 | +1.816 |
| 1 | 0.107 | 50 | 1.22 | 0.41 | −9.02 | 7.6% | 16.05 | +1.816 |
| 1 | 0.050 | 50 | 0.59 | 0.43 | −4.19 | 3.5% | 16.97 | +1.816 |
| 2 | 0.393 | 98 | 4.80 | 0.37 | −41.66 | 54.8% | 8.77 | +1.368 |
| 2 | 0.200 | 98 | 2.98 | 0.39 | −22.71 | 27.7% | 10.75 | +1.368 |
| 2 | 0.107 | 98 | 1.73 | 0.40 | −12.46 | 14.7% | 11.71 | +1.368 |
| 2 | 0.050 | 98 | 0.84 | 0.41 | −5.87 | 6.8% | 12.30 | +1.368 |
| 3 | 0.300 | 148 | 6.66 | 0.47 | −31.43 | 59.9% | 11.11 | +1.428 |
| 3 | 0.200 | 148 | 4.84 | 0.48 | −21.55 | 39.9% | 12.11 | +1.428 |
| 3 | 0.107 | 148 | 2.74 | 0.48 | −11.83 | 21.3% | 12.89 | +1.428 |
| 3 | 0.050 | 148 | 1.33 | 0.49 | −5.64 | 9.8% | 13.61 | +1.428 |
| 5 | 0.200 | 223 | 9.14 | 0.63 | −28.02 | 59.0% | 15.49 | +1.780 |
| 5 | 0.107 | 224 | 5.36 | 0.66 | −15.81 | 31.6% | 16.97 | +1.804 |
| 5 | 0.050 | 224 | 2.62 | 0.68 | −7.33 | 14.6% | 18.00 | +1.804 |
| **8** | **0.107** | **325** | **11.81** | **1.00** | **−19.70** | **45.2%** | **26.12** | +2.675 |
| 8 | 0.050 | 325 | 5.55 | 1.00 | −9.45 | 21.0% | 26.43 | +2.675 |

**结论（三条，两条是证伪）：**

1. **钉死成交信号后，f 只是「用风险换收益」，不存在内部最优点**：cap=1 组 7 个 f 成交的是**同一批 50 笔**（均笔% 全为 +1.816 即铁证）→ CAGR 随 f 单调升（0.59%→5.46%）、Sharpe 单调降（0.43→0.38）、MDD 单调加深（−4.19%→−47.57%）、**CAGR/暴露 单调降（16.97→11.39）**。
   → 与 §1 修正后的凯利一致：正确凯利 f\*≈326%~591%（>100%），**在无杠杆区间内就是"越重仓增长越高、效率越低"**，没有内部极值。凯利在此给不出可执行答案，再次确认。
2. **★ 证伪 P1 结论 4：主导变量是 cap（并发/分散度），不是仓位算法。** 暴露度相近（39.9%~48.0%）时，Sharpe 与 MDD 几乎只由 cap 决定：

   | 组 | 暴露度 | 成交 | CAGR% | Sharpe | MDD% |
   |---|--:|--:|--:|--:|--:|
   | cap=1, f=0.667 | 48.0% | 50 | 5.46 | 0.38 | −47.57 |
   | cap=3, f=0.200 | 39.9% | 148 | 4.84 | 0.48 | −21.55 |
   | **cap=8, f=0.107** | **45.2%** | **325** | **11.81** | **1.00** | **−19.70** |

   → 同样约 45% 暴露，分散到 8 个仓位 vs 押在 1 个：**Sharpe 0.38→1.00（2.6 倍）、MDD −47.57%→−19.70%（2.4 倍）**。这是**分散化红利**，与凯利/风险预算无关。
3. **★ 朴素固定分数 + 高 cap 全面击败自适应凯利**：`cap=8 / f=0.107`（11.81 / 1.00 / −19.70 / 暴露 45.2%）**四维全优于 P1 的 KELLY_HALF**（11.46 / 0.84 / −28.79 / 51.8%），且暴露更低。
   → KELLY_HALF 在 P1 胜出的真实原因是 `floor(1/0.151)=6 > floor(1/0.20)=5`，**并发更多＝更分散**，不是凯利自适应有信息。**P1 结论 4 作废。**

**对 P3 的直接影响**：要改善的不是"仓位算法"，而是**并发上限 / 可执行信号数**（分散度）。凯利与风险预算在本策略上**均无增量信息** → 按 §3.4 纪律，**默认口径不动，不新增 `risk_budget` 开关**。

### 3.3.2 P2② · 并发上限扫描（2026-09-07，已跑通）

**为什么做它**：P2① 里 cap 与 f 是「f×cap ≤ 1」的松散组合，各档实际暴露度差异很大（39.9%~48.0%），无法区分"分散化红利"与"只是多买了点"。
**设计**：
- **A 组（暴露归一化）**：f = E/cap ⇒ 名义总暴露恒定（E ∈ {0.5, 0.8, 1.0}），跨 cap 只改分散度；
- **B 组（对照）**：f 恒 0.20，暴露随 cap 单调上升；
- **C 组（平台真实口径）**：**无硬 cap**，每笔投 `equity×f`，靠现金自然饱和 —— 这才对应平台 `base_strategy` 的真实行为，也是能直接映射到 `config.position_pct` 的曲线。

**★ 前置诊断：信号供给上限**（cap=∞ + 现金无限时的理论并发）

```
均值 7.47｜中位 6｜P75 11｜P90 17｜P99 29｜最大 30
空仓天数占比 11.8%｜≥5 只 58.2%｜≥10 只 30.9%｜≥20 只 5.1%
```

→ **cap 超过 ~17 基本空转**。想再提并发，得先扩**信号供给**（加宽池子 / 放宽入场），而不是继续加 cap。
→ 这也解释了 A 组的反常：cap=20/30 时**实际暴露反而下降**（E=1.0：66.9% → 23.9%），因为 f 变小而持仓数顶到供给天花板（均并发只从 4.31 涨到 7.47）→ **A 组的"暴露归一化"被供给上限破坏，只能作参考，不能作判据**。

**关键发现：唯一约束是 `skip_cash`**（cap=99 时 `skip_cap=0`、`skip_lot=0`）

| f | 成交 | skip_cash | 被跳过比例 |
|--:|--:|--:|--:|
| 0.50 | 186 | 350 | 65.3% |
| 0.20 | 291 | 245 | 45.7% |
| 0.125 | 374 | 162 | 30.2% |
| 0.05 | 516 | 20 | 3.7% |

→ **f 大 ⇒ 现金被少数持仓占满 ⇒ 大量信号因没钱被跳过**。这是"堵仓"机制，也提示**出场速度**可能是另一条瓶颈线（后续可查）。

### 3.3.3 P2③ · walk-forward（2026-09-07，已跑通）

**设计**：3 个**不重叠**窗口（W1 2019-2021 / W2 2022-2023 / W3 2024-2026，独立性最强）+ 5 个**滚动**窗口（R1-R5，3 年窗 1 年步长，满足 5/5 判据但相邻重叠，仅作稳健性补充）× C 组 f 扫描。

**★ f=0.20（默认）vs f=0.10（建议）配对比较 —— 8/8 三维度同时胜出：**

| 窗口 | CAGR 20%→10% | Δ | Sharpe | Δ | MDD | Δ |
|---|--:|--:|--:|--:|--:|--:|
| W1 | 13.49 → 16.32 | **+2.84** | 0.90 → 1.29 | +0.38 | −20.97 → −11.96 | +9.01 |
| W2（熊） | 0.32 → 5.63 | **+5.31** | 0.11 → 0.46 | +0.34 | −19.82 → −16.31 | +3.51 |
| W3 | 15.13 → 18.64 | **+3.52** | 1.01 → 1.34 | +0.33 | −14.85 → −12.88 | +1.97 |
| R2 | 12.34 → 14.95 | +2.61 | 0.72 → 0.99 | +0.27 | −29.83 → −19.33 | +10.50 |
| R3（熊） | −1.50 → 4.80 | **+6.30** | 0.01 → 0.40 | +0.39 | −27.15 → −19.32 | +7.82 |
| R4 | 2.46 → 11.25 | **+8.78** | 0.23 → 0.77 | +0.54 | −27.26 → −16.49 | +10.77 |
| R5 | 4.38 → 13.05 | **+8.67** | 0.38 → 1.05 | +0.67 | −27.46 → −16.31 | +11.15 |

（R1 与 W1 同区间，结果一致。）
→ **熊市窗口（W2 / R3）改善最大**（+5.31 / +6.30），说明收益不是靠 beta 放大——暴露度反而更低。

**候选 f 在 3 个独立窗口的综合表现（决定推荐值）：**

| f | W1 | W2 | W3 | CAGR 均值 | **最差窗口** | 均 Sharpe | 最差 Sharpe | 均 MDD |
|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 0.200（默认） | 13.49 | 0.32 | 15.13 | 9.64 | **0.32** | 0.68 | 0.11 | −18.55 |
| 0.167 | 15.50 | 3.42 | 18.18 | 12.37 | 3.42 | 0.86 | 0.30 | −17.23 |
| 0.125 | 17.27 | 3.15 | 20.49 | 13.64 | 3.15 | 0.99 | 0.29 | −14.74 |
| **0.100（建议）** | 16.32 | 5.63 | 18.64 | 13.53 | **5.63** | 1.03 | 0.46 | −13.72 |
| 0.083 | 15.04 | 8.59 | 18.42 | **14.02** | **8.59** | 1.12 | 0.68 | −12.58 |
| 0.050 | 11.08 | 9.66 | 14.28 | 11.67 | 9.66 | 1.20 | 0.88 | −9.92 |

→ **推荐 f = 0.10（=1/10，最多约 10 个持仓）**：8/8 全胜 + CAGR 均值 13.53%（vs 默认 9.64%，**+3.89pp**）+ 最差窗口 +5.31pp + Sharpe 1.03 + MDD −13.72%。与信号供给（中位 6 / P90 17）匹配，实际均并发 5.73 / 最大 15。
→ **更保守可选 f = 0.083**：均值（14.02）与最差（8.59）双优，minimax 判据下略强，但 1/12 不如 1/10 直观。
→ **不推荐 f ≤ 0.05**：牛市窗口 W1/W3 CAGR 明显下滑（11.08 / 14.28），是资金闲置的代价。

### 3.3.4 P2④ · 归因：分散化 vs 多吃信号（2026-09-07，已跑通）

**为什么必须做**：C 组显示 f 越小越好，但 f 同时放松了现金约束 ⇒ 成交笔数从 291 涨到 374。**两者的贡献必须拆开，因为指向的后续动作完全不同**：
- 若是**分散化** → 降 f 本身就是解，且应继续加宽标的池；
- 若是**多吃信号** → 真问题是资金约束，应改"按质量排序优先成交好信号"，而不是盲目降 f（f 再小就是资金闲置）。

**做法**：固定 f=0.125，随机丢弃 BUY 信号把成交笔数压回 f=0.20 的水平，12 seed 平均消除随机性。

| keep | 成交 | 均并发 | 暴露% | CAGR% | (std) | Sharpe | MDD% | 均笔% |
|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 1.00 | 374 | 5.09 | 53.4 | 14.21 | 0.00 | 1.01 | −21.78 | 2.766 |
| 0.85 | 346 | 4.74 | 51.2 | 13.63 | 1.34 | 1.00 | −21.65 | 2.839 |
| 0.75 | 321 | 4.45 | 48.5 | 13.14 | 1.17 | 0.99 | −20.98 | 3.037 |
| **0.65** | **295** | 4.10 | 45.4 | **12.38** | 1.81 | 0.97 | −19.85 | 3.070 |
| 0.55 | 264 | 3.67 | 41.5 | 12.00 | 2.35 | 0.99 | −18.39 | 3.159 |
| 0.45 | 224 | 3.14 | 36.7 | 10.75 | 1.89 | 0.96 | −16.96 | 3.289 |

**判读**：keep=0.65 时成交 295 ≈ 基准的 291，此时 **CAGR 12.38% vs 基准 9.20%（+3.18pp）**、Sharpe 0.97 vs 0.63、MDD 浅 11.74pp，而**暴露度更低（45.4% vs 60.0%）**。

→ **分散化红利真实存在，不是"多吃信号"的假象。**
→ 改善拆解（总 +5.00pp）：**分散化 +3.18pp（64%）｜多吃信号 +1.82pp（36%）**，两者同号、无互相抵消。

### 3.4 P3 · 落地建议（待用户拍板）

**结论：凯利 / 风险预算在本策略上均无增量信息（§1 修正 + §3.3.1 证伪），不新增任何 sizing 开关。**
真正有效的是**单笔比例 f 这一个参数**，而它已经在 config 里存在。

| 项 | 当前 | 建议 |
|---|---|---|
| 单笔比例（等效 `kelly_max`） | 0.20 | **0.10** |
| 证据 | — | 8/8 窗口 CAGR/Sharpe/MDD 三维度全胜（§3.3.3） |
| 机制 | — | 分散化 64% + 信号覆盖 36%（§3.3.4） |

**⚠️ 落地前需确认的一件事**：平台 `mean_reversion` 走 `use_kelly=True` + `kelly_max=0.20`，实际仓位 = `min(f_kelly, 0.20)`。由于 §1 已证 f_kelly 在此分布下恒 >100%（错误公式下也常 >20%），等效就是**固定 20%**。故改 `kelly_max` 即等效改 f —— 但**建议同时把 `use_kelly` 设为 `False`** 或显式固定，避免"凯利自适应"这个已被证伪的组件继续挂在默认值上误导后续维护者。**这一条改动超出本轮范围，请用户拍板。**

**未做 / 降级（理由）**：
- ~~风险预算 {0.5,1,2,3}% × 距离口径 敏感性~~ → 凯利与风险预算已双双证伪无增量，跑敏感性没有信息量；
- ~~补 P0 的 `mdd_p5` 深尾~~ → P0 的回撤是 round-trip 颗粒度（不逐日盯市），已被 P1 证系统性低估约 2 倍，补分位数只是给错误口径加精度；
- **未做跨股票池验证** —— 本轮结论全部基于**沪深300 as-of 2019 前 40 只**。换池子需重跑，是本结论最大的外推风险，若落地建议补一个中证500 / 全A 的平行验证。

---

## 4. 验收判据（先定死，防事后找补）

| 档位 | 判定为"优于平台默认 FIXED20"的条件（**须同时满足**） |
|---|---|
| FULL | 终值更高 **且** Sharpe 更高 **且** 破产概率未显著上升 → 否则标记为"高暴露赌注" |
| KELLY_FULL | 终值更高 **且** Sharpe 更高 **且** 回撤未显著恶化 |
| KELLY_HALF | 与 FIXED20 数值接近时，看**自适应是否有信息**——分年看熊市段是否自动降仓 |
| RISK_2PCT_NOM | 预期**最差**（66.7% 重仓）；若结果反而好，先怀疑实现 bug 而非相信结论 |
| RISK_2PCT_REAL | 终值/Sharpe 更高 **且** 尾部（P5 终值、破产概率）优于 FIXED20 |

**通用红线**：
1. 任一档若出现"收益更高但暴露度也更高"，必须同时报暴露度归一化后的数字，否则不算胜出；
2. 不追 argmax。参数敏感性出现孤峰（只有一组好、邻域塌）→ 判为挑参，用默认值不优化；
3. 每档必须过部署率自查（≈100%），未过的结果不进结论表。

---

## 5. 交付物

| 阶段 | 文件 | 产物 |
|---|---|---|
| P0 | `run_sizing_bootstrap.py` | `data/results/position_sizing/sizing_bootstrap.csv`（各档终值分布/破产概率/回撤分布） |
| P1 | `run_position_sizing_ablation.py` | `sizing_ablation.csv` + `sizing_nav.csv`（6 档；部署率全档 ≥98%） |
| P2① | 同脚本 `--grid` | `sizing_grid.csv`（f×cap 解耦网格） |
| P2② | 同脚本 `--capscan` | `sizing_capscan.csv`（A/B/C 三组 + 信号供给上限诊断） |
| P2③ | 同脚本 `--wf` | `sizing_walkforward.csv`（3 不重叠 + 5 滚动窗口） |
| P2④ | 同脚本 `--attrib` | `sizing_attribution.csv`（随机丢弃匹配笔数的归因） |
| 报告 | 本文档 §1.1 + §3.3.2~§3.4 | 结论表 + P3 落地建议 |

**凯利↔风险预算桥梁公式（本次要标定的核心产出）**：

```
仓位 = 风险预算 ÷ 止损距离          ← 风险预算法
仓位 = (b·p − q) / b                ← 凯利法
两者相等时的止损距离 = 风险预算 ÷ f*
```

用本次实测的 p/b 代入，可反解"多大的风险预算才等价于半凯利"——这是视频没打通、而我们能用数据填上的那一格。

---

## 6. 纪律声明

- **opt-in 优先**：所有新增开关默认 `False`，默认行为零变更；
- **单变量隔离**：各档只改仓位算法，信号/出场/成本/股票池全部共用；
- **不静默改默认**：不做 walk-forward + 敏感性双重验证，不动 `config.py` 的 `mean_reversion` 默认值；
- **诚实标注**：视频数学 100% 正确（7 组验算全过）已确认，本实验证伪/证实的只是"它的处方在 A 股真实分布下是否成立"，不是否定其内容；
- **沿用布林带教训**：regime-gate 与 walk-forward **都洗不掉 beta**，暴露度对照与部署率自查是必过关卡。
