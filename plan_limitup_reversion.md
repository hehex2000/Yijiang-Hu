# 开发计划：首板低开均值回归策略回测验证

> 视频来源：BV1KpGw6JE8E ｜ UP：上班做量化的鳄鱼（mid 3546682318916510）｜ 发布 2026-07-31
> 视频评级：**C−**（策略方向有学术依据，但 355%/14%MDD 数字不可信 + 付费漏斗）
> 本计划目标：用平台框架**独立验证核心 edge 是否真存在**，还原「扣成本 + 含退市 + 防前视」的真实表现，而非照搬参数。

---

## 0. 判定与定位

- **核心 idea（可信）**：首板涨停后次日低开的均值回归。A 股涨停板制度造成过度反应 → 次日短期修正，学术上称为 *limit-up overreaction*，1-3 天存在统计正期望。方向合理。
- **数字（不可信）**：355% / 14%MDD（年化 ~66% + 回撤 14% → 夏普 >2，顶级对冲基金水平）。缺成本/滑点/退市/前视披露，且结尾付费课程漏斗。
- **本计划边界**：只验证「edge 是否存在 + 真实幅度多少」，不验证视频参数、不买课、不照搬。
- **强制反过拟合（平台对 B 站复刻硬约束）**：walk-forward + 宽成本建模 + 扩展候选池。

---

## 1. 规则精确化 + 现有 infra 复用映射

| 视频规则 | 回测实现口径 | 复用来源 |
|---|---|---|
| ① 首板（昨涨停、非连板） | T-1：`close ≥ pre_close × limit_up_ratio(code,T-1)`（收封板，不要求 high==close）；T-2…T-20 全部非涨停（首板期限 N=20，近期首个涨停） | `src/small_cap_rotation_selector.limit_up_ratio`（主板 ±10% / 创业板·科创板 ±20%） |
| ② 60 天位置 < 50% | `pos = (close_T-1 − low60) / (high60 − low60) < 0.5`（滚动 60 日） | 自算 |
| ③ 次日低开 3-4% | `open_T / pre_close_T-1 − 1 ∈ [−0.04, −0.03]`；信号在 T 开盘触发，仅用 T-1 收盘 + T 开盘价，**无前视** | `daily.open` / `daily.pre_close` |
| ④ 开盘等权买入，≤3 只/日 | T 开盘价成交，当日候选等权 | `backtest_overnight.py` 脚手架 |
| ⑤ 次日 11:28 盈利且未涨停 → 卖 | 日线近似：以 T+1 收盘为代理；若 T+1 收盘 > 入场 →「已止盈」代理 | 见 Phase A 注记 |
| ⑥ 次日 14:50 仍持且未涨停 → 清仓 | 日线近似：T+1 收盘清；若 T+1 涨停 → 无法卖，延持至打开 / 次日 | `backtest_overnight.next_limit_up_open` 逻辑 |
| 涨跌停无法成交 | 开盘涨停买不进 / 开盘跌停卖不出 / T+1 涨停卖不出延持 | 现有 `if bar[0]/bar[4] ≥ limit_up_ratio → skip` + 跌停续持 |
| 退市归零 | 持仓期间退市 → 归零 | `backtest_small_cap_rotation.n_delist` 处理 |

**关键结论**：平台已有完整的涨停识别 / 隔夜回测 / 退市处理骨架，本策略是「隔夜均值回归」的变体，**只需封装新信号，不必重造引擎**。

**口径决策（胡老师拍板，2026-08-26）**：
- **涨停判定 = 收封板**：`close ≥ pre_close × 涨停比例`，**不要求** `high==close`（收封死），**不包含** 仅 `high≥涨停价` 的炸板（曾触板）。理由：均值回归前提是"昨天市场一致看多到收盘"，收封板最契合；收封死会漏掉尾盘回封的强板，曾触板（炸板）昨天已部分反转、信号弱。
- **首板期限 = N=20 交易日**：T-1 涨停 且 T-2…T-20 全部非涨停（= 近期首个涨停），**非**仅排除连板（N=1）。理由："首板"字面的"首"= 近期首个涨停，比 N=1 更纯净；Phase A 同时跑 N=1 做对照。
- **涨停比例一律用 `limit_up_ratio` 的板级逻辑（±10%/±20%）**，**不用** `backtest_overnight` 里 `1.095` 不区分板级的简化（对创业板/科创板 20% 板会误判）。
- **概念区分（明确）**：首板是"序列中第一个涨停"（强调近期首次），非连板是"T-2 不涨停"（仅排除连续）；收封板用 `close`、`close≥涨停价` 判定，日线 open/high/low/close/pre_close 三种口径（收封板/曾触板/收封死）均支持。
- **出场方案（无分钟线，胡老师拍板 2026-08-26）**：Phase A 主跑 **A 开盘判断**（T+1 开盘盈利即卖、否则收盘卖，贴合视频早卖机制+无前视），同时跑 **B 统一收盘**（纯隔夜基线）+ **C 开盘无脑卖**（朴素反转）做对照；A−B 差量化"盈利早卖"贡献。用开盘价代 11:28 可能系统性偏低，如实标注。

---

## 2. 数据现状核查（已查）

- **DB**：`D:/tu-shareData/astock_daily.db`
- **`daily` 表字段**：`ts_code, trade_date, open, high, low, close, pre_close, change, pct_chg, vol, amount` —— 满足日线回测。
- **分钟/日内表**：全库 34 张表 **无任何 min/1m/5m/intraday/tick 表** → 11:28/14:50 精确退出需外采分钟数据（Phase B）。
- **可复用 infra**：
  - `src/small_cap_rotation_selector.py` → `limit_up_ratio` / `limit_down_ratio`
  - `backtest_overnight.py` / `backtest_overnight_scenarios.py` / `backtest_overnight_variants.py` → 隔夜回测骨架、`is_limit_up_close`、`next_limit_up_open`
  - `backtest_main_rise.py` → `is_limit_up_close` 基准实现（`high==close & close≥pre_close*1.095`）
- **待确认**：① `daily` 是否含退市股历史（影响幸存者偏差）；② `limit_up_ratio` 是否覆盖 ST / 科创板 / 创业板（需读 `src` 确认阈值表）。

---

## 3. 实现方案（两阶段）

### Phase A — 日线近似（快速验证核心 edge，~1-2 天）
- 新文件 `run_limitup_reversion.py`，`import limit_up_ratio`；主循环复用 `backtest_overnight` 的「T-1 识别 → T 开盘执行 → T+1 出场」骨架。
- **信号**：T-1 收盘后识别首板 + 60 天位置过滤；T 开盘检查低开区间 → 买入。
- **出场（日线代理）**：T+1 收盘卖出；拆两类报告子样本（T+1 收盘 > 入场 = 止盈代理；否则 = 持到 14:50 代理）。
- **宽成本**：commission `0.0003` + stamp `0.001`（仅卖） + **slippage `0.003~0.005` 单边**（涨停股薄流动性，高于平台默认 `0.001`）。
- **风控**：涨停买不进 / 跌停卖不出 / 退市归零 全部建模（复用现有函数）。

### Phase B — 分钟精确（还原 11:28/14:50，按需）
- 外采分钟数据（Tushare `pro_bar` 或 Tushare-Downloader 的分钟下载）→ 新表 `daily_1m` / `stock_1m`。
- **精确退出**：T+1 11:28 若盈利且未涨停 → 卖；14:50 仍持 → 清；T+1 涨停 → 持有至打开。
- **偏差量化**：对比 Phase A 代理 vs Phase B 精确，测「日内精确」对收益/回撤的影响（直击视频核心假设）。

---

## 4. 反过拟合强制项

- **宽成本**：如上（千 3 佣 + 千 1 印 + 千 3~5 滑）。
- **扩展候选池**：除首板外，另跑「所有涨停（含连板）次日低开」池；低开区间敏感性 `[2%,3%,4%,5%]` 网格。
- **walk-forward**：2016-2026 滚动（1y 训练 / 1y 测试 或 expanding window），报告样本外 Sharpe/MDD；同时单独跑视频声称的 3 年窗口做对照。
- **幸存者偏差**：确认 `daily` 含退市；不含则补 `download_*` / `namechange` 处理，重跑。
- **前视自检**：信号只用 T-1 及之前收盘 + T 开盘价；选股池用 T-1 收盘时点的成分（不用未来快照）。

---

## 5. 输出与对照

- **指标**：总收益 / 年化 / MDD / 夏普 / 胜率 / 笔数 / 单笔均收益 + 扣费前后对比 + 涨停买不进 / 跌停卖不出 / 退市笔数。
- **对照视频 355%/14%MDD**：给出「可复现？差多少？」的诚实结论。
- **报告**：`docs/limitup_reversion_report.md`（仿 `docs/band_swing_report.md` 范式：规则 + 反过拟合设计 + 实证表 + 结论）。

---

## 6. 文件与提交纪律

- **新增**：`run_limitup_reversion.py`（+ 可能 `download_minute.py` 若做 Phase B）。
- **提交**：作为有效研究脚本，按本轮回测纪律 —— **先本地、用户拍板后再推，不擅自 push**（与 `run_oversold_bounce` / `run_etf_tangency` 同族，归入「有效/研究」类，不归入「已确认无效不推」集）。
- **命名**：`run_limitup_reversion.py`。

---

## 7. 风险/陷阱清单（诚实）

- 涨停股开盘滑点巨大 → 宽成本可能吞没 edge（**核心检验点**）。
- 低开 3-4% 是集合竞价结果，盘前不可知 → 回测用实际开盘价 = 上帝视角，实盘难保证成交在该区间（如实标注）。
- 样本稀疏（每日 0-3 只）→ 统计噪声大，小样本高收益可能纯运气 → walk-forward 必做。
- 幸存者偏差 / 退市（必查）。
- 视频数字无成本 / 无滑点 / 无退市 → 直接对照会高估，须扣费后比较。

---

## 8. 下一步

确认后先落 **Phase A（日线近似）** 出实证，据 edge 是否显著决定是否值得 Phase B 分钟精确化。若 Phase A 扣费后已显负 edge，则直接结案（同 band_swing 范式），不浪费时间采分钟数据。
