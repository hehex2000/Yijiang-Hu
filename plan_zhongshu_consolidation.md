# 缠论中枢 → 量化模块：分析与开发计划

> 状态：计划阶段（用户 2026-07-29 要求先出分析+开发计划，暂未写实现代码）
> 关联：道氏理论 `--breadth-confirm`（已落地、默认关、判定为负贡献）

---

## 0. 结论先置（TL;DR）

- 缠论里**能机器执行**的部分，本质就是「波动率收缩 + 区间突破」，平台已覆盖约 80%。
- 真正**缺口**只有一个：**盘整期（中枢）回避**——「吵架的地方别动手」。
- **推荐落地项**：`--consolidation-filter`（中枢震荡回避滤镜），作为 v2 可选滤镜，默认关、跑对照、再决定进菜单（与 `--breadth-confirm` 同样处置流程）。
- 笔/段/分型等主观画法**一律不碰**（无法标准回测，等同 `--breadth-confirm` 必须「先验正贡献」才进菜单）。

---

## 1. 概念可量化性评估

| 缠论概念 | 能否机器化 | 平台已有对应 | 结论 |
|---|---|---|---|
| 笔 / 段 / 分型 | ❌ 主观、标准模糊 | 无 | 不碰 |
| **中枢（重叠区间）** | ✅ 布林带宽低位 / ATR 收缩 | 无 | **本项目目标** |
| 中枢方向（客栈上移/下移） | ✅ MA / pivot 链 | v2 的 HS300 MA200 月检 | 已有（备选 C 可增强） |
| 真出门（放量突破+回踩） | ✅ Donchian 突破 + 量能 | 海龟 volume_filter | 已有 |
| 假出门（冲出又缩回） | ✅ 收回通道止损 | 无 | 备选 B |
| 多级别嵌套 | ⚠️ 需周线聚合 | 无 | 备选 D（性价比低） |
| 市场广度确认 | ✅ 成分股站上 MA 比例 | v2 `--breadth-confirm` | 已有 |

> 关键认知：缠论「中枢」= 布林带收窄 / 波动率收缩区间。用代码写出来比手工画笔段准得多，且可回测。

---

## 2. 目标模块设计：中枢震荡回避滤镜

### 2.1 中枢识别（机器化定义）

- **主方案**：20 日布林带宽 `bbw = (upper - lower) / mid`；计算其过去 `lookback=120` 日滚动分位 `pct`；`pct < th(默认 0.25)` → 判定为「中枢期」（价格来回至少三趟重叠的平地）。
- **备选方案**：20 日 `ATR% = ATR(20) / close`，同样滚动分位法。
- **为什么用分位而非绝对值**：A 股波动率结构性下行，绝对值阈值会随时间失效；分位法自适应。
- **数据源**：`daily` 表（需 `high/low/close`）即可算布林带，**无需新表**（实施前先 `PRAGMA` 确认字段）。

### 2.2 状态与行为（对应缠论「三看 / 三步」）

| 缠论动作 | 机器实现 | 仓位行为 |
|---|---|---|
| 在中枢里看戏 | `pct < th` | 降到 `--consolidation-half`（默认 0.5，或 0=不新建）→「吵架的地方别动手」 |
| 真出门 | 带宽分位回升过中位 **且** 收盘突破上轨 **且** 量能 > 均值 | 恢复满仓 →「出门再跟」 |
| 假出门 | 突破后 `K=5` 日缩回带内 | 维持降仓（避免追高挨打） |

### 2.3 与 v2 现有三件套的整合

- 现有 `_target_ratio` 两维：`leg1`=指数 MA200（方向）、`leg2`=breadth（确认）。
- 新增 `leg3`=consolidation（盘整回避）作为**独立门控**，与趋势/确认**正交**。
- **组合方式**：`final_ratio = min(trend_ratio, consolidation_ratio)`。
  - 理由：盘整回避是「风险降档」而非「方向判断」，取 min 最稳；同时支持「关趋势、只开中枢」单独跑。
- 起点口径：首日按 T-1 中枢状态初始化（镜像现有 `_r0` 逻辑）。

### 2.4 接线（镜像 `--breadth-confirm` 的写法）

- 新增函数 `index_bollinger_width(idx_code, td, win=20, lookback=120)`（复用 `_IDX` 缓存体系）。
- 新增 `index_in_consolidation(idx_code, td, win=20, lookback=120, th=0.25)` → `(bool, bbw_pct)`。
- `_target_ratio()` 增加 consolidation 维度（取 min 组合）。
- CLI 参数：
  - `--consolidation-filter`（开关，默认关）
  - `--consolidation-th 0.25`（分位阈值）
  - `--consolidation-win 20`（布林窗口）
  - `--consolidation-half 0.5`（中枢期目标仓位比）
- 落盘：`backtest_v2_consol_*.csv`（**不覆盖**基线）。
- 报告：加「v2 基线(纯 MA200) vs v2+中枢」对照表（镜像 `--breadth-confirm` 那段）。
- **默认关，不进主菜单**（与 `--breadth-confirm` 一致）。

---

## 3. 回测验证方案

- **对照**：v2 基线（纯 MA200）vs v2 + consolidation。
- **指标**：年化、最大回撤 MDD、夏普、月切换次数、中枢期平均仓位占比。
- **重点观察区间**：
  - 2015-06 ~ 2016-02 股灾（看是否因降仓少亏，注意 v2 月检本就滞后）
  - 2018 全年阴跌盘整
  - 2021-02 后抱团瓦解 + 2022–2024 震荡（中枢多发期，核心验证区）
  - 2019–2020 慢牛（应不误伤，中枢占比低）
- **阈值网格**：`th ∈ {0.10, 0.20, 0.25, 0.30, 0.40}` 各跑一遍，选 MDD 改善最大且不伤年化的点。

### 决策门槛（什么结果才进菜单）

满足**全部**才考虑进菜单，否则保持默认关、记档为「负贡献/中性」：
1. 年化不降 > 0.5pp（或持平）；**且**
2. MDD 改善 ≥ 2pp，**或** 夏普改善 ≥ 0.05；**且**
3. 中枢期平均仓位确实显著低于满仓（证明滤镜在干活，不是摆设）；**且**
4. 月切换次数不过度增加（避免手续费侵蚀收益）。

---

## 4. 风险与已知坑

- **带宽阈值敏感**：25% 仅是起点，必须做网格（见 §3）。
- **与 breadth 共线**：两者都含「市场状态」信息，叠加可能双降仓过度 → 验证时也要跑「v2 + breadth + consolidation」三重组合，看是否边际递减。
- **月度采样丢失**：中枢判定在月检点（T-1）采样，可能漏掉月内中枢；可接受（v2 本就月度节奏）。
- **可能负贡献**：若 A 股「盘整后多向上突破」，降仓会踏空 → 预期管理：先验证、不预设。
- **数据前置检查**：实施前先 `PRAGMA table_info(daily)` 确认 `high/low` 字段存在（布林带必需）。

---

## 5. 实施步骤（任务拆解）

1. `PRAGMA` 查 `daily` 表结构，确认 `high/low` 字段（布林带前置依赖）。
2. 实现 `index_bollinger_width()` + `index_in_consolidation()`（复用 `_IDX` 缓存）。
3. 改造 `_target_ratio()` 接入 consolidation 维（min 组合）。
4. 加 CLI 参数 + 报告对照表 + CSV 落盘（镜像 breadth）。
5. 跑「基线 vs +consol」对照，做 `th` 阈值网格。
6. 出结论 + 按 §3 决策门槛判定。
7. （如通过）评估进菜单；否则记档默认关（同 breadth）。

---

## 6. 备选方案简版计划

- **B. 海龟假出门退出**：在 turtle 引擎加「唐奇安突破后 K 日收盘收回通道内→平仓」，专治鞭梢；实施前先读 turtle 文件结构。
- **C. 中枢方向替代 MA200（P1）**：实现 pivot high/low 链（higher-high + higher-low）作为 v2 可选趋势腿，比单 MA 更贴「客栈上移/下移」。▶ 续篇已具体化为「线段 = Zigzag swing structure」，见 `plan_zhongshu_pen_segment.md`。
- **D. 多级别嵌套**：从 `daily` 聚合周线 OHLC，日线方向用周线过滤；工作量最大、偏离月度节奏，**建议搁置**。

---

## 7. 决策记录

- 2026-07-29：用户看完缠论中枢科普视频，要求头脑风暴可用之处 → 给出概念→平台对照 + 推荐 A（中枢回避滤镜）。
- 用户选择：**先不写代码，先写分析开发计划**。
- 本文件为计划交付物，待用户拍板后按 §5 实施。
